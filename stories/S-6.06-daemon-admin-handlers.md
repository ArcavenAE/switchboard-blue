---
artifact_id: S-6.06-daemon-admin-handlers
document_type: story
level: ops
story_id: S-6.06
title: "Daemon-Side Admin RPC Handlers (admin.key.register / revoke / expire / list-keys)"
status: draft
producer: product-owner
timestamp: 2026-06-29T00:00:00
version: "1.0"
phase: 2
epic: E-6
wave: 5
priority: P1
scope_phase: E
estimated_points: 5
bc_traces:
  - BC-2.05.004
  - BC-2.07.001
vp_traces: [VP-048]
subsystems: [network-management, admission-security]
architecture_modules: [cmd/switchboard, internal/svtnmgmt, internal/mgmt, internal/admission]
tdd_mode: strict
cycle: v1.0.0-greenfield
depends_on: [S-6.02]
blocks: [S-W5.02]
inputDocuments:
  - '.factory/specs/behavioral-contracts/ss-05/BC-2.05.004.md'
  - '.factory/specs/behavioral-contracts/ss-07/BC-2.07.001.md'
  - '.factory/specs/verification-properties/VP-048.md'
  - '.factory/specs/architecture/ARCH-08-module-dependency-rules.md'
acceptance_criteria_count: 5
# Origin: minted per CR-W5-SCOPE-SPLIT ruling (adversary Pass 1 finding F-001).
# S-6.02 shipped sbctl CLI commands + SVTNManager methods; the daemon never
# registered handlers for those RPCs. This story closes the production gap.
---

# S-6.06: Daemon-Side Admin RPC Handlers

> **Execute:** `/vsdd-factory:deliver-story S-6.06`

## Scope Note

S-6.02 shipped the `SVTNManager` methods (`RegisterKey`, `RevokeKey`, `ExpireKey`) and
`sbctl admin` CLI commands. However, the `cmd/switchboard` daemon never registered
`mgmt.Handler` entries for those RPCs — every call site passed `startMgmtServer(..., nil)`.
`mgmt_wire.go:259` contains `TODO(CR-002)` documenting this gap.

Adversary Pass 1 on S-6.02 (finding F-001, CRITICAL) confirmed: admin RPC commands
invoked via `sbctl admin` against a live daemon return `E-RPC-010 "unknown command"`
because handlers are nil. CLI tests pass only because they use a fake/stub server.

This story wires the missing handler layer. It does not re-implement SVTNManager methods
(owned by S-6.02) nor the sbctl CLI dispatch (owned by S-6.02/S-6.03). It only adds the
`[]mgmt.Handler` builder in `cmd/switchboard` that bridges incoming RPC envelopes to the
already-existing SVTNManager methods.

Per the HUMAN DECISION (CR-W5-SCOPE-SPLIT), S-6.02 ships with an explicit deferral note
and S-W5.02 (e2e harness) depends on this story before it can exercise admin RPCs end-to-end.

## Behavioral Contracts

| BC | Title | PCs covered |
|----|-------|------------|
| BC-2.05.004 | Key Lifecycle — Register, Revoke, Expire | PC-1 (register), PC-2 (revoke), PC-3 (expire) — handler layer |
| BC-2.07.001 | Control Node Creates and Destroys SVTNs | PC-1 (create) — handler registration; PC-2 (bootstrap) — handler registration |

## Narrative

- **As a** control node operator
- **I want** `sbctl admin key register / revoke / expire / list-keys` to work against
  a running `switchboard` daemon
- **So that** key lifecycle management works end-to-end (not just against a test stub)

## Acceptance Criteria

### AC-001 (traces to BC-2.05.004 postconditions 1–3; BC-2.07.001 PC-1)
`cmd/switchboard/admin_handlers.go` exports a function with signature
`BuildAdminHandlers(m *svtnmgmt.SVTNManager) []mgmt.Handler` that returns handlers for
`admin.key.register`, `admin.key.revoke`, `admin.key.expire`, and `admin.list-keys`.
Each handler:
- Parses the JSON args field from the incoming `mgmt.Request`
- Calls the appropriate `SVTNManager` method
- Maps sentinel errors to wire-level error codes (see Error Code Map section)
- Returns a `mgmt.Response` with `ok: true` and serialized result on success

Tests (unit, stdlib `testing`, table-driven, no testify):
- `TestBuildAdminHandlers_KeyRegister_HappyPath`
- `TestBuildAdminHandlers_KeyRevoke_HappyPath`
- `TestBuildAdminHandlers_KeyExpire_HappyPath`
- `TestBuildAdminHandlers_ListKeys_HappyPath`
- `TestBuildAdminHandlers_KeyRegister_ErrorMapping` (ErrSVTNNotFound → E-SVTN-003)
- `TestBuildAdminHandlers_KeyRevoke_ErrorMapping` (ErrKeyNotRegistered → E-ADM-013; ErrRoleMismatch → E-ADM-014)

### AC-002 (traces to BC-2.05.004 PC-2; HOLD-001 hybrid pattern; ADR-004)
The `admin.key.revoke` handler:
1. Unmarshals `current_role` from the wire JSON args field
2. Converts it to `admission.KeyRole`
3. Passes it as `currentRole` to `SVTNManager.RevokeKey`

End-to-end tests using a real `mgmt.Server` + real Unix socket (NOT a fake server):
- `TestE2E_AdminRevoke_RoleMismatch`: caller claims `console` role, key is `control` →
  response contains `E-ADM-014`
- `TestE2E_AdminRevoke_ControlWithoutConfirm`: control-to-control revocation without
  `"confirm":true` in args → `E-ADM-004`
- `TestE2E_AdminRevoke_ControlWithConfirm`: control-to-control with `"confirm":true` →
  success; key no longer present in `AdmittedKeySet`

### AC-003 (traces to BC-2.05.004 PC-1, PC-3; BC-2.07.001 PC-1)
`admin.key.register`, `admin.key.expire`, and `admin.list-keys` handlers are each
registered and exercised in end-to-end tests against a real `mgmt.Server` + real
Unix socket.

Tests:
- `TestE2E_AdminRegister_HappyPath`: registers key; verifies it appears in list-keys response
- `TestE2E_AdminExpire_HappyPath`: sets TTL; verifies expiry timestamp set on key
- `TestE2E_AdminListKeys_HappyPath`: registers two keys; list-keys returns both

### AC-004 (traces to BC-2.05.004 PC-1; ADR-004 role-exclusion; ARCH-08 §6.6.2)
Handler registration is gated by daemon mode per ADR-004 role-exclusion:
- Control-mode daemon: all four admin handlers registered via `BuildAdminHandlers`
- Access, console, and router-mode daemons: `BuildAdminHandlers` is NOT called; those
  daemon-mode entry points pass a nil or empty handler slice for admin commands

Tests assert per-daemon-mode behavior:
- `TestControlMode_AdminHandlersRegistered`: sends `admin.key.register` to control
  daemon socket; gets `ok: true` (or a domain error, not `E-RPC-010`)
- `TestAccessMode_AdminHandlersNotRegistered`: sends `admin.key.register` to
  access daemon socket; gets `E-RPC-010 "unknown command"` (or connection refused)
- Same pattern for console and router modes

### AC-005 (traces to BC-2.05.004 PC-3; defense-in-depth; DI-003)
Server-side duration validation in the `admin.key.expire` handler:
- Handler re-parses the `after` duration field from the raw JSON args (even though
  sbctl CLI also validates it — defense-in-depth)
- Rejects `ttl <= 0` with `E-CFG-001`
- Rejects `ttl > 100*365*24*time.Hour` (100 years) with `E-CFG-001`

Test bypasses sbctl CLI (posts JSON directly to Unix socket):
- `TestE2E_AdminExpire_ServerRejectsTTLNegative`: POST `{"after":"-1h"}` directly to
  socket → response contains `E-CFG-001`
- `TestE2E_AdminExpire_ServerRejectsTTLZero`: POST `{"after":"0s"}` → `E-CFG-001`
- `TestE2E_AdminExpire_ServerRejectsTTLTooLong`: POST `{"after":"876001h"}` (>100 years)
  → `E-CFG-001`

## Error Code Map

Handler-level sentinel error → wire-level error code mapping:

| Sentinel Error | Wire Code | Message Pattern |
|---------------|-----------|-----------------|
| `svtnmgmt.ErrSVTNNotFound` | E-SVTN-003 | `"SVTN not found: <id>"` |
| `svtnmgmt.ErrSVTNAlreadyExists` | E-SVTN-002 | `"SVTN already exists: <id>"` |
| `admission.ErrKeyNotRegistered` | E-ADM-013 | `"key not registered: <fingerprint>"` |
| `svtnmgmt.ErrRoleMismatch` | E-ADM-014 | `"role mismatch: caller is <role>, key is <role>"` |
| `svtnmgmt.ErrControlRevocationRequiresConfirm` | E-ADM-004 | `"control-to-control revocation requires --confirm"` |
| invalid/out-of-range duration | E-CFG-001 | `"invalid duration: <value>"` |

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| BuildAdminHandlers | cmd/switchboard/admin_handlers.go | boundary |
| SVTNManager (existing) | internal/svtnmgmt | boundary |
| mgmt.Server (existing) | internal/mgmt | effectful |
| Daemon mode entry points | cmd/switchboard/{control,access,console,router}.go | effectful |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | `admin.key.register` with malformed JSON args | E-CFG-001 "invalid request args: <parse error>" |
| EC-002 | `admin.key.revoke` with unknown `current_role` string | E-CFG-001 "invalid role: <value>" |
| EC-003 | `admin.list-keys` on SVTN with zero admitted keys | `ok: true`; `data.keys` is empty array (not null) |
| EC-004 | Handler called with nil SVTNManager (misconfiguration) | Panic at startup — caught by test asserting BuildAdminHandlers panics on nil input |
| EC-005 | `admin.key.expire` with `after` field absent from args | E-CFG-001 "missing required field: after" |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| cmd/switchboard/admin_handlers.go | boundary | Depends on SVTNManager (boundary type) + mgmt.Handler (interface) |
| internal/svtnmgmt (existing) | boundary | Manages key set; unchanged by this story |
| cmd/switchboard/{control,access,console,router}.go | effectful | Daemon startup; wires handlers into mgmt.Server |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~1,400 |
| BC-2.05.004.md | ~700 |
| BC-2.07.001.md | ~500 |
| VP-048.md | ~600 |
| internal/svtnmgmt/svtnmgmt.go (S-6.02 impl) | ~900 |
| internal/mgmt/mgmt.go (S-W5.01 impl) | ~800 |
| cmd/switchboard/mgmt_wire.go (TODO(CR-002) context) | ~400 |
| cmd/switchboard/control.go + access.go + console.go + router.go | ~600 |
| New files: admin_handlers.go + admin_handlers_test.go | ~800 |
| New e2e test file | ~400 |
| Tool outputs overhead | ~400 |
| **Total** | **~7,500** |
| Agent context window | 200K |
| **Budget usage** | **~3.8%** |

## Tasks (MANDATORY)

1. [ ] Read `cmd/switchboard/mgmt_wire.go` at line 259 — understand `TODO(CR-002)` and the `startMgmtServer` call sites that pass nil handlers
2. [ ] Read `internal/mgmt/mgmt.go` — understand `mgmt.Handler` interface, `mgmt.Request`/`mgmt.Response` types, and how `NewServer` accepts handlers
3. [ ] Read `internal/svtnmgmt/svtnmgmt.go` — confirm sentinel error names: `ErrSVTNNotFound`, `ErrSVTNAlreadyExists`, `ErrKeyNotRegistered`, `ErrRoleMismatch`, `ErrControlRevocationRequiresConfirm`
4. [ ] Read `cmd/switchboard/control.go`, `access.go`, `console.go`, `router.go` — identify where `startMgmtServer` is called; confirm control mode is the only one that should receive admin handlers
5. [ ] Write failing tests for AC-001 (unit: `admin_handlers_test.go`) and AC-004/AC-005 (e2e: `admin_handlers_e2e_test.go` with `//go:build integration` tag)
6. [ ] Verify Red Gate (density ≥ 0.5): all tests red before any implementation
7. [ ] Create `cmd/switchboard/admin_handlers.go`: implement `BuildAdminHandlers(m *svtnmgmt.SVTNManager) []mgmt.Handler` with all four handlers + error code mapping
8. [ ] Implement per-handler JSON parsing with full error validation (AC-005 duration bounds check in expire handler)
9. [ ] Wire `BuildAdminHandlers` into `cmd/switchboard/control.go` only; leave access/console/router unchanged (nil or empty handlers for admin namespace)
10. [ ] Write and pass AC-002 e2e tests (real mgmt.Server + real Unix socket): role-mismatch, no-confirm, with-confirm
11. [ ] Write and pass AC-003 e2e tests: register + list-keys, expire + inspect, list-keys empty
12. [ ] `just fmt && just lint` pass (zero warnings; no `//nolint` without justification)

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-6.02 | `SVTNManager` methods use value receivers; `RevokeKey` takes `currentRole admission.KeyRole` as HOLD-001 hybrid pattern | `internal/svtnmgmt/svtnmgmt.go` is the boundary type; sentinel errors are package-level `var` per go.md | `startMgmtServer(..., nil)` pattern: nil handlers silently cause `E-RPC-010` for ALL admin commands. `TODO(CR-002)` at mgmt_wire.go:259 marks the gap. |
| S-W5.01 | `mgmt.NewServer(ln, daemonKey, ops, handlers)` API; handler dispatch is by `Command` field | `mgmt.Handler` interface: `Command() string` + `Handle(ctx, req) (Response, error)` | Handler nil-slice is valid — server starts but returns E-RPC-010 for unregistered commands. This is intentional for non-control daemons. |
| S-6.03 | `sbctl` dispatch marshals args into a `map[string]any` JSON field in the request envelope | `client.go` Authenticate() + dispatch() pattern | The `current_role` field in revoke args must match the JSON key the CLI sends — verify the exact field name in S-6.02's sbctl admin revoke implementation before parsing on handler side. |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| Only control-mode daemon registers admin handlers | ADR-004 (role-exclusion); ARCH-08 §6.6.2 | TestControlMode_AdminHandlersRegistered passes; TestAccessMode_AdminHandlersNotRegistered / console / router pass |
| `cmd/switchboard` MUST NOT import `internal/frame` | ARCH-08 position 15 | `go build ./cmd/switchboard/...` must not introduce new `frame` import |
| `admin_handlers.go` imports only: `internal/svtnmgmt`, `internal/admission`, `internal/mgmt` | ARCH-08 layering; forbidden dependencies below | `go build ./cmd/switchboard/...` + import review |
| HOLD-001 hybrid: `current_role` is caller-supplied + cross-checked by SVTNManager | ARCH-04 v1.8; S-6.02 RevokeKey design | TestE2E_AdminRevoke_RoleMismatch; TestE2E_AdminRevoke_ControlWithoutConfirm |
| Server-side defense-in-depth: duration validation independent of CLI | DI-003 (defense-in-depth invariant) | TestE2E_AdminExpire_ServerRejectsTTLNegative; posted directly to socket (bypassing sbctl) |
| E2E tests use `//go:build integration` tag | ARCH-12 §e2e tagging pattern (per S-W5.02) | Build tag in e2e test file header |
| Unix socket paths unique per test run | OS socket uniqueness | `t.TempDir()` for socket directory |

## Forbidden Dependencies

The following MUST NOT appear as imports in `cmd/switchboard/admin_handlers.go`:

| Forbidden | Why |
|-----------|-----|
| `internal/frame` | Data-plane codec; ARCH-08 position 15 |
| `internal/routing` | Data-plane package |
| `internal/multipath` | Data-plane package |
| `internal/arq` | Data-plane package |
| `internal/replay` | Data-plane package |
| `internal/paths` | Data-plane package |
| `internal/halfchannel` | Data-plane package |
| `internal/session` | Data-plane package |
| `internal/tmux` | Data-plane package |
| `internal/discovery` | Data-plane package |
| `cmd/sbctl` | CLI package — circular and wrong layer |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Go | 1.25.4 | Per go.mod |
| `encoding/json` | stdlib | JSON args parsing in handlers |
| `time` | stdlib | Duration parsing and bounds validation (AC-005) |
| `crypto/ed25519` | stdlib | E2E test key generation (avoid dependency on `~/.ssh/id_ed25519`) |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| `cmd/switchboard/admin_handlers.go` | create | `BuildAdminHandlers(m *svtnmgmt.SVTNManager) []mgmt.Handler` + all four handler types |
| `cmd/switchboard/admin_handlers_test.go` | create | Unit tests for AC-001, error mapping (AC-002 unit path) |
| `cmd/switchboard/admin_handlers_e2e_test.go` | create | `//go:build integration` — AC-002 role-mismatch / no-confirm / with-confirm; AC-003 register+list, expire+inspect; AC-004 per-mode; AC-005 direct-socket bypass |
| `cmd/switchboard/control.go` | modify | Wire `BuildAdminHandlers(svtnMgr)` into control-mode `startMgmtServer` call; resolve `TODO(CR-002)` |
| `cmd/switchboard/access.go` | modify (noop or comment) | Confirm nil/empty admin handlers in access-mode call site; add comment citing AC-004 rationale |
| `cmd/switchboard/console.go` | modify (noop or comment) | Same as access.go |
| `cmd/switchboard/router.go` | modify (noop or comment) | Same as access.go |

## Changelog

| Version | Date | Author | Change |
|---------|------|--------|--------|
| 1.0 | 2026-06-29 | product-owner | Initial creation — minted per CR-W5-SCOPE-SPLIT ruling. Adversary Pass 1 on S-6.02 found F-001 CRITICAL: daemon registers no admin RPC handlers; TODO(CR-002) at mgmt_wire.go:259 documents gap. Human decision: Option 3 (split). S-6.02 ships CLI-layer-only with explicit deferral note; this story closes the production gap end-to-end. Wave 5, P1, 5pts. depends_on: S-6.02. blocks: S-W5.02. |
