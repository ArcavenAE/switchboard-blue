---
artifact_id: S-7.04-FU-DRAIN-WIRE
document_type: story
level: ops
story_id: S-7.04-FU-DRAIN-WIRE
title: "DRAIN-over-SVTN wire propagation — per-node observer registration in drain coordinator"
status: ready
producer: story-writer
timestamp: 2026-07-11T00:00:00Z
version: "1.3"
phase: 2
epic: E-7
wave: backlog
priority: P2
scope_phase: PE
points: 5
estimated_points: 5
bc_traces:
  - BC-2.09.002   # PC-1 DRAIN broadcast to nodes; EC-003 unresponsive-registered-observer forced-exit (v1.2 best-effort-delivery ruling consumed)
  - BC-2.01.008   # v1.1 — control_type schema home; opcode table; EC-002 payload_len<4 → E-PRT-002; Postcondition 4 strengthened no-logging (PO obligation); new Invariant 2 terminal-consumer-by-construction (PO ruling consumed, F-DW-SP3-002/004)
vp_traces:
  - VP-037        # two-stage discharge: stage-1 wire round-trip + Wait-within-window (this story); lock stays false; stage-2 migration e2e named follow-on
subsystems: [deployment-operations]
architecture_modules:
  - internal/drain         # RegisterObserver (single-observer seam); defer recover() in Signal fan-out; no new imports
  - internal/frame         # FrameTypeCtl (0x03) reuse; EncodeOuterHeader for DRAIN frame assembly; no new constant
  - internal/netingress    # ServeConfig (gains IfaceIDSeed, v1.3) + NodeHandle (gains Done, v1.3) types; Serve owns DATA creation (IfaceID/Send/Done allocation, v1.3 ownership split) + OnAccept hook wiring (Q-SEAM); package-doc rewrite (:15-17) permitting the internal/routing import
  - cmd/switchboard        # per-node send map (sync.Map[InterfaceID, *nodeConn], v1.3 — send/done/doneOnce, never-close invariant); OnAccept closure owns BEHAVIOR only (v1.3 ownership split); writerWG + bounded shutdown-flush phase before ingressCancel (v1.3, F-DW-SP3-005); single startup drain observer + drainObserverFiredHook test observability (Q-AC003, v1.3); nodeConnHook test observability (Q-AC002); ctl payload_len guard + control_type dispatch, route closure only, no-log default arm + Inv-2 basis (Q-CTL-GUARD, v1.3)
tdd_mode: strict
cycle: v1.0.0-greenfield
depends_on:
  - S-7.04    # drain coordinator (merged PR #101/#103); RegisterObserver seam in place
  - S-BL.NI   # network ingress — per-node connection concept; netingress.ServeConn
  - S-BL.PE-RECEIVE-LOOP   # PE receive/forward loop — MERGED PR #118 @ e940fc2 2026-07-11; FrameFn + SetFrameCallback seam live
blocks: []
inputDocuments:
  - '.factory/decisions/S-7.04-FU-DRAIN-WIRE-placement-note.md'   # v1.3 — Q1–Q7 binding rulings + Q-SEAM (ownership split + shutdown ordering guarantee, v1.3)/Q2-AMENDED/Q3-AMENDED/Q4-AMENDED/Q-SINGLE-OBS/Q-CTL-GUARD/Q-AC002/Q-AC003 (drainObserverFiredHook, v1.3)/Q-AC005; FCL 11-file skeleton; spec-adversarial pass-3 remediation (F-DW-SP3-001..008, all confirmed); pass-2 remediation (F-DW-SP2-001..010, all confirmed) carried forward; FO-RECV-FWD-001 DEFERRED (not consumed); Appendix A symbol sweep at e940fc2
  - '.factory/specs/behavioral-contracts/ss-09/BC-2.09.002.md'    # v1.2 — PC-1 DRAIN broadcast; EC-003 forced-exit; Q3.P1 BINDING option (2): no wire ACK; observer returns after queuing
  - '.factory/specs/behavioral-contracts/ss-01/BC-2.01.004.md'    # v1.4 — terminal-consumer ctl carve-out; Inv-2 DI-001 amended; BC-2.01.008 as schema home cross-ref
  - '.factory/specs/behavioral-contracts/ss-01/BC-2.01.005.md'    # v1.2 — router-terminated-ctl carve-out in PC-2 (PO ruling consumed; placement-note cross-ref only, no story-specific AC discharge)
  - '.factory/specs/behavioral-contracts/ss-01/BC-2.01.008.md'    # v1.1 — control_type schema home; opcode table (0x01=DRAIN, 0x02=RESYNC reserved); EC-002 payload_len<4 → E-PRT-002; PC-4 strengthened no-logging (F-DW-SP3-002); new Invariant 2 terminal-consumer-by-construction (F-DW-SP3-004)
  - '.factory/specs/verification-properties/VP-037.md'            # v1.5 — two-stage discharge lifecycle; lock stays false this story; stage-1 wire round-trip binding (discharge-trace corrected v1.2; read-determinism dependency-noted v1.3 per Shutdown ordering guarantee); stage-2 named follow-on; unchanged this pass
  - '.factory/specs/architecture/ARCH-02-protocol-stack.md'       # current — § "Outer Header Format" (NOT §3.1 — ARCH-02 has no §3.1, corrected v1.2) frame_type table; ctl (0x03) payload note obligation
  - '.factory/specs/architecture/ARCH-08-dependency-graph.md'     # v2.11 — bumping to v2.12 same-commit as netingress.go §6.5 import-set edit (F-DW-SP2-008); FCL row 11
acceptance_criteria_count: 5
backlog_origin:
  source: S-7.04-DELIVERY
  deferred_from: S-7.04 AC-003 + AC-004
  drift_items_consumed:
    - DRIFT-HS006-DRAIN-CLI-MISSING
    - DRIFT-HS006-DRAIN-TIMEOUT-FORCED-EXIT-UNEVIDENCED
  notes: >
    Elaborated from v0.2-backlog-stub consuming placement note v1.0 (Q1-Q7 all answered).
    Placement note confirmed: FrameTypeCtl reuse (Q1), DRAIN dispatch via single startup
    drain observer over live sync.Map (Q2/Q-SEAM/Q2-AMENDED/Q-SINGLE-OBS — FO-RECV-FWD-001
    NOT consumed; nil ForwardFunc stays; interfaceSet stays []{peIfaceID}), single observer
    registered at runRouter setup (Q3-AMENDED), VP-037 two-stage discharge via
    TestE2E_RouterDrain_WireRoundTrip stage-1/lock-stays-false (Q4-AMENDED), EC-003 via
    drainCoordHook + cfg.DrainTimeout (Q-AC005), 10-file FCL skeleton (Q6), non-goals
    confirmed (Q7). v1.1: consumed note v1.1 (F-DW-SP1-001..012 remediation + PO rulings
    BC-2.09.002 v1.2/BC-2.01.004 v1.4/BC-2.01.008 v1.0/Q3.P1→BINDING);
    FO-RECV-FWD-001 consumed→DEFERRED; AC-002 respecified to Q-SEAM seam contract;
    AC-003 reshaped + PROVISIONAL removed; AC-004 lock-stays-false; AC-005 drainCoordHook
    shape; FCL 9→10 rows; tasks reworked. v1.2: consumed note v1.2 (spec-adversarial
    pass-2 remediation, F-DW-SP2-001..010 all confirmed). AC-004 discharge-trace
    corrected to the real-runRouter pattern (testenv.NewWithRouters never ran a real
    runRouter — unrealizable); AC-002 send/done never-close invariant closes a
    close-race panic against the single observer's live-map Range; AC-003's Q-CTL-GUARD
    firmed to the netingress route closure exclusively (not the frozen PE FrameFn
    closure) plus a second unknown-control_type pin test; nodeConnHook (Q-AC002) added
    as the send-map registration/removal test observable, doubling as the accept/
    register barrier AC-001 and AC-004 both require before triggering drain; AC-005
    EC-003 shape changed to extend TestRunRouter_ForcedExitPastDrainTimeout rather than
    add a duplicate, and drops the flaky drain.ErrTimeout assertion; FCL 10→11 rows
    (ARCH-08-dependency-graph.md added, v2.11→v2.12 same-commit); AC-001 and AC-004 kept
    as separate ACs sharing one harness helper (story-writer ruling — distinct BC vs VP
    obligations). Test surface recounted. v1.3: consumed note v1.3 (spec-adversarial
    pass-3 remediation, F-DW-SP3-001..008 all confirmed) and BC-2.01.008 v1.1 (PO
    rulings PC-4 strengthened no-logging + new Invariant 2 consumed). AC-002 respecified
    around the Q-SEAM ownership split (netingress owns DATA creation, runRouter's
    OnAccept owns BEHAVIOR only; send-map value type is *nodeConn, not a bare chan
    []byte); new production design constraint — Shutdown ordering guarantee (writerWG +
    bounded flush phase between drainCoord.Wait and ingressCancel, closes the AC-001/
    AC-004 egress race) with AC-001/AC-004 read-step cross-references (recipes
    unchanged); AC-003 observable replaced with drainObserverFiredHook (the v1.1 "flag
    inside the observer closure" had no defined mechanism); ctl guard's unknown-
    control_type default arm loses its log call entirely (BC-2.01.008 PC-4 v1.1) and
    gains an explicit Invariant-2 terminal-consumer basis with a named revisit trigger;
    E-PRT-002 log loses its conn.RemoteAddr() clause (no conn in scope in the route
    closure). FCL stays at 11 rows; row 5 reworded from MODIFIED to NO CHANGE EXPECTED;
    rows 1/10 gain writerWG/doneOnce/drainObserverFiredHook/IfaceIDSeed. Test surface
    composition unchanged at ~8 net-new (bodies reworded only).
---

# S-7.04-FU-DRAIN-WIRE: DRAIN-over-SVTN Wire Propagation

## Narrative

- **As an** operator gracefully shutting down a PE router
- **I want** connected nodes to receive a DRAIN signal over their SVTN channels before
  the router disconnects
- **So that** nodes can migrate active sessions to alternate routers within the drain
  window, rather than losing them abruptly

## Context

S-7.04 (merged PR #101/#103) delivered `internal/drain.Drain` with `RegisterObserver`,
`Signal`, `Wait`, and the `drainCoordHook` test-injection mechanism. The coordinator's
liveness model (fan-out-then-race under a bounded child context, EC-003 ErrTimeout) is
complete and tested. However, `runRouter` currently has zero registered observers: no
per-node connection identity model exists at the DRAIN observer call site.

S-BL.PE-RECEIVE-LOOP (merged PR #118 @ `e940fc2`, 2026-07-11) delivered the per-connection
receive goroutine, `upstreamdial.Connector.SetFrameCallback`, and the
`FrameArrivalHandler.OnFrameArrival` wiring. It also emitted FO-RECV-FWD-001: the nil
`ForwardFunc` at `mgmt_wire.go`:549 must be replaced when the interface set widens.

**v1.1 clarification (Q2-AMENDED):** FO-RECV-FWD-001 is NOT consumed by this story. The
nil `ForwardFunc` stays; the `interfaceSet` stays `[]{peIfaceID}`. DRAIN is a
router-originated broadcast dispatched directly from the drain observer closure to per-node
send channels — it does NOT route through `OnFrameArrival`. The E-FWD-001 invariant is
preserved. FO-RECV-FWD-001 remains deferred to the future data-frame-widening story.

This story lands three deliverables:

1. **DRAIN frame on the wire** — A single startup drain observer (registered once at
   `runRouter` setup) iterates the live per-node send map at `Signal` time. For each
   connected node, it assembles a `FrameTypeCtl` (0x03) frame with a 4-byte payload
   (`control_type = 0x01`, DRAIN opcode) and non-blocking sends it to the node's send
   channel. The per-node send map is a `sync.Map` owned by `runRouter`; entries are
   added via the `netingress.ServeConfig.OnAccept` hook and removed on conn close.

2. **netingress OnAccept seam** — `netingress.Serve` gains a `ServeConfig` parameter
   carrying an `OnAccept func(conn net.Conn, h NodeHandle) func()` hook. `runRouter`
   supplies the hook to allocate `routing.InterfaceID`, create the per-node send channel,
   start the writer goroutine, and register the channel in the send map. Cleanup
   (channel close + map remove) is deferred on conn close. This is the binding Q-SEAM
   design.

3. **Per-observer panic recovery and EC-003 production-path evidence** — `drain.Signal`'s
   fan-out goroutine currently has no `defer recover()`. This story adds the recovery
   wrapper. EC-003 is evidenced via `drainCoordHook` + blocked observer +
   `cfg.DrainTimeout=100ms` (the real drain-timeout injection seam per Q-AC005).

**Previous Story Intelligence.** `S-BL.PE-RECEIVE-LOOP` completed 4 adversarial passes
(21 findings, all dispositioned) before convergence. Key lessons carried forward:
(1) Every symbol in ACs must be grep-verified at `e940fc2` or marked "(new — defined by
this story)" — the placement note's Appendix A performed this sweep; use it.
(2) Avoid line-number citations in story prose — use mechanism-anchor descriptions.
(3) The "ctl discrimination" lives in the netingress `route` RouteFn closure — NOT the
PE-upstream `FrameFn` closure, which stays frozen per Q2-AMENDED — and must silently
ignore unknown `control_type` values (FO-DRAIN-WIRE-001 forward-compat rule) — the
RESYNC opcode (0x02) is RESERVED but not dispatched here; failing to add this guard was
a recurring error class in S-7.04-FU-PE-CONNECTOR (F-P11-001 lesson).
(4) Production send-path seams need full round-trip pins — a test that only registers
an observer without verifying the DRAIN frame arrives on the wire is a false positive.
(5) All drainCoord-behavior tests use the real-runRouter pattern (`startRunRouterWithConfig`,
verified `router_drain_test.go`:94-114 at `e940fc2`) + `drainCoordHook` to capture the
`*drain.Drain`; do NOT use `testenv.SendDrainSignal` for runRouter's coordinator (it
drives testenv's separate `drainCtrl` — a different `drain.Drain` instance).

**Token Budget Estimate (forecast).** Story spec: ~13k tokens. Referenced production
code (`drain.go`, `mgmt_wire.go`, `frame.go`, `netingress.go`): ~7k tokens.
Test infrastructure (`vp037_e2e_test.go`, `router_drain_test.go`, testenv patterns): ~4k tokens.
Placement note: ~5k tokens. Total implementing-agent context: ~29k tokens — well within
20–30% of a 200k context window. No story split required.

## Anchors Consumed

| Anchor | Verbatim ID | Source | Disposition |
|--------|-------------|--------|-------------|
| BC-2.09.002 PC-1 — DRAIN signal broadcast to all connected nodes | BC-2.09.002 PC-1 | S-7.04 DELIVERY deferred AC-003 | TO DISCHARGE — AC-001 + AC-003 (single observer sends DRAIN frame per node via send map) |
| BC-2.09.002 EC-003 — drain timeout exceeded; unresponsive observer forced-exit | BC-2.09.002 EC-003 | DRIFT-HS006-DRAIN-TIMEOUT-FORCED-EXIT-UNEVIDENCED residual | TO DISCHARGE — AC-005 (drainCoordHook + blocked observer + cfg.DrainTimeout=100ms) |
| BC-2.01.008 Postcondition 4 — forward-compat silent-ignore for unrecognized control_type | BC-2.01.008 PC-4 | BC-2.01.008 v1.0 (PO obligation, schema home) | TO DISCHARGE — AC-003 pin test (FO-DRAIN-WIRE-001 forward-compat guard) |
| VP-037 — drain within window: `drainCoord.Wait` returns nil (stage 1) | VP-037 | S-7.04 DELIVERY deferred ("drain within window"); two-stage discharge (Q4-AMENDED) | TO DISCHARGE (stage 1) — AC-004 (untagged `TestE2E_RouterDrain_WireRoundTrip`); lock stays false |
| FO-RECV-FWD-001 — nil ForwardFunc in `mgmt_wire.go` SetFrameCallback closure | FO-RECV-FWD-001 | S-BL.PE-RECEIVE-LOOP (emitted per delivery doc :270) | **DEFERRED (amended v1.1 — Q2-AMENDED, F-DW-SP1-002): NOT consumed by this story; nil stays; interfaceSet stays `[]{peIfaceID}`** |

---

## Design Constraints

### DRAIN Frame Wire Schema (Q1 — binding)

**Binding (per placement note Q1).**

The DRAIN signal is carried as a `FrameTypeCtl = 0x03` frame (existing constant, verified
`internal/frame/frame.go` at `e940fc2`). No new `FrameType` constant is introduced. The
DRAIN payload schema is a fixed 4-byte control message (BC-2.01.008 v1.0, schema home):

```
byte[0]   control_type   = 0x01   // DRAIN opcode
byte[1]   version        = 0x01   // protocol version; allows future extension
byte[2:4] reserved       = 0x0000 // zero-filled; receiver MUST ignore
```

Rationale: `FrameTypeCtl` is the existing discriminator for authenticated control frames.
Adding `FrameTypeDrain = 0x07` would require widening `Valid()` past `FrameTypePEConnect =
0x06` — the guard S-BL.PE-RECEIVE-LOOP erected. Using `FrameTypeCtl` with a
`control_type` payload byte is the correct design: outer header identifies the wire
family (`ctl`); the control-message opcode lives in the payload. BC-2.01.008 is the
authoritative schema home; BC-2.01.004 Inv-2 carries the terminal-consumer carve-out
cross-reference.

**Assembly path:** The observer closure constructs the 4-byte payload and calls
`frame.EncodeOuterHeader` + `append(ehdr[:], payload...)`. The `Envelope` fields
(SVTN, SrcAddr, DstAddr, FrameAuthKey) are sourced from the per-node connection context
stored in the `runRouter` connection registry using the zero-envelope pattern identical to
S-7.04-FU-PE-CONNECTOR (FO-DRAIN-WIRE-002 notes that full Ed25519 bootstrap is a
session-bootstrap follow-on). `frame.EncodeOuterHeader` is an EXISTING function at
`e940fc2` (not new — defined by this story).

**Wire-format spec-pair obligation (same-commit — binding):** This story introduces the
`control_type` discriminator byte in the `FrameTypeCtl` payload schema. The following
same-commit amendments are required:

1. ARCH-02 § "Outer Header Format" (NOT §3.1 — ARCH-02 has no §3.1, corrected v1.2,
   F-DW-SP2-009) frame_type table: add a note that `ctl (0x03)` payloads carry a
   `control_type` byte discriminator; enumerate opcode `0x01 = DRAIN`.
2. BC-2.01.004 Postcondition 2 (outer-header layout): add the same `control_type` note
   under the `frame_type = ctl` row (with cross-ref to BC-2.01.008 as schema home).

These are a NARROWER obligation than the FrameTypePEConnect case (F-SP14-001) — the
frame_type byte itself is unchanged (0x03 already listed); only the payload schema
documentation is new. The same-commit rule (F-P8-008) still applies.

**Forward-compatibility silent-ignore rule (FO-DRAIN-WIRE-001, binding; anchors to
BC-2.01.008 Postcondition 4; guard home firmed v1.2, F-DW-SP2-003):** Any `FrameTypeCtl`
frame arriving at the netingress `route` RouteFn closure (`mgmt_wire.go`, the inbound
per-node path) with an unrecognized `control_type` value MUST be silently ignored:
logged (naming the unrecognized byte) and dropped — the `route` closure returns `nil`
directly, with no forwarding to `routing.RouteFrame`/`OnFrameArrival`. This guard does
NOT live in the PE-upstream `FrameFn` closure, which is frozen per Q2-AMENDED and
carries no code change this story. The DRAIN receive path MUST NOT fail on unknown
opcodes. `control_type = 0x02` (RESYNC) is reserved and dispatched by
`S-BL.RESYNC-FRAME` — NOT this story. EC-003 in BC-2.01.008 specifies that RESYNC
received before S-BL.RESYNC-FRAME ships is silently ignored.

### Send-Path Seam and Per-Node Send Map (Q-SEAM + Q2-AMENDED — binding, amended v1.3)

**Binding (per placement note Q-SEAM and Q2-AMENDED, F-DW-SP1-001/F-DW-SP1-002, amended
v1.2 F-DW-SP2-002/F-DW-SP2-004/F-DW-SP2-008, amended v1.3 F-DW-SP3-001/005 — ownership
split + shutdown ordering guarantee).**

**FO-RECV-FWD-001 disposition:** FO-RECV-FWD-001 is NOT consumed by this story. The
`SetFrameCallback` closure in `mgmt_wire.go` MUST NOT change the `interfaceSet` or
`ForwardFunc`. The nil `ForwardFunc` stays nil (correct — E-FWD-001 invariant: split-horizon
exhausts before `fn` is reached with the single-element set `[]{peIfaceID}`). FO-RECV-FWD-001
remains deferred to the future data-frame-widening story.

**DRAIN dispatch path:** DRAIN is a router-originated broadcast, NOT a frame arriving
via `OnFrameArrival`. The drain observer closure in `runRouter` dispatches directly to
each node's send channel. The routing layer is not in the DRAIN path.

**netingress seam (Q-SEAM, binding, ownership split firmed v1.3 — F-DW-SP3-001):**

`netingress.Serve` gains an `OnAccept` hook via a `ServeConfig` struct parameter.
`NodeHandle` is passed INTO `OnAccept` as a parameter — by Go's own calling
convention, the CALLER of `OnAccept` (i.e. `netingress.Serve`) constructs it before
the call. `netingress` owns DATA creation (allocates `ifaceID` from an atomic
counter seeded by `ServeConfig.IfaceIDSeed`; creates `Send`/`Done`; populates
`NodeHandle` before calling `OnAccept`); `runRouter`'s `OnAccept` closure owns
BEHAVIOR only (see "Per-node send map ownership" below) — it does not construct
any `NodeHandle` field itself:

```go
// In internal/netingress:

// NodeHandle is the write handle for an accepted node connection, fully
// populated by netingress BEFORE OnAccept is called. OnAccept receives it as
// a finished value; it does not construct any of its fields.
type NodeHandle struct {
    IfaceID routing.InterfaceID
    Send    chan []byte    // netingress-created. NEVER closed by any goroutine,
                             // for the lifetime of the process — see the
                             // single-closer/no-send-after-close invariant
                             // below (Q-SEAM v1.2, F-DW-SP2-002).
    Done    chan struct{}  // netingress-created. Closed EXACTLY ONCE, always
                             // through a per-node doneOnce sync.Once — never
                             // call close(h.Done) directly. See "Shutdown
                             // ordering guarantee" below for the two trigger
                             // paths that race to close it (v1.3, F-DW-SP3-005).
}

type ServeConfig struct {
    // OnAccept is called for each accepted connection, after netingress has
    // allocated the connection's InterfaceID and created its Send/Done
    // channels (NodeHandle is fully populated), and before ServeConn starts.
    // The returned func() (behavior cleanup) is called in a defer inside the
    // per-conn goroutine when ServeConn returns. If nil, no hook fires and
    // netingress allocates nothing for that connection (zero-cost when unused).
    OnAccept func(conn net.Conn, h NodeHandle) func()

    // IfaceIDSeed is the first InterfaceID netingress allocates when OnAccept
    // is non-nil; subsequent connections get IfaceIDSeed+1, IfaceIDSeed+2, ...
    // via an internal atomic counter. Zero defaults to 2 (preserves the
    // reserved peIfaceID = routing.InterfaceID(1) and keeps a zero-value
    // ServeConfig backward-compatible). New v1.3, F-DW-SP3-001.
    IfaceIDSeed routing.InterfaceID
}

// Serve extended to: func Serve(ctx, ln, route, logger, cfg ServeConfig) error
// Backward compat: callers passing a zero ServeConfig are unaffected.
```

**ARCH-08 impact (amended v1.2 — F-DW-SP2-008):** `internal/netingress` imports
`internal/routing` (for `routing.InterfaceID`) — pos 18 → pos 17, a forward edge
(18 > 17); LAWFUL under ARCH-08 §6.5. ARCH-08 §6.5 row for `internal/netingress` MUST
be amended in the same commit: current `{frame}` → updated `{frame, routing}`. **This
IS an ARCH-08 version bump** — ARCH-08's own v2.11 changelog entry (from
S-BL.PE-RECEIVE-LOOP) bumped the version for exactly this class of change (a §6.5
import-set edit); the v1.1 "not a version-bump" claim was wrong and is struck. This
story bumps ARCH-08 `2.11` → `2.12` in the same commit as the `netingress.go` edit,
with a changelog row citing this story (see FCL row 11).

**netingress package-doc rewrite (new v1.2, F-DW-SP2-004, same-commit doc-sweep item):**
`netingress.go`:15–17's current package doc reads: "this package MAY import
`internal/frame` only. It receives a `RouteFn` from callers to avoid importing
`internal/routing` (which would invert the ARCH-08 layering; ingress is upstream of
routing)." This story's `NodeHandle`/`OnAccept` design mandates exactly the import this
text forbids. The doc-sweep rewrites the "Import constraints" paragraph to state that
`netingress` MAY import `internal/frame` and `internal/routing` (for the `InterfaceID`
type only — no routing decisions are made in `netingress`), dropping the now-false
"to avoid importing internal/routing" rationale. Justification carried in the rewritten
text: the new edge is `internal/netingress` (§6.5 pos 18) importing `internal/routing`
(§6.5 pos 17) — a forward edge in ARCH-08's topological ordering (18 > 17), and
`internal/routing` does not import `internal/netingress` (no cycle). Same-commit with
the `ServeConfig`/`NodeHandle`/`OnAccept` implementation, not a follow-on cleanup.

**Per-node send map ownership (ownership split binding — v1.3, F-DW-SP3-001):**

- **Ownership split.** `netingress.Serve` owns DATA creation: it allocates `ifaceID`
  from an atomic counter seeded by `ServeConfig.IfaceIDSeed` (default 2; `peIfaceID = 1`
  stays reserved for the PE upstream interface), creates `send := make(chan []byte, 32)`
  and `done := make(chan struct{})`, populates `h := NodeHandle{IfaceID: ifaceID, Send:
  send, Done: done}`, and calls `cfg.OnAccept(conn, h)`. `runRouter`'s `OnAccept` closure
  owns BEHAVIOR only: it wraps `h.Send`/`h.Done` in a `*nodeConn` (see "Send-map value
  type" below), stores that in the per-node send map under `h.IfaceID`, fires
  `nodeConnHook`, and starts the writer goroutine — it never re-creates or mutates the
  identity of `send`/`done`, only stores and selects on the ones netingress handed it.
- **Send-map value type (binding — v1.3, F-DW-SP3-001/005):** because the `done`-close
  trigger has two independent call sites (this connection's own teardown, and the
  router-wide drain-shutdown flush pass — see "Shutdown ordering guarantee" below), the
  per-node send map's value type is `*nodeConn`, not a bare `chan []byte`:
  ```go
  type nodeConn struct {
      send     chan []byte
      done     chan struct{}
      doneOnce *sync.Once
  }
  ```
  stored as `sync.Map[routing.InterfaceID, *nodeConn]`.
- **Single-closer / no-send-after-close invariant (binding — v1.2, extended v1.3,
  F-DW-SP2-002 / F-DW-SP3-001/005):** the per-node `send` channel is NEVER closed, by any
  goroutine, for the lifetime of the process. The per-node `done` channel IS still closed
  exactly once, but now has TWO possible triggers racing to close it: (a) this
  connection's own teardown via the behavior-cleanup `func()` `OnAccept` returns, and (b)
  the router-wide drain-shutdown flush pass, which proactively closes every still-live
  node's `done` BEFORE `ingressCancel` runs (see "Shutdown ordering guarantee" below).
  BOTH triggers call `nc.doneOnce.Do(func() { close(nc.done) })` rather than
  `close(nc.done)` directly — exactly one performs the actual close, the other is a
  harmless no-op. This eliminates a close-race against the single drain observer's
  concurrent `sendMap.Range` (Q-SINGLE-OBS): closing `send` while the observer's
  `select { case nc.send <- frame: default: }` targets it would panic — `default:` only
  guards a full channel, not a closed one — and the resulting panic would be swallowed by
  `Signal`'s `defer recover()` (Q5/AC-005), silently truncating DRAIN delivery to every
  node ordered after the disconnecting one in map-iteration order. `send` becomes
  unreferenced (GC-eligible) once its map entry is deleted and its writer goroutine has
  exited; any frame the drain observer still manages to enqueue on it in the race window
  between `Range` visiting the entry and cleanup deleting it is a harmless best-effort
  send into an abandoned buffered channel (Q3.P1 best-effort ruling) — no consumer reads
  it, and nothing re-closes or re-sends on it.
- `runRouter` `OnAccept` closure (writer goroutine ownership, binding — v1.3,
  F-DW-SP3-001/005):
  1. Receives `h` (fully populated by netingress — `h.Send`/`h.Done` are the actual
     channels netingress created, not copies) and `conn`. Wraps them as
     `nc := &nodeConn{send: h.Send, done: h.Done, doneOnce: new(sync.Once)}` and stores
     it in the per-node send map (`sendMap.Store(h.IfaceID, nc)`); fires
     `nodeConnHook(nodeConnRegistered, h.IfaceID)` if `nodeConnHook != nil` (Q-AC002 —
     the test observability hook, see below; nil in production, no-op).
  2. Increments the router-scoped `writerWG` (`sync.WaitGroup`, one per `runRouter`
     invocation — see "Shutdown ordering guarantee" below) by 1, then starts a writer
     goroutine (sole writer to `conn`):
     ```go
     go func() {
         defer writerWG.Done()
         for {
             select {
             case frame := <-nc.send:
                 if _, err := conn.Write(frame); err != nil {
                     return // conn closed or write error; exit loop
                 }
             case <-nc.done:
                 // Flush any frame(s) already queued (e.g. a DRAIN frame enqueued by
                 // the single observer's Range call) before exiting — do NOT drop
                 // them silently (F-DW-SP3-005).
                 for {
                     select {
                     case frame := <-nc.send:
                         if _, err := conn.Write(frame); err != nil {
                             return
                         }
                     default:
                         return
                     }
                 }
             }
         }
     }()
     ```
  3. Does NOT register a per-connection `drain.Observer` — see Q-SINGLE-OBS below; only
     the single startup observer, registered once before serving, is ever registered.
  4. Returns a behavior-cleanup `func()` that: removes the `InterfaceID` entry from the
     per-node send map; fires `nodeConnHook(nodeConnRemoved, h.IfaceID)` if
     `nodeConnHook != nil`; calls `nc.doneOnce.Do(func() { close(nc.done) })` — one of the
     two possible `done`-close triggers (harmless no-op if the router-wide shutdown-flush
     pass already closed it first). `send` itself is NEVER closed — see the invariant
     above.
- `ServeConn` is the sole reader of `conn`. No mutex is required (net.TCPConn read/write
  are independent paths).

### Shutdown Ordering Guarantee (Q-SEAM — binding, new v1.3, F-DW-SP3-005)

**The problem:** Q3.P1's best-effort delivery means the single drain observer returns as
soon as it has QUEUED the DRAIN frame to every live node's `send` channel (a non-blocking
`select`/`default`) — `drainCoord.Wait` returning `nil` says only "queuing finished," not
"every writer goroutine has flushed its queued frame to the wire." The existing shutdown
block calls `ingressCancel()` immediately after `drainCoord.Wait(drainCtx)` returns, which
tears down `ingressCtx` and closes every accepted connection's underlying `net.Conn`.
Nothing orders the writer goroutine's `conn.Write(DRAIN)` before that close — under
scheduling pressure the `net.Conn` can close before the writer even reaches its
`case frame := <-nc.send:` branch, and the node reads EOF instead of the DRAIN frame.
This is the AC-004/AC-001 read-determinism dependency both discharge-traces rely on.

**Ruling — insert a bounded flush phase between `Wait` and `ingressCancel()`:**

```
Signal → Wait → close all live nodes' done (router-wide flush pass) →
writerWG.Wait() bounded by a short flush deadline → ingressCancel()
```

The shutdown block gains two new steps between the existing `drainCoord.Wait(drainCtx)`
call and `ingressCancel()`:

```go
// (existing) drainCoord.Signal(drainCtx); if derr := drainCoord.Wait(drainCtx); ... ; drainCtxCancel()

// NEW — router-wide flush pass: tell every still-live writer goroutine to drain its
// queued frame(s) and exit, BEFORE the connections are torn down.
sendMap.Range(func(key, value any) bool {
    nc := value.(*nodeConn)
    nc.doneOnce.Do(func() { close(nc.done) })
    return true
})

// NEW — bounded wait for writers to finish flushing. writerWG has no native timeout,
// so race it against a short deadline; on expiry proceed anyway (best-effort,
// consistent with Q3.P1 — a stalled/non-reading node must not block router shutdown
// indefinitely).
flushDone := make(chan struct{})
go func() {
    writerWG.Wait()
    close(flushDone)
}()
select {
case <-flushDone:
case <-time.After(drainFlushTimeout):
    routerLogger.Log("runRouter: drain flush deadline exceeded; proceeding with shutdown")
}

ingressCancel() // (existing, now runs only after the flush phase)
dataWG.Wait()   // (existing)
```

`drainFlushTimeout` is a small fixed constant — **PROVISIONAL value (e.g. 200ms)**; the
MECHANISM (bounded-select flush phase before `ingressCancel`) is BINDING, the exact
duration is left to the implementer to tune against real write-syscall latency for a
48-byte frame. It is deliberately NOT `cfg.DrainTimeout` — that budget is already spent by
the time this phase runs; this is a second, much shorter bound purely for flushing
already-queued, already-small frames to a socket that should accept them near-instantly
under normal conditions.

**Why `sync.Once` and not a direct `close(nc.done)` here:** this router-wide pass and each
connection's own behavior-cleanup are two INDEPENDENT triggers that can both fire for the
same node — e.g. a node that happens to disconnect on its own at the same moment the
router is draining. `doneOnce` (created alongside `done`, stored in the `nodeConn` entry)
generalizes the single-closer fix to any number of trigger sites, present or future,
without reintroducing a double-close panic.

**FCL:** production code inside `runRouter` (`cmd/switchboard/mgmt_wire.go`, FCL row 1 —
no new file). `writerWG` is a new `sync.WaitGroup` local to `runRouter`, alongside the
existing `dataWG`/`mgmtWG`.

**Reconciles AC-001/AC-004:** both discharge traces' final "read from the inbound conn;
assert DRAIN frame received" step already assumed the frame would be observable before
the connection tore down — this section is what makes that assumption sound. The traces'
enumerated steps do NOT change (the fix is entirely inside production `runRouter`
shutdown code); each AC gains a cross-reference note at its read step pointing here as
the mechanism that makes the read deterministic rather than flaky.

### Per-Node Send-Map Registration Observable (Q-AC002 — new v1.2, F-DW-SP2-006, binding)

AC-002's tests need to assert that the per-node send map (an unexported `sync.Map`
local to `runRouter`, per Q-SEAM) gained/lost an entry on connect/disconnect, without
reaching into that unexported state. The mechanism is a package-level test hook,
`nodeConnHook`, mirroring the existing `drainCoordHook` precedent
(`mgmt_wire.go`:514–516):

```go
// nodeConnEvent identifies why nodeConnHook fired.
type nodeConnEvent int

const (
    nodeConnRegistered nodeConnEvent = iota // OnAccept stored the send channel
    nodeConnRemoved                          // cleanup deleted the map entry
)

// nodeConnHook, when non-nil, is called synchronously from the OnAccept registration
// closure and from the per-connection cleanup closure. nil in production (no-op).
var nodeConnHook func(event nodeConnEvent, ifaceID routing.InterfaceID)
```

Called at exactly two points (both inside the `OnAccept`/cleanup closures above,
variable names aligned v1.3 to the Writer goroutine ownership steps — `h.IfaceID`/`nc`,
not the bare `ifaceID`/`send` this section used through v1.2): immediately after
`sendMap.Store(h.IfaceID, nc)`, and immediately after `sendMap.Delete(h.IfaceID)`. This
hook also supplies the accept/register barrier AC-001 and AC-004's discharge-traces
require before triggering drain (see those ACs) — tests should wire both needs from one
shared helper rather than two separate mechanisms.

### Single Startup Observer (Q-SINGLE-OBS — binding, amended v1.3)

**Binding (per placement note Q-SINGLE-OBS, F-DW-SP1-007, amended v1.3 F-DW-SP3-001/005/
006/008).**

`runRouter` registers a SINGLE `drain.Observer` with `drainCoord`, ONCE at `drainCoord`
construction time — this is guaranteed to precede `drainCoord.Signal` (`RegisterObserver`
no-ops after `Signal`), which is the only ordering that matters. This is NOT the same
claim as "before serving any connections": the netingress accept loop starts textually
BEFORE `drainCoord` is even constructed (verified at `ef1ee1e`); registration-vs-accept-
loop ordering is immaterial because the per-node send map is zero-value-usable — `OnAccept`
can safely store entries into it from the moment the accept loop starts, whether or not
the observer has been registered yet (corrected v1.3, F-DW-SP3-008; no phase reorder is
mandated by this correction). The observer captures a reference to the per-node send map
(`sync.Map[routing.InterfaceID, *nodeConn]` as of v1.3 — F-DW-SP3-001/005; see "Send-map
value type" above).

At `Signal` time, the observer first fires the test-only `drainObserverFiredHook`
(Q-AC003, new v1.3 — F-DW-SP3-006), then iterates the live map:
```go
observer := func(ctx context.Context) {
    if drainObserverFiredHook != nil {
        drainObserverFiredHook()
    }
    sendMap.Range(func(key, value any) bool {
        nc := value.(*nodeConn)
        frame := assembleDrainFrame(...)
        select {
        case nc.send <- frame:
        default: // channel full — best-effort (never closed, per the single-closer/
                 // no-send-after-close invariant above; this send cannot panic)
        }
        return true
    })
}
drainCoord.RegisterObserver(observer)
```

The observer then returns (context deadline is the bound — Q3.P1 BINDING option (2)).

**Consequence for OnAccept:** The `OnAccept` closure inserts the per-node send channel
into the live map and removes it on cleanup, but does NOT call
`drainCoord.RegisterObserver`. Only the single startup observer is registered.

**Consequence for `drain.Drain` API:** No deregister API is needed. `RegisterObserver`
retains its current shape.

### Acknowledgment Path (Q3.P1 — BINDING, v1.1)

**BINDING (per placement note Q3-AMENDED, F-DW-SP1-003/F-DW-SP1-006; BC-2.09.002 v1.2).**

The Q3.P1 PROVISIONAL from v1.0 is resolved. Option (2) is BINDING: the drain observer
returns after queuing the DRAIN frame to the node's send channel. No wire-level ACK
opcode is implemented. Acknowledgment is defined (BC-2.09.002 PC-3 v1.2) as: observer
returns after DRAIN frame queued to node's send channel, bounded by drain window.

The drain coordinator's `Wait` is the only ACK boundary. No `control_type = 0x02`
DRAIN_ACK opcode dispatch is required or implemented.

**All drainCoord-behavior tests MUST use the real-runRouter harness rule (Q3-AMENDED,
binding):** Use `startRunRouterWithConfig` (verified `router_drain_test.go`:94–114 at
`e940fc2`). Do NOT use `testenv.SendDrainSignal` for runRouter's `drainCoord` — it
drives testenv's own `drainCtrl` (a different `drain.Drain` instance).

Triggering `runRouter`'s `drainCoord.Signal`: cancel the `ctx` passed to `runRouter`
(production path via the shutdown block at `mgmt_wire.go`:638), or use `drainCoordHook`
to capture the `*drain.Drain` ref for direct calls.

### VP-037 Two-Stage Discharge (Q4-AMENDED — binding, discharge-trace corrected v1.2)

**Binding (per placement note Q4-AMENDED, F-DW-SP1-004, amended v1.2 F-DW-SP2-001;
VP-037 v1.5).**

VP-037's full property requires node-side migration logic not implemented by this story.
Architect ruled two-stage discharge:

**Stage 1 (this story):** `TestE2E_RouterDrain_WireRoundTrip` (NEW, no build tag,
`cmd/switchboard/router_drain_wire_test.go`) — real-`runRouter` pattern (per
Q3-AMENDED; `testenv.NewWithRouters` is NOT used — it never runs a real `runRouter`
and has no bound listener to dial, per F-DW-SP2-001): `startRunRouterWithConfig`, dial
`cfg.ListenAddr` (test acts as the simulated node), wait for `nodeConnHook` (Q-AC002) to
observe `nodeConnRegistered` for the connection (the mandatory accept/register barrier —
without it, `cancel()` can fire before the send map has the entry the drain observer
needs, and the test flakes), `cancel()` to trigger the shutdown block's
`drainCoord.Signal`, then assert the received frame is `FrameTypeCtl + payload[0]=0x01`
within 2s and `drainCoord.Wait` returns `nil` within the default drain window (do not
assert against `drain.ErrTimeout` or any other specific error — see Q-AC005's flakiness
note; this path is expected to succeed well within the window). `verification_lock`
remains `false` after stage 1. `TestE2E_RouterDrain_NodesMigrateWithin2s` (the
`go:build integration`-gated copy in `internal/drain/vp037_e2e_test.go`) stays gated
as-is — it is untouched by this story and is a separate, permanently-gated harness test,
structurally incapable of proving real router-to-router migration (it is built on the
same `testenv.NewWithRouters` stub pattern); it is NOT the stage-2 discharge target.

**Stage 2 (named follow-on TBD):** Un-gate `TestE2E_RouterDrain_NodesMigrateWithin2s` in
`cmd/switchboard/router_pe_connector_test.go` (the `t.Skip`-gated copy — NOT the
`internal/drain` copy; disambiguated v1.2, F-DW-SP2-007), implement node-side migration
logic, flip `verification_lock: true`. No silent weakening — this is an explicit named
obligation.

**VP-037.md edit (this story):** VP-037.md carries the two-stage discharge lifecycle note
(landed at v1.5). This story adds `TestE2E_RouterDrain_WireRoundTrip`; the lock
stays false. VP-037 postcondition 5 of AC-004: VP-037.md already has the stage-1
partial-discharge note; no lock flip this story.

### Ctl Receive Guard (Q-CTL-GUARD — binding, guard home firmed v1.2, terminal-consumer basis + logging asymmetry firmed v1.3)

**Binding (per placement note Q-CTL-GUARD, F-DW-SP1-008, firmed up v1.2 F-DW-SP2-003,
amended v1.3 F-DW-SP3-002/003/004; BC-2.01.008 EC-002/PC-4/Inv-2).**

The `FrameTypeCtl` frame receive path in `cmd/switchboard` MUST length-check the payload
before reading `payload[0]`. **Guard home (firmed v1.2): the netingress `route` RouteFn
closure exclusively** (`mgmt_wire.go`'s `route := func(hdr frame.OuterHeader, payload
[]byte) error { return routing.RouteFrame(hdr, payload, router) }`) — NOT the PE
`FrameFn` closure, which is frozen per Q2-AMENDED (nil `ForwardFunc`, single-element
`interfaceSet`, no code change this story) and already passes any `FrameTypeCtl` frame
it happens to see straight into `OnFrameArrival` unmodified, untouched by this story.
The v1.1 "route RouteFn closure or per-connection dispatch code" hedge is resolved: it
is the `route` closure, full stop.

**Terminal-consumer basis for the unconditional guard (binding — v1.3, F-DW-SP3-004; PO
ruling BC-2.01.008 v1.1 Inv-2 consumed):** this guard applies to EVERY `ctl` frame
`route` sees, with NO runtime "is this addressed to me" check. That is legitimate ONLY
because BC-2.01.008 Inv-2 (new v1.1) establishes that netingress-arriving `ctl` frames
are terminal-consumer BY CONSTRUCTION under the current architecture: `netingress` is
exclusively the node-facing accept loop, and no inter-router relay path exists anywhere
in this codebase (`internal/routing.SVTNRoute` validates forwarding-table entries but
never relays frame bytes to another connection) — so a `ctl` frame arriving here can
never be in transit to a downstream router. Inv-2 is explicitly scoped to today's
architecture and carries a **REVISIT TRIGGER**: if a future story introduces
inter-router forwarding/relay (a topology where a `ctl` frame could arrive via a
router-facing connection addressed to a downstream router), this guard's unconditional
posture must be re-examined against BC-2.01.008 Precondition 2's general
terminal-consumer language — carry this revisit trigger forward into any follow-on
story that adds router-to-router connections.

```go
if hdr.FrameType == frame.FrameTypeCtl {
    if len(payload) < 4 {
        // E-PRT-002: control frame truncated. No `conn` is in scope here — the
        // route RouteFn closure signature is func(hdr, payload) error; there is
        // no per-connection dispatch seam with conn access (F-DW-SP3-003) — do
        // not add one for this log line.
        routerLogger.Log(fmt.Sprintf(
            "netingress: E-PRT-002: ctl frame payload_len=%d < 4; discarding",
            len(payload)))
        // silent-ignore: no connection close per BC-2.01.008 EC-002
        return nil // drop, keep reading
    }
    controlType := payload[0]
    switch controlType {
    case 0x01: // DRAIN — this story's dispatch target; no-op on receipt (DRAIN is
               // router-originated broadcast per Q2-AMENDED; a router does not act
               // on an inbound DRAIN byte from a node)
    default:
        // Unknown/forward-compat control_type (includes the reserved-but-
        // undispatched 0x02 RESYNC opcode until S-BL.RESYNC-FRAME lands).
        // BC-2.01.008 PC-4 (v1.1, F-DW-SP3-002 / PO ruling): silent-ignore means
        // NO logging of any kind on this path — not even a diagnostic/
        // informational line — and no connection close. Do NOT add a log call to
        // this arm; an earlier draft logged an "unrecognized ctl control_type"
        // line here, which PC-4 v1.1 explicitly forbids (unbounded log growth
        // across a rolling upgrade). This is intentionally asymmetric with the
        // E-PRT-002 branch above, which DOES log — a truncated payload is
        // corruption, diagnostic-worthy; a well-formed-but-unrecognized opcode is
        // ordinary forward-compatible protocol evolution, not an anomaly.
    }
    return nil
}
```

**Disposition (asymmetric logging, v1.3 — BC-2.01.008 PC-4 v1.1, F-DW-SP3-002, PO
ruling):** Silent-ignore (no connection close) for BOTH arms, but the LOGGING behavior
is asymmetric: the short-payload guard DOES log (BC-2.01.008 EC-002: discard with an
E-PRT-002 log — with no `conn.RemoteAddr()`/`from %s` clause, since no `conn` is in
scope in the `route` closure, corrected v1.3 F-DW-SP3-003) while the unknown-
`control_type` default arm does NOT log at all (PC-4's strengthened no-logging rule,
including no diagnostic/informational line). The node can send further frames after
either case — neither closes the connection.

**Pin tests (two, both in `cmd/switchboard/router_drain_wire_test.go`, targeting the
`route` closure path):** `TestRouter_CtlFrame_ShortPayload_NoConnClose` (existing shape,
unchanged, no `conn.RemoteAddr()` assertion) and `TestRouter_CtlFrame_UnknownControlType_SilentIgnore`
(respecified v1.3, F-DW-SP3-002 — the v1.2 shape asserted a positive log-line match that
BC-2.01.008 v1.1 PC-4 now prohibits; the observable is redesigned WITHOUT a log
assertion: send a 4-byte ctl payload with `control_type = 0xFF`; assert conn NOT closed
via a liveness check — a subsequent well-formed `FrameTypeData` frame on the same
connection is received/dispatched without error; assert `buf.String()` contains NO line
matching the unknown-opcode pattern (negative assertion — no "unrecognized ctl
control_type" substring, no line otherwise correlating the injected `0xFF` byte with a
control_type-labeled log entry); assert NO panic and NO E-PRT-002 marker — that marker
is reserved for the length-guard case only).

### EC-003 Production-Path Evidence and drain.Signal Panic Recovery (Q5 / Q-AC005 — binding, amended v1.2)

**Binding (per placement note Q5 and Q-AC005, F-DW-SP1-010, amended v1.2 F-DW-SP2-005).**

**EC-003 test shape (Q-AC005, binding — v1.2):** EXTEND the existing
`TestRunRouter_ForcedExitPastDrainTimeout` (`cmd/switchboard/router_drain_test.go`,
already uses `drainCoordHook` + a blocked observer + `cfg.DrainTimeout`) rather than
authoring a new duplicate test. The real drain-timeout injection seam is
`cfg.DrainTimeout = 100*time.Millisecond` in the test config — `drainTimeoutFor(cfg)`
is called at `mgmt_wire.go`:490 and passed to `drain.New()` — CONFIRMED, no longer
PROVISIONAL: demonstrated live, today, in `TestRunRouter_ForcedExitPastDrainTimeout`
(sets `DrainTimeout: 500*time.Millisecond` this way already). Assert the EC-003 log
marker only. **Do NOT assert `drainCoord.Wait`'s return value against
`drain.ErrTimeout`** (or any other specific error) — `runRouter`'s shutdown block passes
the SAME `drainCtx` to both `drainCoord.Signal` and `drainCoord.Wait`
(`mgmt_wire.go`:638–639), so at the deadline `Wait`'s internal `select` races `d.done`
(delivers `drain.ErrTimeout`) against `drainCtx.Done()` (delivers
`context.DeadlineExceeded`) pseudo-randomly. This is the exact race the existing test
already avoids asserting against (see its in-file comment at ~:404–411); any new EC-003
assertion drives the identical production shutdown path and inherits the identical race.

**Per-observer panic recovery — production CODE change (binding):**

The `drain.Signal` method fan-out goroutine at `e940fc2` lines 126–129 reads:

```go
go func() {
    defer obsWG.Done()
    fn(drainCtx)
}()
```

This story adds `defer recover()` around `fn(drainCtx)`:

```go
go func() {
    defer obsWG.Done()
    defer func() {
        if r := recover(); r != nil {
            // log the panic; drain coordinator continues
        }
    }()
    fn(drainCtx)
}()
```

This is a **production change to `internal/drain/drain.go`** (pos 16, stdlib-only). No
new import edge is introduced.

A unit test in `internal/drain/drain_test.go` pins the panic recovery behavior:
the panicking observer MUST NOT kill the process; the drain coordinator continues to
call remaining observers and eventually calls `Wait`.

---

## Acceptance Criteria

### AC-001 — DRAIN frame assembled and sent to every connected PE node on drainCoord.Signal (Q1, Q-SEAM, Q-SINGLE-OBS)

**BC Anchors:** BC-2.09.002 PC-1 (DRAIN signal broadcast to all connected nodes);
BC-2.01.008 PC-2 (DRAIN opcode 0x01 dispatched); placement note Q1 (wire schema),
Q-SEAM (send path), Q-SINGLE-OBS (single observer over live map). Traces to BC-2.09.002 PC-1.

**Precondition (recipe corrected v1.2, F-DW-SP2-002 sweep — Q-SINGLE-OBS consistency;
phase-order corrected v1.3, F-DW-SP3-008):** `runRouter` is running
(`startRunRouterWithConfig`, per Q3-AMENDED) — the single startup drain observer is
already registered by the time `runRouter` constructs `drainCoord`, which is guaranteed
to precede `drainCoord.Signal` (Q-SINGLE-OBS) — this is the only ordering requirement; it
does NOT depend on preceding the accept loop (the netingress accept loop actually starts
textually BEFORE `drainCoord` is constructed — verified at `ef1ee1e` — but this is
immaterial because the per-node send map is zero-value-usable). A simulated node dials an
inbound TCP connection to `cfg.ListenAddr`; `netingress.Serve` allocates `IfaceID`/`send`/
`done` (Q-SEAM ownership split, v1.3) and calls `OnAccept`, which stores the node's send
channel in the per-node `sync.Map` and fires `nodeConnHook(nodeConnRegistered, ifaceID)`
(Q-AC002). The test waits for that `nodeConnHook` event — the same accept/register
barrier AC-004 uses — before proceeding. `drainCoord.Signal` is then triggered via
`cancel()` of the `ctx` passed to `runRouter` (production path through the shutdown
block at `mgmt_wire.go`:638; see Q3-AMENDED harness rule). There is no per-connection
`RegisterObserver` event to wait on instead — that design was superseded by
Q-SINGLE-OBS; connection-open only adds an entry to the live send map.

**Postconditions:**

1. The single startup drain observer — registered via `drainCoord.RegisterObserver` once
   at `runRouter` setup, capturing the per-node `sync.Map` reference — iterates the live
   map at `Signal` time. For each registered node channel, it assembles a `FrameTypeCtl`
   (0x03) outer frame with the 4-byte payload `{control_type: 0x01, version: 0x01,
   reserved: 0x0000}`. Assembly uses `frame.EncodeOuterHeader` + `append(ehdr[:], payload...)`.
   `frame.EncodeOuterHeader` is an EXISTING function at `e940fc2` (not new — defined by
   this story).
2. The assembled frame is non-blocking sent to the per-node send channel (best-effort
   per BC-2.09.002 v1.2 PC-3 — no wire ACK). The channel consumer (the writer goroutine
   started by `OnAccept`) transmits the frame on the SVTN connection. An integration
   test — after confirming the `nodeConnHook` accept/register barrier has fired for the
   node — asserts the simulated node's read side receives a `FrameTypeCtl` frame with
   `control_type = 0x01 (DRAIN)` byte at payload offset 0 within 2s. This read is
   deterministic, not racy, because of the Shutdown ordering guarantee (Q-SEAM, v1.3 —
   F-DW-SP3-005): the router-wide flush pass forces the writer goroutine to flush the
   queued DRAIN frame before `ingressCancel` tears down the connection.
3. `drainCoord.Wait` returns `nil` when the observer completes (under the drain window).

**Test names (anchored to Q3-AMENDED harness recipes; shares its dial-and-await-
`nodeConnRegistered` harness helper with AC-004's `TestE2E_RouterDrain_WireRoundTrip` —
kept as a SEPARATE test per the AC-001-vs-AC-004 ruling below):**

- `TestDrainObserver_AssemblesAndSendsDRAINFrame` (integration, `cmd/switchboard/router_drain_wire_test.go` — real-`runRouter` (`startRunRouterWithConfig`) + `nodeConnHook` accept/register barrier (Q-AC002) + `drainCoordHook`/`cancel()` trigger; simulated node reads back `FrameTypeCtl + control_type=0x01` after registration is confirmed; asserts `drainCoord.Wait` returns `nil`)

**AC-001-vs-AC-004 ruling (story-writer, v1.2):** AC-001 and AC-004 stay separate
acceptance criteria, sharing a common test harness helper (the dial-and-await-barrier
sequence). They trace different obligations — AC-001 discharges BC-2.09.002 PC-1 (a
connected node is reachable by the single observer's broadcast); AC-004 discharges VP-037
stage-1 (the wire round-trip AND `Wait`-within-window as a verification property, not
just a behavioral-contract clause). Collapsing them into one test would conflate a BC
obligation with a VP obligation and lose independent traceability if either is revised
later — this is the shape the architect flagged as expected. `TestDrainObserver_AssemblesAndSendsDRAINFrame`
and `TestE2E_RouterDrain_WireRoundTrip` are two distinct test functions built on the
same shared dial+barrier helper.

**Test level:** integration
**Test file:** `cmd/switchboard/router_drain_wire_test.go`

---

### AC-002 — netingress OnAccept seam: per-node connection registers in send map; writer goroutine is sole writer; cleanup deregisters (Q-SEAM binding, ownership split firmed v1.3)

**BC Anchors:** BC-2.09.002 PC-1 (DRAIN reaches each connected node via per-node send
channel); placement note Q-SEAM (F-DW-SP1-001, ownership split F-DW-SP3-001). Traces to
BC-2.09.002 PC-1.

**Precondition:** `runRouter` is running with the `OnAccept` hook wired via `ServeConfig`.
A raw TCP connection is accepted by `netingress.Serve`.

**Postconditions:**

1. `netingress.Serve` owns DATA creation (ownership split, v1.3 — F-DW-SP3-001):
   allocates `IfaceID` from an atomic counter seeded by `ServeConfig.IfaceIDSeed`
   (default 2; `peIfaceID=1` stays reserved), creates `Send chan []byte` (buffered 32,
   NEVER closed by any goroutine — single-closer/no-send-after-close invariant, Q-SEAM
   v1.2/v1.3, F-DW-SP2-002/F-DW-SP3-001/005) and `Done chan struct{}`, and populates a
   fully-built `NodeHandle{IfaceID, Send, Done}` BEFORE calling `OnAccept`. `OnAccept`
   fires for each accepted connection with this pre-populated handle, BEFORE `ServeConn`
   starts; it does not construct any of the handle's fields.
2. The `runRouter` `OnAccept` closure owns BEHAVIOR only: wraps `h.Send`/`h.Done` into
   `nc := &nodeConn{send: h.Send, done: h.Done, doneOnce: new(sync.Once)}`; stores `nc`
   in the per-node `sync.Map` under `h.IfaceID`; fires
   `nodeConnHook(nodeConnRegistered, h.IfaceID)` (Q-AC002) immediately after the `Store`;
   increments the router-scoped `writerWG` and starts a writer goroutine that `select`s
   on `nc.send`/`nc.done` — reads and writes frames from `nc.send`, and on `nc.done`
   flushes any already-queued frame before exiting (Shutdown ordering guarantee, v1.3,
   F-DW-SP3-005) rather than dropping it — exits when `conn.Write` errors or after the
   flush completes.
3. The writer goroutine is the SOLE writer to `conn`; `ServeConn` is the sole reader.
4. When `conn` closes (ServeConn returns), the behavior-cleanup `func()` returned by
   `OnAccept` is called: the `sync.Map` entry for `h.IfaceID` is deleted;
   `nodeConnHook(nodeConnRemoved, h.IfaceID)` fires (Q-AC002) immediately after the
   `Delete`; `nc.doneOnce.Do(func() { close(nc.done) })` is called — one of two possible
   `done`-close triggers (the other being the router-wide shutdown-flush pass; harmless
   no-op if that pass already closed it first, v1.3, F-DW-SP3-005). `send` is NEVER
   closed — see the single-closer/no-send-after-close invariant, Q-SEAM v1.2/v1.3,
   F-DW-SP2-002/F-DW-SP3-001/005.
5. **Negative postcondition (binding — Q2-AMENDED):** The `SetFrameCallback` closure in
   `mgmt_wire.go` MUST NOT be changed by this story. `interfaceSet` stays
   `[]{peIfaceID}`; `ForwardFunc` stays `nil`. FO-RECV-FWD-001 is NOT consumed. The
   `TestRunRouter_ForwardFunc_WiredToNodeSendMap` test from v1.0 is REMOVED and replaced
   by lifecycle tests (see below).
6. **Observability (Q-AC002, binding — v1.2, variable names aligned v1.3):** tests MUST
   NOT assert directly on the contents of the unexported per-node `sync.Map`
   (`sync.Map[routing.InterfaceID, *nodeConn]` as of v1.3). Registration and removal are
   observed exclusively via the package-level `nodeConnHook(event, ifaceID)` test hook
   (mirrors the existing `drainCoordHook` precedent), fired synchronously from `OnAccept`
   (`nodeConnRegistered`) and `cleanup` (`nodeConnRemoved`); nil in production (no-op).

**Test names (respecified v1.2 against `nodeConnHook`, F-DW-SP2-006; descriptors aligned
v1.3 to the ownership split — neither test reads the `sync.Map` directly):**

- `TestNetingress_OnAccept_RegistersNodeHandle` (unit/integration, `cmd/switchboard/router_drain_wire_test.go` — accepts a simulated inbound conn; asserts `OnAccept` fires before `ServeConn`; asserts `NodeHandle.IfaceID >= 2` — now legitimately satisfiable, since `netingress.Serve` itself allocates the `IfaceID` from the `ServeConfig.IfaceIDSeed`-seeded counter (v1.3 ownership split); asserts `nodeConnHook` records a `nodeConnRegistered` event for the `IfaceID` — the send-map-registration observable, per Q-AC002)
- `TestRunRouter_NodeConnClose_CleansUpSendMap` (integration, `cmd/switchboard/router_drain_wire_test.go` — accepted conn; close conn; assert `nodeConnHook` records a `nodeConnRemoved` event for the same `IfaceID` (events observed via the hook — the send map's value type is `*nodeConn`, not read directly per Q-AC002); assert the writer goroutine exits (via `nc.done` closing, through `doneOnce`); assert `send` is NEVER closed — no panic on a best-effort late send into it, per the single-closer invariant)

**Test level:** integration
**Test file:** `cmd/switchboard/router_drain_wire_test.go`

---

### AC-003 — Single startup observer registered; observer fires; FO-DRAIN-WIRE-001 forward-compat guard; ctl-short-payload + unknown-control_type pins (Q-SINGLE-OBS, Q-AC003 firmed v1.3, Q-CTL-GUARD firmed v1.3, BC-2.01.008 v1.1)

**BC Anchors:** BC-2.09.002 PC-1 (observer broadcasts DRAIN); BC-2.01.008 PC-4 +
Invariant 2 (forward-compat silent-ignore for unrecognized `control_type` +
terminal-consumer-by-construction basis — PO obligations, v1.1); placement note
Q-SINGLE-OBS, Q-AC003 (F-DW-SP1-007/F-DW-SP1-009, respecified v1.3 F-DW-SP3-006),
Q-CTL-GUARD (F-DW-SP1-008, amended v1.3 F-DW-SP3-002/003/004). Traces to BC-2.09.002
PC-1 and BC-2.01.008 PC-4.

**Precondition (Q-AC003 shape; phase-order corrected v1.3, F-DW-SP3-008):** `runRouter`
started via `startRunRouterWithConfig`. The single startup observer is registered at
`drainCoord`-construction time, which is guaranteed to precede `drainCoord.Signal` — the
only ordering that matters (NOT "before any node connects": the send map's zero-value
usability makes accept-loop-vs-registration ordering immaterial; no phase reorder).

**Postconditions:**

1. The single startup observer is registered at `drainCoord`-construction time (Q-SINGLE-
   OBS v1.3). Observable (respecified v1.3, F-DW-SP3-006): a new package-level test hook,
   `drainObserverFiredHook func()` (nil in prod, mirrors `drainCoordHook`/`nodeConnHook`),
   fired unconditionally as the FIRST statement of the observer's body, before
   `sendMap.Range` — independent of any live node connection. Test shape (Q-AC003,
   binding — v1.3):
   - Set `drainObserverFiredHook` to close a test channel (or set a mutex-guarded flag) —
     no node connection required.
   - `startRunRouterWithConfig(t, cfg, &buf)` → errCh, cancel — the single observer is
     registered at `drainCoord`-construction time.
   - `cancel()` → shutdown block → `drainCoord.Signal` → the observer fires →
     `drainObserverFiredHook` is called.
   - Assert the hook fired (channel received, or flag observed set) within a short
     timeout.
   - Assert `drainCoord.Wait` returns `nil`.
   No "observer count 0→1" accessor is needed; the Q-SINGLE-OBS ruling makes that shape
   obsolete. The v1.1 "flag inside the observer closure" language named no defined
   mechanism (the production observer is a closure inside `runRouter`; a same-package
   test cannot reach "a flag inside" it without an actual package-level seam) — struck
   and replaced by `drainObserverFiredHook`.
2. When `drainCoord.Signal` fires, the single observer closure first fires
   `drainObserverFiredHook` (if non-nil), then iterates the live
   `sync.Map[routing.InterfaceID, *nodeConn]` and dispatches DRAIN frames to all
   registered node channels (best-effort non-blocking per BC-2.09.002 v1.2 PC-3).
3. All new `cmd/switchboard` (top leaf) → `internal/drain` (pos 16) and
   `cmd/switchboard` → `internal/frame` (pos 2) call sites are ARCH-08 lawful (top leaf
   > pos 16 and top leaf > pos 2; both already imported at `e940fc2`). `internal/netingress`
   (pos 18) importing `internal/routing` (pos 17) is LAWFUL — forward edge (18 > 17).
   `internal/drain` (pos 16) remains stdlib-only; no new import edges from `drain.go`.
4. **FO-DRAIN-WIRE-001 forward-compat guard (BC-2.01.008 PC-4 v1.1 + Invariant 2, guard
   home firmed v1.2 — F-DW-SP2-003, logging + placement basis amended v1.3 —
   F-DW-SP3-002/004):** Any `FrameTypeCtl` frame arriving at the netingress `route`
   RouteFn closure with a `control_type` value not equal to `0x01 (DRAIN)` MUST be
   silently ignored — dropped (`route` returns `nil`; connection stays open; no
   forwarding to `routing.RouteFrame`/`OnFrameArrival`) with **NO logging of any kind**
   (BC-2.01.008 PC-4 v1.1 strengthened the rule to forbid diagnostic/informational
   logging as well as error/close — a v1.2 draft that logged "the unrecognized byte" is
   superseded). The guard's unconditional posture (no runtime "addressed to me" check) is
   justified by BC-2.01.008 v1.1 Invariant 2: netingress-arriving `ctl` frames are
   terminal-consumer by construction under the current architecture, with a REVISIT
   TRIGGER for a future inter-router-relay story (see the Ctl Receive Guard design
   constraint above for the full basis). The PE-upstream `FrameFn` closure is unrelated
   and carries no code change (frozen per Q2-AMENDED) — it is not this postcondition's
   guard home; a guard written against the `FrameFn` closure is a scope-conflation error.
   Specifically, `control_type = 0x02 (RESYNC)` is reserved (BC-2.01.008 EC-003: RESYNC
   before S-BL.RESYNC-FRAME ships → silent-ignore, no log).
5. **Ctl short-payload pin (Q-CTL-GUARD, BC-2.01.008 EC-002):** The `route` RouteFn in
   `cmd/switchboard` MUST silently discard any `FrameTypeCtl` frame with `payload_len < 4`
   (E-PRT-002 log — with no `conn.RemoteAddr()`/`from %s` clause, no `conn` in scope in
   the `route` closure, corrected v1.3 F-DW-SP3-003 — no connection close). Test:
   `TestRouter_CtlFrame_ShortPayload_NoConnClose` in
   `cmd/switchboard/router_drain_wire_test.go`.
6. **Ctl unknown-control_type pin (Q-CTL-GUARD, NEW v1.2 F-DW-SP2-003, respecified v1.3
   F-DW-SP3-002, BC-2.01.008 PC-4 v1.1):** The `route` RouteFn MUST silently ignore (NO
   log of any kind, no connection close) any `FrameTypeCtl` frame whose
   4-byte-or-longer payload carries a `control_type` value the router does not dispatch
   (i.e., anything other than `0x01`). This closes the previously-untested silent-ignore
   default. Test: `TestRouter_CtlFrame_UnknownControlType_SilentIgnore` in
   `cmd/switchboard/router_drain_wire_test.go`.

**Test names (v1.2 — `TestFrameFn_UnknownCtlSubtype_PassesToOnFrameArrival` REMOVED,
unreachable per the corrected guard home above; replaced by a second `route`-closure
pin test; descriptors updated v1.3 for the `drainObserverFiredHook` mechanism and the
no-log unknown-control_type respecification):**

- `TestDrainObserver_RegisteredAtStartup_FiresOnSignal` (integration, `cmd/switchboard/router_drain_wire_test.go` — sets `drainObserverFiredHook`; `startRunRouterWithConfig`; `cancel()` triggers shutdown; asserts the hook fired within a short timeout; asserts `drainCoord.Wait` returns `nil`; Q-AC003 v1.3 shape)
- `TestRouter_CtlFrame_ShortPayload_NoConnClose` (integration, `cmd/switchboard/router_drain_wire_test.go` — send `FrameTypeCtl` with `PayloadLen=1`; assert conn NOT closed; assert subsequent `FrameTypeData` dispatches without error; assert E-PRT-002 in log (no `conn.RemoteAddr()` clause); Q-CTL-GUARD pin)
- `TestRouter_CtlFrame_UnknownControlType_SilentIgnore` (respecified v1.3, integration, `cmd/switchboard/router_drain_wire_test.go` — send `FrameTypeCtl` with a 4-byte payload, `control_type = 0xFF`; liveness assertion: a subsequent well-formed `FrameTypeData` frame on the same connection is received/dispatched without error (proves conn NOT closed); negative assertion: `buf.String()` contains NO line matching the unknown-opcode pattern (no "unrecognized ctl control_type" substring or equivalent); assert NO panic and NO E-PRT-002 marker; pins BC-2.01.008 v1.1 PC-4 / Invariant 2 / FO-DRAIN-WIRE-001 / Q-CTL-GUARD)

**Test level:** integration + unit
**Test file:** `cmd/switchboard/router_drain_wire_test.go`

---

### AC-004 — VP-037 stage-1 discharge: TestE2E_RouterDrain_WireRoundTrip, untagged, drainCoord.Wait nil within window; verification_lock stays false (Q4-AMENDED binding, discharge-trace corrected v1.2)

**BC Anchors:** VP-037 stage-1 (wire round-trip; `drainCoord.Wait` returns nil within
10s); BC-2.09.002 PC-4 (all observer functions returned before timeout); BC-2.01.008
PC-2 (DRAIN opcode 0x01 at payload[0]); placement note Q4-AMENDED (amended v1.2,
F-DW-SP2-001). Traces to BC-2.09.002 PC-4.

**Precondition:** The current `internal/drain/vp037_e2e_test.go` at `e940fc2` is guarded
by `//go:build integration` (NOT `t.Skip` — a build-tag gate per placement note Q4
correction). Stage-1 adds a new untagged test in a different file/package
(`cmd/switchboard/router_drain_wire_test.go` — `startRunRouterWithConfig` is unexported
outside `cmd/switchboard`, so this test cannot live in `internal/drain`).

**Postconditions:**

1. A new untagged test `TestE2E_RouterDrain_WireRoundTrip` is added to
   `cmd/switchboard/router_drain_wire_test.go` (new file). The test has NO
   `//go:build integration` guard and runs in standard `go test ./...`.
2. Discharge-trace (Q4-AMENDED, binding — corrected v1.2, F-DW-SP2-001: the v1.1 trace
   using `testenv.NewWithRouters` + "router-0's ListenAddr" was physically unrealizable —
   `testenv.NewWithRouters` builds stub `RouterHandle`s that never run a real `runRouter`,
   so there is no bound listener to dial, and its `drainCtrl` is a DIFFERENT `*drain.Drain`
   than any `runRouter`'s `drainCoord`):
   1. `startRunRouterWithConfig(t, cfg, &buf)` → `errCh`, `cancel` (per Q3-AMENDED
      real-`runRouter` rule).
   2. Dial a raw TCP connection to `cfg.ListenAddr` (test acts as the simulated node).
   3. Wait for `nodeConnHook` (Q-AC002) to observe a `nodeConnRegistered` event for this
      connection — the mandatory accept/register barrier. Do NOT proceed to step 4
      before this fires, or the drain observer's `sendMap.Range` at `Signal` time may
      see zero entries and the test flakes.
   4. `cancel()` → shutdown block → `drainCoord.Signal` (production path 1, per
      Q3-AMENDED).
   5. Read from the simulated node's connection. This read is deterministic, not racy,
      because of the Shutdown ordering guarantee (Q-SEAM, v1.3 — F-DW-SP3-005): the
      router-wide flush pass forces the writer goroutine to flush the queued DRAIN frame
      before `ingressCancel` tears down the connection.
   6. Assert: received frame has `FrameType = FrameTypeCtl (0x03)` AND
      `payload[0] = 0x01 (DRAIN opcode per BC-2.01.008 PC-2)` within 2s deadline.
   7. Assert: `drainCoord.Wait` returns `nil` within the default drain window (do not
      assert against `drain.ErrTimeout` or any other specific error — see Q-AC005's
      flakiness note; this path is expected to succeed well within the window).
3. The existing `//go:build integration` guard on `internal/drain/vp037_e2e_test.go`'s
   `TestE2E_RouterDrain_NodesMigrateWithin2s` stays. That test is a separate,
   permanently-gated harness test (built on the same unrealizable `testenv.NewWithRouters`
   pattern step 2 above corrects) — it is NOT un-gated by this story and is not either
   stage's discharge target (disambiguated v1.2, F-DW-SP2-007; the stage-2 target is the
   `cmd/switchboard/router_pe_connector_test.go` copy of the same-named test).
4. `internal/drain/vp037_e2e_test.go` comment updated (doc-comment only — no test-logic
   change) to reflect that `TestE2E_RouterDrain_WireRoundTrip` in
   `cmd/switchboard/router_drain_wire_test.go` provides VP-037 stage-1 wire evidence, and
   that this file's own `TestE2E_RouterDrain_NodesMigrateWithin2s` is not the stage-2
   discharge target.
5. **`VP-037.md` carries the stage-1 partial-discharge lifecycle note (landed v1.5 per
   Q4-AMENDED architect ruling).** `verification_lock` MUST remain `false` after this
   story. The lock flip to `true` is stage-2 scope only (named follow-on story TBD).
   This story does NOT flip `verification_lock`.

**Test names:**

- `TestE2E_RouterDrain_WireRoundTrip` (integration, `cmd/switchboard/router_drain_wire_test.go` — untagged; real-`runRouter` (`startRunRouterWithConfig`); simulated node dials `cfg.ListenAddr`; waits for `nodeConnHook` accept/register barrier (Q-AC002); `cancel()` → drain; asserts `FrameTypeCtl + payload[0]=0x01` within 2s; `drainCoord.Wait` returns `nil`; shares its dial+barrier harness helper with AC-001's `TestDrainObserver_AssemblesAndSendsDRAINFrame` — see AC-001's ruling; VP-037 stage-1 discharge)

**Test level:** integration (untagged, runs in standard `go test`)
**Test file:** `cmd/switchboard/router_drain_wire_test.go`

---

### AC-005 — EC-003 forced-exit via extending TestRunRouter_ForcedExitPastDrainTimeout + drain.Signal panic recovery (Q5, Q-AC005 binding, amended v1.2)

**BC Anchors:** BC-2.09.002 EC-003 (drain timeout exceeded; unresponsive observer
forced-exit); BC-2.01.008 EC-003 (RESYNC-before-story silent-ignore — informational
only; evidenced by `TestRouter_CtlFrame_UnknownControlType_SilentIgnore` in AC-003);
placement note Q5 and Q-AC005 (F-DW-SP1-010, amended v1.2 F-DW-SP2-005). Traces to
BC-2.09.002 EC-003.

**Precondition:** `drain.go` Signal method fan-out goroutine currently has NO `defer
recover()` wrapper (verified `e940fc2`:126-129). The real drain-timeout injection seam
is `cfg.DrainTimeout = 100*time.Millisecond` (calls `drainTimeoutFor(cfg)` at
`mgmt_wire.go`:490, passed to `drain.New()`) — CONFIRMED, demonstrated live today in
the existing `TestRunRouter_ForcedExitPastDrainTimeout`
(`cmd/switchboard/router_drain_test.go`:305–419), which already sets `DrainTimeout`
this way, registers a blocking observer via `drainCoordHook`, triggers `Signal` via
`cancel()`, and asserts the EC-003 log marker plus elapsed-time bounds — the exact
shape AC-005 needs.

**Postconditions:**

1. **EC-003 evidence via EXTENDING the existing test (Q-AC005, binding — v1.2, prefer
   extension over a new duplicate):**
   - Extend `TestRunRouter_ForcedExitPastDrainTimeout` (e.g. a table-driven case) if
     AC-005's pass criterion needs a different window value than its existing 500ms;
     do NOT author a second, near-identical test function for the same property.
   - Assert: EC-003 log marker appears in `buf.String()`.
   - **Do NOT assert `drainCoord.Wait`'s return value against `drain.ErrTimeout`** (or
     any other specific error) — struck v1.2, F-DW-SP2-005: `runRouter`'s shutdown
     block passes the SAME `drainCtx` to both `drainCoord.Signal` and
     `drainCoord.Wait` (`mgmt_wire.go`:638–639), so at the deadline `Wait`'s internal
     `select` races `d.done` (`drain.ErrTimeout`) against `drainCtx.Done()`
     (`context.DeadlineExceeded`) pseudo-randomly — the existing test already avoids
     this exact assertion (see its in-file comment at ~:404–411).
   Test file: `cmd/switchboard/router_drain_test.go` (MODIFIED — existing test
   extended, NOT a new test function).
2. **Panic recovery production change to `internal/drain/drain.go`:** The fan-out
   goroutine in `Signal` gains a `defer recover()` wrapper (per Q5 binding). A
   panicking observer MUST log the panic and allow the drain coordinator to continue
   processing remaining observers. `drainCoord.Wait` still returns (either nil or
   ErrTimeout) regardless of observer panics. This wrapper is retained as
   belt-and-suspenders protection against any panic source inside an observer closure
   — it was never solely a mitigation for the send-on-closed-channel race the Q-SEAM
   v1.2 no-close invariant (AC-002) now eliminates structurally.
3. **Panic recovery unit test in `internal/drain/drain_test.go`:** A new test registers
   a panicking observer and asserts that: (a) the drain coordinator does NOT panic;
   (b) `drainCoord.Wait` returns within the window (either nil or ErrTimeout — not a
   process crash). Test name: `TestDrain_ObserverPanicRecovery`.

**Test names (v1.2 — `TestRunRouter_DrainEC003_UnresponsiveObserver` REMOVED as a
duplicate; the existing test is extended in place instead):**

- `TestRunRouter_ForcedExitPastDrainTimeout` (integration, `cmd/switchboard/router_drain_test.go` — EXISTING test, EXTENDED per Q-AC005 v1.2; `drainCoordHook` + blocked observer + `cfg.DrainTimeout`; asserts the EC-003 log marker + elapsed-time bounds only — NOT `drain.ErrTimeout`)
- `TestDrain_ObserverPanicRecovery` (unit, `internal/drain/drain_test.go` — MODIFIED; panicking observer registered via `RegisterObserver`; asserts coordinator does not panic; `Wait` returns without process kill)

**Test level:** integration (`router_drain_test.go`) + unit (`drain_test.go`)
**Test files:** `cmd/switchboard/router_drain_test.go`, `internal/drain/drain_test.go`

---

## Non-Goals

- **`sbctl router drain` CLI subcommand** — explicitly deferred (DRIFT-HS006-DRAIN-CLI-MISSING
  adjudicated at PR #103). Only the SIGTERM/shutdown path triggers drain. Targeted-drain UX
  is a follow-on ops-UX story.
- **SIGHUP reload** — deferred to `S-7.04-FU-SIGHUP-RELOAD` (merged PR #113).
- **PE outbound connector** — already shipped (`S-7.04-FU-PE-CONNECTOR`, merged PR #115).
- **RESYNC frame consumer** — The PE receive loop already forwards `FrameTypeCtl` frames to
  `FrameFn` (F-SP17-001 pin, verified `S-BL.PE-RECEIVE-LOOP-DELIVERY.md`:182). RESYNC frame
  dispatch from `FrameTypeCtl + control_type = 0x02` is `S-BL.RESYNC-FRAME` scope. This story
  MUST NOT implement RESYNC dispatch. The DRAIN receive path MUST silently ignore
  `control_type` values it does not recognize (FO-DRAIN-WIRE-001 binding rule, anchors to
  BC-2.01.008 Postcondition 4).
- **`control_type = 0x02`** is reserved per FO-DRAIN-WIRE-001 and BC-2.01.008 — defined-but-not-dispatched
  by this story. Incoming `FrameTypeCtl` frames with `control_type = 0x02` silently pass
  through until `S-BL.RESYNC-FRAME` adds the dispatch.
- **Session-bootstrap / Ed25519 HMAC** — The drain observer uses zero-envelope pattern
  (FO-DRAIN-WIRE-002). Full key derivation is a session-bootstrap follow-on.
- **FO-RECV-FWD-001 discharge** — NOT scope of this story (Q2-AMENDED, F-DW-SP1-002).
  `interfaceSet` stays `[]{peIfaceID}`, nil `ForwardFunc` stays. Deferred to the
  data-frame-widening follow-on story.
- **VP-037 verification_lock flip** — Stage-2 scope only. Lock stays `false` after this story.

---

## Files-Changed List (FCL)

**11 files (amended v1.2 — F-DW-SP2-001/004/008; was 10 in v1.1):**

| # | File | Change | BC / Anchor |
|---|------|--------|-------------|
| 1 | `cmd/switchboard/mgmt_wire.go` (MODIFIED) | Add `sync.Map[routing.InterfaceID, *nodeConn]` per-node send map (local var in `runRouter`; value type `*nodeConn{send, done, doneOnce}` as of v1.3, F-DW-SP3-001/005 — not a bare `chan []byte`); `OnAccept` closure via `ServeConfig` receives a netingress-populated `NodeHandle` and owns BEHAVIOR only (wraps `h.Send`/`h.Done` into `*nodeConn`, stores it, fires `nodeConnHook`, starts the writer goroutine whose `done` case flushes queued frames before exiting rather than dropping them); new router-scoped `writerWG sync.WaitGroup`; new bounded shutdown-flush phase between `drainCoord.Wait` and `ingressCancel()` (router-wide `doneOnce`-guarded `done` close pass, `writerWG.Wait()` bounded by `drainFlushTimeout` — PROVISIONAL ~200ms, mechanism BINDING; Shutdown ordering guarantee, new v1.3, F-DW-SP3-005); single startup drain observer (registered once at `drainCoord`-construction time, fires new `drainObserverFiredHook` test hook before iterating the live map at Signal time; Q-SINGLE-OBS, Q-AC003 v1.3, F-DW-SP3-006); ctl payload_len guard + `control_type` dispatch in the `route` RouteFn closure exclusively (Q-CTL-GUARD, firmed v1.2, unconditional-guard basis + no-log default arm firmed v1.3 — F-DW-SP3-002/004); `nodeConnHook` test hook (Q-AC002, NEW v1.2, mirrors `drainCoordHook`); update `#DEFERRED` doc comment at :408–412 → `#SHIPPED` for DRAIN-over-SVTN; **NOTE: `SetFrameCallback` closure UNCHANGED — `interfaceSet` stays `[]{peIfaceID}`, nil `ForwardFunc` stays (Q2-AMENDED)** | AC-001 / AC-002 / AC-003 / Q1 / Q-SEAM / Q-SINGLE-OBS / Q-CTL-GUARD / Q-AC002 / Q-AC003 |
| 2 | `internal/drain/drain.go` (MODIFIED) | Add `defer recover()` wrapper inside `Signal` fan-out goroutine body (per Q5 — production CODE gap from S-7.04 DELIVERY doc); update package doc comment — remove "Wave 7 follow-on" language | AC-005 / Q5 / BC-2.09.002 EC-003 |
| 3 | `internal/drain/drain_test.go` (MODIFIED) | Add `TestDrain_ObserverPanicRecovery` — panicking observer; drain coordinator does not panic; `Wait` returns | AC-005 / Q5 |
| 4 | `internal/drain/vp037_e2e_test.go` (MODIFIED, rescoped v1.2 — F-DW-SP2-001, doc-comment-only) | Update harness comment to reflect `TestE2E_RouterDrain_WireRoundTrip` in `cmd/switchboard/router_drain_wire_test.go` provides VP-037 stage-1 wire evidence; comment clarifies this file's own `TestE2E_RouterDrain_NodesMigrateWithin2s` (`go:build integration`) is NOT the stage-2 discharge target (that's the `cmd/switchboard/router_pe_connector_test.go` copy) — no test-logic change; integration-tagged test stays gated as-is permanently (structurally incapable of proving real migration — same `testenv.NewWithRouters` stub pattern) | AC-004 / Q4-AMENDED / VP-037 |
| 5 | `cmd/switchboard/router_drain_test.go` (NO CHANGE EXPECTED, v1.3 — F-DW-SP3-007) | The existing `TestRunRouter_ForcedExitPastDrainTimeout` (already uses `drainCoordHook` + blocked observer + `cfg.DrainTimeout`) already discharges EC-003 (Q-AC005) as-is; touch this file ONLY if a distinct drain-window value beyond its existing 500ms case is independently required. If touched, prefer extending the existing test (Q-AC005 v1.2) over a new duplicate; assert the EC-003 log marker only — NOT `drain.ErrTimeout` (flaky under the shared-`drainCtx` race, F-DW-SP2-005) | AC-005 / Q5 / BC-2.09.002 EC-003 |
| 6 | `cmd/switchboard/router_drain_wire_test.go` (NEW) | New file — DRAIN wire integration tests: `TestDrainObserver_AssemblesAndSendsDRAINFrame` (AC-001), `TestNetingress_OnAccept_RegistersNodeHandle` (AC-002, via `nodeConnHook`), `TestRunRouter_NodeConnClose_CleansUpSendMap` (AC-002, via `nodeConnHook`), `TestDrainObserver_RegisteredAtStartup_FiresOnSignal` (AC-003), `TestRouter_CtlFrame_ShortPayload_NoConnClose` (AC-003 Q-CTL-GUARD pin), `TestRouter_CtlFrame_UnknownControlType_SilentIgnore` (AC-003 Q-CTL-GUARD v1.2 second pin, NEW), `TestE2E_RouterDrain_WireRoundTrip` (AC-004 VP-037 stage-1, untagged, real-`runRouter` pattern) | AC-001 / AC-002 / AC-003 / AC-004 / Q1 / Q-SEAM / Q-SINGLE-OBS / Q-CTL-GUARD / Q-AC002 / VP-037 |
| 7 | `.factory/specs/architecture/ARCH-02-protocol-stack.md` (MODIFIED) | § "Outer Header Format" (NOT §3.1 — ARCH-02 has no §3.1, corrected v1.2, F-DW-SP2-009) frame_type table: add note that `ctl (0x03)` payloads carry a `control_type` byte discriminator; enumerate opcode `0x01 = DRAIN`, `0x02 = RESYNC (reserved)`; must land in same commit as FCL row 1 changes (wire-format spec-pair — Q1 same-commit obligation) | Q1 / BC-2.01.004 spec-pair / F-P8-008 precedent |
| 8 | `.factory/specs/behavioral-contracts/ss-01/BC-2.01.004.md` (MODIFIED) | Postcondition 2 outer-header layout `ctl` row: add `control_type` discriminator note with cross-reference to BC-2.01.008 as schema home (terminal-consumer carve-out Inv-2 already updated v1.4; this is the payload-note twin per same-commit spec-pair Q1 F-P8-008) | Q1 / ARCH-02 wire-format spec-pair |
| 9 | `.factory/specs/verification-properties/VP-037.md` (MODIFIED) | Stage-1 partial-discharge note already landed (v1.5; two-stage discharge ruling Q4-AMENDED). This story adds `TestE2E_RouterDrain_WireRoundTrip` evidence; `verification_lock` stays `false`; comment that stage-2 will flip it | AC-004 / VP-037 / Q4-AMENDED |
| 10 | `internal/netingress/netingress.go` (MODIFIED) | `ServeConfig` struct (gains `IfaceIDSeed routing.InterfaceID`, new v1.3 — F-DW-SP3-001, default 2) + `NodeHandle` struct (gains `Done chan struct{}`, new v1.3) + `OnAccept` hook wiring in `Serve`; `Serve` performs the DATA-ownership half of the ownership split (v1.3, F-DW-SP3-001) — allocates `IfaceID` from the `IfaceIDSeed`-seeded atomic counter, creates `Send`/`Done`, populates `NodeHandle` BEFORE calling `OnAccept`; import `internal/routing` for `routing.InterfaceID`; ARCH-08 §6.5 import-set update `{frame}` → `{frame, routing}` in same commit; package-doc rewrite at `:15–17` (F-DW-SP2-004, same-commit doc-sweep item — current text FORBIDS the `internal/routing` import this story requires; rewrite states `netingress` MAY import `internal/frame` and `internal/routing` (`InterfaceID` type only), with forward-edge/acyclic justification) | AC-002 / Q-SEAM / ARCH-08 |
| 11 | `.factory/specs/architecture/ARCH-08-dependency-graph.md` (MODIFIED — NEW to FCL, added v1.2, F-DW-SP2-008; was missing from the FCL entirely in v1.0/v1.1) | §6.5 `internal/netingress` row: `{frame}` → `{frame, routing}`; version bump `v2.11` → `v2.12` + changelog row citing this story and the netingress import-set change, SAME COMMIT as FCL row 10 (mirrors the v2.11 precedent's changelog-entry shape from S-BL.PE-RECEIVE-LOOP; struck the v1.1 "not a version-bump" claim) | Q-SEAM / ARCH-08 |

**Doc-comment sweep obligations (non-FCL rows — same-commit with row 1, except item 4 which is same-commit with row 10):**

1. `internal/frame/frame.go` — `FrameTypeCtl` doc comment: add one-line note that `ctl`
   payloads carry a `control_type` discriminator byte defined in BC-2.01.008 (schema home)
   and ARCH-02 § "Outer Header Format" (corrected v1.2 — was miscited as §3.1, which does
   not exist in ARCH-02). One-line addition to existing doc comment; no constant changes.
2. `internal/drain/drain.go` — package doc: remove "Wave 7 follow-on" language (in FCL row 2).
3. `cmd/switchboard/mgmt_wire.go` — `#DEFERRED` comment at :408-412: update to `#SHIPPED`
   for DRAIN-over-SVTN (in FCL row 1).
4. `internal/netingress/netingress.go`:15–17 package doc (NEW item, v1.2, F-DW-SP2-004,
   MED — same-commit with FCL row 10): the current text forbids exactly the
   `internal/routing` import this story's `NodeHandle`/`OnAccept` design requires; a
   deliberate reversal, not a follow-on cleanup. See the "netingress package-doc
   rewrite" subsection above (Send-Path Seam and Per-Node Send Map) for the full
   rewritten text and justification.

---

## Architecture Compliance Rules

- **ARCH-08 §6.5 pos-16 drain:** `internal/drain` MUST NOT gain any new import edges to
  internal packages. The `defer recover()` wrapper uses only stdlib primitives.
  `drain.go`'s package doc notes a potential `internal/routing` import (verified at
  `e940fc2`:11-13); this story does NOT instantiate it. The ARCH-08
  `drain --> routing` Mermaid edge remains planned-but-not-realized.
- **ARCH-08 §6.5 pos-18 netingress:** `internal/netingress` gains a new import of
  `internal/routing` (for `routing.InterfaceID`). This is a LAWFUL forward edge
  (pos 18 > pos 17). ARCH-08 §6.5 row MUST be updated in the same commit as
  `netingress.go` changes: `{frame}` → `{frame, routing}` — AND ARCH-08 itself bumps
  `v2.11` → `v2.12` in that same commit with a changelog row (amended v1.2,
  F-DW-SP2-008: this IS a version-bump-worthy change per ARCH-08's own v2.11
  precedent for the identical class of §6.5 import-set edit; the v1.1 "not a
  version-bump" claim is struck). See FCL row 11.
- **ARCH-08 §6.6.2 top leaf cmd/switchboard:** `cmd/switchboard` is at the DAG top leaf;
  all new import edges from `cmd/switchboard` are unconditionally lawful. No ARCH-08
  §6.4 registration required.
- **`sync.Map` ownership:** The per-node send map is LOCAL to `runRouter` in
  `cmd/switchboard/mgmt_wire.go`. It MUST NOT be exported or passed to `internal/drain`
  — the drain observer closure captures a reference at startup-registration time, and
  `drain.go` remains stdlib-only.
- **Wire-format spec-pair rule (F-P8-008):** ARCH-02 § "Outer Header Format" (NOT §3.1
  — ARCH-02 has no §3.1, corrected v1.2, F-DW-SP2-009) and BC-2.01.004 Postcondition 2
  MUST be amended in the same commit that adds the `control_type` dispatch to `mgmt_wire.go`.
  BC-2.01.008 is the schema home (PO ruling); BC-2.01.004 carries the cross-ref. Failing
  to update either spec document leaves the wire-format specification inconsistent.
- **FO-RECV-FWD-001 not consumed:** The `SetFrameCallback` closure MUST NOT be changed.
  `interfaceSet` stays `[]{peIfaceID}`, nil `ForwardFunc` stays. Any diff that touches
  these fields in the `SetFrameCallback` closure violates Q2-AMENDED.
- **FO-DRAIN-WIRE-001 forward-compat (BC-2.01.008 PC-4, guard home firmed v1.2 —
  F-DW-SP2-003):** Any new `FrameTypeCtl` dispatch branch in the netingress `route`
  RouteFn closure (exclusively — NOT the PE-upstream `FrameFn` closure, which is
  frozen per Q2-AMENDED) MUST have a silent-ignore default for unrecognized
  `control_type` values. A `switch control_type` with no `default: /* ignore */`
  clause is a spec violation.
- **Q-CTL-GUARD (guard home firmed v1.2 — F-DW-SP2-003):** The ctl frame receive path
  in `cmd/switchboard` MUST length-check payload before reading `payload[0]`. Guard
  home: the netingress `route` RouteFn closure, exclusively. Silent-ignore + E-PRT-002
  log; no connection close.
- **Line-number citation hygiene (F-DW-SP1-012):** Line-number citations in the story body
  (e.g., "mgmt_wire.go:549", ":528") were derived from `e940fc2`. Implementer MUST
  verify they still match HEAD at story-start. Cite SHA alongside any line number, or
  use function/symbol names instead of bare offsets. Note: `ef1ee1e` (develop HEAD post-PR
  #119) touched only `cmd/sbctl/client.go` + `client_test.go` — no DRAIN-WIRE surface
  overlap; anchors at `e940fc2` remain valid, confirmed.
- **go.md rule compliance:** New test files MUST pass `go test -race -count=1 ./...`
  and `golangci-lint run` without suppressions.

---

## Library & Framework Requirements

All imports must use existing module versions pinned in `go.mod` at develop `e940fc2`.
No new external dependencies are introduced. Internal packages used:

| Package | Import path | Position | Verified at | Used by |
|---------|-------------|----------|-------------|---------|
| `frame` | `internal/frame` | 2 | `e940fc2` | `cmd/switchboard` (DRAIN frame assembly: `EncodeOuterHeader`, `FrameTypeCtl`); `internal/netingress` (existing) |
| `drain` | `internal/drain` | 16 | `e940fc2` | `cmd/switchboard` (existing: `RegisterObserver`, `Signal`, `Wait`, `ErrTimeout`, `Observer` type) |
| `routing` | `internal/routing` | 17 | `e940fc2` | `cmd/switchboard` (existing: `InterfaceID`); `internal/netingress` NEW import (`InterfaceID` for `NodeHandle.IfaceID`) |
| `netingress` | `internal/netingress` | 18 | `e940fc2` | `cmd/switchboard` (existing: `Serve` — gains `ServeConfig` param) |
| `testenv` | `internal/testenv` | 23 | `e940fc2` | not used by this story's own drain-wire tests (`testenv.NewWithRouters` never runs a real `runRouter` — see AC-004/Q4-AMENDED); listed for completeness as a dependency of pre-existing `internal/drain` harness tests this story does not modify |

---

## File Structure Requirements

**New file created by this story:**

- `cmd/switchboard/router_drain_wire_test.go` — DRAIN wire integration + unit tests
  (AC-001, AC-002, AC-003, AC-004) including VP-037 `TestE2E_RouterDrain_WireRoundTrip`
  (untagged stage-1); FO-DRAIN-WIRE-001 forward-compat guard tests; Q-SEAM lifecycle
  tests; Q-CTL-GUARD pin test.

**Modified files:**

- `cmd/switchboard/mgmt_wire.go` — per-node `sync.Map[routing.InterfaceID, *nodeConn]`
  (value type `*nodeConn{send, done, doneOnce}`, v1.3); `OnAccept` closure receives a
  netingress-populated `NodeHandle` and owns BEHAVIOR only (never-close invariant on
  `send`); `writerWG` + bounded shutdown-flush phase between `Wait` and `ingressCancel()`
  (v1.3); single startup observer + `drainObserverFiredHook` test hook (v1.3);
  `nodeConnHook` test hook; ctl guard (route closure only, no-log default arm v1.3); `#DEFERRED`
  → `#SHIPPED` doc comment; `SetFrameCallback` closure UNCHANGED
- `internal/netingress/netingress.go` — `ServeConfig` (gains `IfaceIDSeed`, v1.3) +
  `NodeHandle` (gains `Done`, v1.3) types; `Serve` performs IfaceID/Send/Done allocation
  (DATA ownership, v1.3) and `OnAccept` hook wiring; new `internal/routing` import;
  package-doc rewrite at `:15–17`
- `internal/drain/drain.go` — `defer recover()` in Signal fan-out; package doc update
- `internal/drain/drain_test.go` — `TestDrain_ObserverPanicRecovery`
- `internal/drain/vp037_e2e_test.go` — comment update (doc-comment only); integration-tag stays
- `cmd/switchboard/router_drain_test.go` — NO CHANGE EXPECTED (v1.3, F-DW-SP3-007);
  `TestRunRouter_ForcedExitPastDrainTimeout` already discharges EC-003 as-is — touch only
  if a distinct drain-window value beyond its existing 500ms case is independently
  required, and EXTEND rather than add a new test
- `.factory/specs/architecture/ARCH-02-protocol-stack.md` — § "Outer Header Format" ctl payload discriminator note
- `.factory/specs/behavioral-contracts/ss-01/BC-2.01.004.md` — Postcondition 2 ctl row + BC-2.01.008 cross-ref
- `.factory/specs/verification-properties/VP-037.md` — stage-1 wire evidence note; lock stays false
- `.factory/specs/architecture/ARCH-08-dependency-graph.md` — §6.5 netingress row update; v2.11 → v2.12 + changelog row

---

## Estimated Test Surface

Estimated test counts are forecasts; actual delivered count may differ after adversarial hardening.

**`internal/drain/drain_test.go` (MODIFIED — unit):**

| Function | Proves |
|----------|--------|
| `TestDrain_ObserverPanicRecovery` | Panicking observer does not kill coordinator; `Wait` returns; panic logged (AC-005 / Q5) |

**`cmd/switchboard/router_drain_test.go` (MODIFIED — integration, EXTENDS an existing
test, v1.2 F-DW-SP2-005 — no new test function):**

| Function | Proves |
|----------|--------|
| `TestRunRouter_ForcedExitPastDrainTimeout` (EXTENDED) | `drainCoordHook` + blocked observer + `cfg.DrainTimeout`; EC-003 log marker fires + elapsed-time bounds — NOT `drain.ErrTimeout` (flaky, see Q-AC005) (AC-005 / Q5 / BC-2.09.002 EC-003) |

**`cmd/switchboard/router_drain_wire_test.go` (NEW — unit + integration):**

| Function | Proves |
|----------|--------|
| `TestDrainObserver_AssemblesAndSendsDRAINFrame` | Single startup observer sends `FrameTypeCtl + control_type=0x01` to simulated node, via real-`runRouter` + `nodeConnHook` accept/register barrier (AC-001 / Q1 / Q-SEAM / Q-AC002) |
| `TestNetingress_OnAccept_RegistersNodeHandle` | `OnAccept` fires before `ServeConn`; `NodeHandle.IfaceID >= 2`; `nodeConnHook` records `nodeConnRegistered` (AC-002 / Q-SEAM / Q-AC002) |
| `TestRunRouter_NodeConnClose_CleansUpSendMap` | Conn close → `nodeConnHook` records `nodeConnRemoved`; writer goroutine exits (via `done`); `send` NEVER closed (AC-002 / Q-SEAM cleanup / Q-AC002) |
| `TestDrainObserver_RegisteredAtStartup_FiresOnSignal` | Single startup observer fires; `drainObserverFiredHook` set (v1.3 — replaces the undefined "side-channel flag"); `drainCoord.Wait` nil (AC-003 / Q-AC003 / Q-SINGLE-OBS) |
| `TestRouter_CtlFrame_ShortPayload_NoConnClose` | `FrameTypeCtl + PayloadLen=1`; conn NOT closed; subsequent frame dispatches; E-PRT-002 in log (no `conn.RemoteAddr()` clause, v1.3); Q-CTL-GUARD pin, `route` closure (AC-003) |
| `TestRouter_CtlFrame_UnknownControlType_SilentIgnore` (respecified v1.3) | `FrameTypeCtl` 4-byte payload, `control_type=0xFF`; conn NOT closed (liveness via subsequent frame); NO log line of any kind (negative assertion, BC-2.01.008 v1.1 PC-4); NO panic, NO E-PRT-002; BC-2.01.008 PC-4 / Invariant 2 / FO-DRAIN-WIRE-001 / Q-CTL-GUARD (AC-003) |
| `TestE2E_RouterDrain_WireRoundTrip` | **VP-037 stage-1** — real-`runRouter`; simulated node dials + awaits `nodeConnHook` barrier; `drainCoord.Wait` returns `nil`; untagged (AC-004 / Q4-AMENDED / VP-037 / Q-AC002) |

**Estimated new test count (forecast, v1.2):** ~8 net-new test functions (1
drain_test.go unit + 0 new router_drain_test.go functions — the EC-003 test is an
extension of the existing `TestRunRouter_ForcedExitPastDrainTimeout`, not a new
function — + 7 router_drain_wire_test.go unit+integration). Same total as v1.1's
stated ~8, but the composition shifted (F-DW-SP2-001/002/003/005): `TestRunRouter_DrainEC003_UnresponsiveObserver`
is no longer counted as a new function (folded into the existing extended test,
router_drain_test.go net-new drops 1→0); `TestFrameFn_UnknownCtlSubtype_PassesToOnFrameArrival`
(removed as unreachable) is replaced 1-for-1 by `TestRouter_CtlFrame_UnknownControlType_SilentIgnore`
(wire_test.go stays at 7); `TestE2E_RouterDrain_WireRoundTrip` was always slated for
`cmd/switchboard/router_drain_wire_test.go`, now confirmed as its only possible location
(`startRunRouterWithConfig` is unexported outside `cmd/switchboard`).
This is a pre-implementation forecast; adversarial hardening typically adds additional
pin tests.

**v1.3 note (F-DW-SP3-001..008):** the test-surface COMPOSITION is unchanged from v1.2 —
no test functions were added or removed by the pass-3 remediation. Several test bodies
were reworded (`TestNetingress_OnAccept_RegistersNodeHandle`,
`TestRunRouter_NodeConnClose_CleansUpSendMap`, `TestDrainObserver_RegisteredAtStartup_FiresOnSignal`,
`TestRouter_CtlFrame_UnknownControlType_SilentIgnore`) to match the ownership split, the
new `drainObserverFiredHook` observable, and BC-2.01.008 v1.1's strengthened no-logging
rule. Estimated total stays ~8 net-new.

---

## Tasks

1. [ ] Read placement note `decisions/S-7.04-FU-DRAIN-WIRE-placement-note.md` v1.3 and
       all four BCs (`BC-2.01.008` v1.1, `BC-2.01.004` v1.4, `BC-2.01.005` v1.2,
       `BC-2.09.002` v1.2) before writing any code. Verify all symbols against the
       Appendix A sweep table at `e940fc2`. Note `ef1ee1e` is develop HEAD (PR #119 —
       `cmd/sbctl/client.go` only; DRAIN-WIRE surface unaffected; `e940fc2` anchors
       remain valid). Pay particular attention to BC-2.01.008 v1.1's strengthened PC-4
       no-logging rule and new Invariant 2 (terminal-consumer-by-construction) before
       writing the ctl guard in step 6.
2. [ ] Amend ARCH-02 § "Outer Header Format" (NOT §3.1 — ARCH-02 has no §3.1) `ctl`
       frame_type row with `control_type` discriminator note (opcode `0x01 = DRAIN`,
       `0x02 = RESYNC (reserved)`); in the same commit amend BC-2.01.004 Postcondition 2
       `ctl` row identically with cross-ref to BC-2.01.008 as schema home. These must
       land in the same commit as the first production code change to `mgmt_wire.go`
       (Q1 wire-format spec-pair obligation, F-P8-008).
3. [ ] Add `ServeConfig` (gains `IfaceIDSeed routing.InterfaceID`, default 2, v1.3 —
       F-DW-SP3-001) + `NodeHandle` (gains `Done chan struct{}`, v1.3) types to
       `internal/netingress/netingress.go`. `Serve` owns DATA creation (ownership split,
       v1.3): when `cfg.OnAccept != nil`, allocate `ifaceID` from the `IfaceIDSeed`-
       seeded atomic counter, create `send := make(chan []byte, 32)` and
       `done := make(chan struct{})`, populate `h := NodeHandle{IfaceID: ifaceID, Send:
       send, Done: done}`, then call `cfg.OnAccept(conn, h)` — `OnAccept` receives a
       fully-built handle and constructs none of its fields. Import `internal/routing`
       (ARCH-08 §6.5 `{frame}` → `{frame, routing}` in same commit — Q-SEAM ARCH-08
       ruling). In the SAME commit, rewrite `netingress.go`:15–17's package-doc "Import
       constraints" paragraph, which currently forbids the `internal/routing` import
       this task just added — replace with text permitting `internal/frame` +
       `internal/routing` (`InterfaceID` type only) and the forward-edge/acyclic
       justification (F-DW-SP2-004).
4. [ ] Add per-node send map (`sync.Map[routing.InterfaceID, *nodeConn]`, value type
       `type nodeConn struct { send chan []byte; done chan struct{}; doneOnce *sync.Once }`
       — v1.3, F-DW-SP3-001/005, not a bare `chan []byte`) to `runRouter` in
       `cmd/switchboard/mgmt_wire.go`. Wire `ServeConfig.OnAccept` as the BEHAVIOR half
       of the ownership split (v1.3): receive the netingress-populated `NodeHandle h`;
       wrap `nc := &nodeConn{send: h.Send, done: h.Done, doneOnce: new(sync.Once)}`;
       store `sendMap.Store(h.IfaceID, nc)`; fire
       `nodeConnHook(nodeConnRegistered, h.IfaceID)`; increment a new router-scoped
       `writerWG sync.WaitGroup` and start a writer goroutine that `select`s on
       `nc.send`/`nc.done` (sole writer to conn) — its `done` case flushes any
       already-queued frame before exiting rather than dropping it (F-DW-SP3-005);
       return a behavior-cleanup func that deletes the map entry, fires
       `nodeConnHook(nodeConnRemoved, h.IfaceID)`, and calls
       `nc.doneOnce.Do(func() { close(nc.done) })` (NEVER `close(nc.send)` — single-
       closer/no-send-after-close invariant, F-DW-SP2-002/F-DW-SP3-001/005). Add the
       package-level `nodeConnHook func(event nodeConnEvent, ifaceID routing.InterfaceID)`
       test hook (nil in production; mirrors `drainCoordHook`; Q-AC002, F-DW-SP2-006)
       and the package-level `drainObserverFiredHook func()` test hook (nil in
       production; new v1.3, Q-AC003, F-DW-SP3-006) in the same file. Register the
       SINGLE startup drain observer at `drainCoord`-construction time; its body fires
       `drainObserverFiredHook` (if non-nil) as its FIRST statement, before
       `sendMap.Range` iterates the live map (Q-SINGLE-OBS). Do NOT call
       `drainCoord.RegisterObserver` from within `OnAccept` — only the startup observer
       is registered.
5. [ ] Add the router-wide shutdown-flush phase (Shutdown ordering guarantee, new v1.3,
       F-DW-SP3-005) to `runRouter`'s shutdown block in `cmd/switchboard/mgmt_wire.go`,
       between the existing `drainCoord.Wait(drainCtx)` call and `ingressCancel()`:
       range over the per-node send map calling `nc.doneOnce.Do(func() { close(nc.done)
       })` on every still-live entry (forces each writer goroutine to flush its queued
       frame and exit); then wait on `writerWG` bounded by a new `drainFlushTimeout`
       constant (PROVISIONAL value ~200ms — the bounded-flush MECHANISM is BINDING, the
       exact duration is left to tune against real write-syscall latency for a 48-byte
       frame; deliberately NOT `cfg.DrainTimeout`, which is already spent by this point);
       proceed to the existing `ingressCancel()` regardless of whether the bound was hit
       (best-effort, consistent with Q3.P1). This closes the AC-001/AC-004 egress race
       where `Wait` returning only proves queuing, not writing.
6. [ ] Add ctl payload_len guard AND `control_type` dispatch to the netingress `route`
       RouteFn closure in `cmd/switchboard` — exclusively; NOT the PE `FrameFn` closure,
       which is frozen per Q2-AMENDED (Q-CTL-GUARD, guard home firmed v1.2, unconditional-
       guard basis + logging asymmetry firmed v1.3, F-DW-SP3-002/003/004 — PO ruling
       BC-2.01.008 v1.1 Inv-2/PC-4 consumed): `payload_len < 4` → E-PRT-002 log (no
       `conn.RemoteAddr()` clause — no `conn` in scope in the `route` closure) +
       silent-ignore + return nil, no connection close; `control_type` `switch` with a
       `case 0x01` (DRAIN, no-op) and a `default:` arm that silently ignores unrecognized
       values with **NO logging of any kind** (not even diagnostic/informational — do
       NOT add a log call to this arm) and no connection close — FO-DRAIN-WIRE-001
       forward-compat rule. The guard's unconditional placement (no runtime "addressed
       to me" check) rests on BC-2.01.008 v1.1 Invariant 2 (terminal-consumer by
       construction); carry Inv-2's revisit trigger forward in a code comment for any
       future inter-router-relay story.
7. [ ] Update `#DEFERRED` doc comment at `mgmt_wire.go`:408-412 → `#SHIPPED` for
       DRAIN-over-SVTN; add one-line `control_type` note to `FrameTypeCtl` doc comment
       in `internal/frame/frame.go` citing BC-2.01.008 and ARCH-02 § "Outer Header
       Format" (non-FCL doc-sweep, same commit as production code). **Do NOT change
       `SetFrameCallback` closure — `interfaceSet` and nil `ForwardFunc` are UNCHANGED
       (Q2-AMENDED).**
8. [ ] Add `defer recover()` wrapper to `drain.Signal` fan-out goroutine in
       `internal/drain/drain.go` (production CODE change; Q5 binding). Update package
       doc comment to remove "Wave 7 follow-on" language.
9. [ ] Write `TestDrain_ObserverPanicRecovery` in `internal/drain/drain_test.go` (RED
       gate before step 8).
10. [ ] EC-003: `cmd/switchboard/router_drain_test.go` needs NO CHANGE EXPECTED (v1.3,
        F-DW-SP3-007) — the existing `TestRunRouter_ForcedExitPastDrainTimeout` already
        discharges EC-003 (Q-AC005) as-is via `drainCoordHook` + blocked observer +
        `cfg.DrainTimeout`. Touch this file ONLY if a distinct drain-window value beyond
        its existing 500ms case is independently required, and EXTEND the existing test
        (do NOT add a new duplicate test function, Q-AC005 v1.2, F-DW-SP2-005). If
        touched, assert the EC-003 log marker only — do NOT assert `drainCoord.Wait`'s
        return value against `drain.ErrTimeout` (flaky under the shared-`drainCtx`
        race).
11. [ ] Create `cmd/switchboard/router_drain_wire_test.go` (NEW); write RED gate tests:
        `TestDrainObserver_AssemblesAndSendsDRAINFrame` (AC-001),
        `TestNetingress_OnAccept_RegistersNodeHandle` (AC-002, via `nodeConnHook`;
        asserts `NodeHandle.IfaceID >= 2` — netingress allocates it, v1.3),
        `TestRunRouter_NodeConnClose_CleansUpSendMap` (AC-002 cleanup, via `nodeConnHook`),
        `TestDrainObserver_RegisteredAtStartup_FiresOnSignal` (AC-003 Q-AC003 v1.3 —
        sets `drainObserverFiredHook`, no node connection required),
        `TestRouter_CtlFrame_ShortPayload_NoConnClose` (AC-003 Q-CTL-GUARD pin, no
        `conn.RemoteAddr()` assertion),
        `TestRouter_CtlFrame_UnknownControlType_SilentIgnore` (AC-003 Q-CTL-GUARD
        respecified v1.3, F-DW-SP3-002 — liveness assertion via a subsequent valid
        frame PLUS a negative log assertion; do NOT assert a log line is present),
        `TestE2E_RouterDrain_WireRoundTrip` (AC-004 VP-037 stage-1, untagged, real-
        `runRouter` via `startRunRouterWithConfig` — NOT `testenv.NewWithRouters`, which
        never runs a real `runRouter`, per F-DW-SP2-001). All RED before production
        code. All AC-001/AC-002/AC-004 tests share a dial-and-await-`nodeConnRegistered`
        harness helper (Q-AC002).
12. [ ] Add ARCH-08 §6.5 `netingress` import-set row update (`{frame}` → `{frame,
        routing}`) AND bump ARCH-08 `2.11` → `2.12` with a changelog row citing this
        story, in the same commit as step 3 (`internal/netingress/netingress.go`). This
        IS an ARCH-08 version bump (amended v1.2, F-DW-SP2-008 — struck the v1.1
        "not a version-bump" claim; ARCH-08's own v2.11 changelog entry bumped for
        exactly this class of §6.5 import-set edit).
13. [ ] Update `internal/drain/vp037_e2e_test.go` comment (doc-comment only, no
        test-logic change) to reflect that `TestE2E_RouterDrain_WireRoundTrip` in
        `cmd/switchboard/router_drain_wire_test.go` provides VP-037 stage-1 wire
        evidence, and that this file's own `TestE2E_RouterDrain_NodesMigrateWithin2s`
        (`go:build integration`) is a separate, permanently-gated harness test — NOT
        either stage's discharge target (the stage-2 target is the
        `cmd/switchboard/router_pe_connector_test.go` copy, F-DW-SP2-007). Guard stays
        (Q4-AMENDED). VP-037.md `verification_lock` MUST remain `false` — do NOT flip
        it this story.
14. [ ] Verify `go test -race -count=1 ./...` full green; `golangci-lint run` 0 issues;
        `gofumpt` no diffs.

---

## Forward Obligations Consumed

| FO ID | Origin | Description | Consumed by | Notes |
|-------|--------|-------------|-------------|-------|
| — | — | — | — | FO-RECV-FWD-001 is NOT consumed by this story (Q2-AMENDED, F-DW-SP1-002). Nil ForwardFunc stays; interfaceSet unchanged. |

---

## Forward Obligations Emitted

| FO ID | Target | Description |
|-------|--------|-------------|
| FO-DRAIN-WIRE-001 | S-BL.RESYNC-FRAME | `FrameTypeCtl` payload uses `control_type` byte discriminator (BC-2.01.008). `control_type = 0x01` (DRAIN) is defined here. `control_type = 0x02` (RESYNC) is RESERVED but NOT dispatched — that dispatch lives in S-BL.RESYNC-FRAME. The DRAIN receive path MUST silently ignore `control_type` values it does not recognize (BC-2.01.008 Postcondition 4 forward-compatibility rule). |
| FO-DRAIN-WIRE-002 | session-bootstrap story | The drain observer assembles DRAIN frames using a per-node `Envelope` with zero SrcAddr/DstAddr/FrameAuthKey (matching the PE connector's zero-envelope pattern). Full bootstrap with Ed25519 key material is a session-bootstrap follow-on. HMAC verification of inbound DRAIN frames on the receiving node's router is a security hardening story. |
| FO-RECV-FWD-001 | data-frame-widening story | DEFERRED from S-BL.PE-RECEIVE-LOOP. Nil `ForwardFunc` in `mgmt_wire.go` SetFrameCallback closure — must be wired when the router's interface set widens to carry upstream Data frames to node channels. NOT consumed by this story. Remains an obligation on the future widening story. |

---

## Changelog

| Version | Date | Change |
|---------|------|--------|
| 1.3 | 2026-07-11 | Consumed placement note v1.3 (spec-adversarial pass-3 remediation, F-DW-SP3-001..008, all confirmed; POL-001) and BC-2.01.008 v1.1 (PO ruling consumed for F-002/F-004). **AC-002 (F-DW-SP3-001, HIGH — ownership split):** the `NodeHandle`/`OnAccept` data-ownership contradiction is resolved — `netingress.Serve` now owns DATA creation (allocates `ifaceID` from an atomic counter seeded by new `ServeConfig.IfaceIDSeed`, default 2; creates `send`/`done`; populates `NodeHandle` before calling `OnAccept`); `runRouter`'s `OnAccept` closure owns BEHAVIOR (wraps `h.Send`/`h.Done` into `*nodeConn{send, done, doneOnce}`, stores it in the per-node send map, fires `nodeConnHook`, starts the writer goroutine). The send-map value type is `*nodeConn`, not a bare `chan []byte`; `TestNetingress_OnAccept_RegistersNodeHandle` keeps its `IfaceID >= 2` assertion (now legitimately satisfiable — netingress allocates) and `TestRunRouter_NodeConnClose_CleansUpSendMap` asserts via `nodeConnHook` events plus writer-goroutine exit via `done` (send never closed). **New production design constraint (F-DW-SP3-005, MED) — Shutdown ordering guarantee:** `runRouter`'s shutdown block gains a bounded flush phase between `drainCoord.Wait` and `ingressCancel()`: close every live node's `done` (router-wide pass via `doneOnce`) → `writerWG.Wait()` bounded by `drainFlushTimeout` (~200ms, PROVISIONAL value; the bounded-flush MECHANISM is BINDING) → `ingressCancel()`. New router-scoped `writerWG sync.WaitGroup`; the writer goroutine's `done` case now flushes any already-queued frame before exiting instead of dropping it. AC-001 and AC-004 discharge-trace steps are UNCHANGED; both gain a cross-reference note at their read step pointing to this guarantee as the mechanism that makes the read deterministic rather than flaky. **AC-003 (F-DW-SP3-006, MED — full observable replacement):** the undefined "flag inside the observer closure" is replaced by a new package-level `drainObserverFiredHook func()` (nil in prod, mirrors `drainCoordHook`/`nodeConnHook`), fired unconditionally as the FIRST statement of the single startup observer's body, before `sendMap.Range`. Precondition and test shape reworded to this mechanism; AC count stays 5. **Guard + pin tests (F-DW-SP3-002/003/004, BC-2.01.008 v1.1 consumed):** (a) the unknown-`control_type` default arm in the `route` RouteFn's ctl guard loses its log call entirely — BC-2.01.008 PC-4 v1.1 forbids ANY logging (including diagnostic/informational) on this path; `TestRouter_CtlFrame_UnknownControlType_SilentIgnore` respecified around a liveness assertion (a subsequent valid frame is still processed) plus a negative log assertion (no matching line appears), replacing the prior positive log assertion. (b) the E-PRT-002 short-payload log loses `conn.RemoteAddr()`/`from %s` — no `conn` is in scope in the `route` closure (compile error as previously written). (c) the guard's unconditional (no runtime "addressed to me" check) placement is now explicitly justified by BC-2.01.008 v1.1 Invariant 2 (netingress-arriving ctl frames are terminal-consumer by construction), carrying Inv-2's revisit trigger (a future inter-router-relay story) forward wherever the story states the guard's placement rationale. **F-DW-SP3-008 (LOW) phase-order fix:** every story occurrence of the observer being registered "before serving any connections"/"before any node connects" is restated: registered at `drainCoord`-construction time, before `drainCoord.Signal` (the only ordering that matters); the send map's zero-value usability makes accept-loop-vs-registration ordering immaterial; no phase reorder. **Body sweep (OBS-2):** struck or corrected every occurrence of "OnAccept creates send"/"creates send := make", "allocated by runRouter", "before serving any connections", `conn.RemoteAddr`/`from %s`, the unknown-opcode log string, "flag inside the observer closure", a bare `chan []byte` as the send-map value type, and stale AC-003 test text asserting the old flag/log shape. Frontmatter: `inputDocuments` re-pinned (placement note v1.3, BC-2.01.008 v1.1; VP-037 stays v1.5; BC-2.09.002 v1.2 / BC-2.01.004 v1.4 / BC-2.01.005 v1.2 unchanged); `bc_traces`/`architecture_modules` comments refreshed for the ownership split, `writerWG`/shutdown-flush phase, and `drainObserverFiredHook`. FCL stays at 11 rows: row 1 gains `writerWG`/`doneOnce`/`drainObserverFiredHook`/shutdown-flush-phase; row 5 reworded from MODIFIED to NO CHANGE EXPECTED (the existing `TestRunRouter_ForcedExitPastDrainTimeout` already discharges EC-003 as-is; touch only if a distinct drain-window value beyond its existing 500ms case is independently required); row 10 gains `ServeConfig.IfaceIDSeed` + `NodeHandle` population responsibility. Tasks updated: task 3 (netingress owns DATA creation), task 4 (OnAccept receives a pre-populated `NodeHandle`, wraps it in `*nodeConn`, writer flushes on `done`), a new task for the shutdown-flush phase, task 5 (no-log default arm), task 10 (respecified pin-test RED gates). PROVISIONAL: `drainFlushTimeout` (~200ms; mechanism BINDING, exact duration left to the implementer to tune). Estimated test surface unchanged at ~8 net-new (composition unchanged — test bodies reworded, no functions added or removed). |
| 1.2 | 2026-07-11 | Consumed placement note v1.2 (spec-adversarial pass-2 remediation, F-DW-SP2-001..010, all confirmed; POL-001). **AC-004 (F-DW-SP2-001, HIGH):** discharge-trace was physically unrealizable (`testenv.NewWithRouters` never runs a real `runRouter`, no bound listener); replaced with the real-`runRouter` pattern (`startRunRouterWithConfig`), a mandatory `nodeConnHook` accept/register barrier before triggering drain, and relocation confirmed to `cmd/switchboard/router_drain_wire_test.go` (the only file `TestE2E_RouterDrain_WireRoundTrip` can live in — `startRunRouterWithConfig` is unexported outside `cmd/switchboard`); FCL row 4 (`vp037_e2e_test.go`) rescoped to doc-comment-only. **AC-002 (F-DW-SP2-002 + F-DW-SP2-006):** `send` channel is NEVER closed (single-closer/no-send-after-close invariant — eliminates a close-race panic against the single observer's live-map `Range`); cleanup closes a new private `done` channel instead; postconditions/tests reworded from "send closed" to "writer goroutine exits via done, send NOT closed"; observability changed from asserting the unexported `sync.Map` directly to a new `nodeConnHook(event, ifaceID)` test hook (Q-AC002, mirrors `drainCoordHook`), fired at OnAccept-registration and cleanup-removal. **AC-003 (F-DW-SP2-003):** PC-4's forward-compat guard corrected to live EXCLUSIVELY in the netingress `route` RouteFn closure — NOT the frozen PE `FrameFn` closure (Q2-AMENDED); `TestFrameFn_UnknownCtlSubtype_PassesToOnFrameArrival` REMOVED as unreachable; pin tests are `TestRouter_CtlFrame_ShortPayload_NoConnClose` (existing) + NEW `TestRouter_CtlFrame_UnknownControlType_SilentIgnore`. **AC-001 (Q3-AMENDED v1.2 rewrite):** recipe reworded off the superseded per-connection `RegisterObserver` event to the actual causal chain — connection-open → `nodeConnHook` barrier → `Signal` → single observer's `sendMap.Range` → per-node send; kept as a SEPARATE AC from AC-004, sharing one dial-and-await-barrier harness helper (story-writer ruling: AC-001 traces BC-2.09.002 PC-1 reachability, AC-004 traces the VP-037 wire-round-trip-plus-Wait verification property — distinct obligations warrant distinct tests even with near-identical recipes). **FCL 10→11 rows:** row 11 = `ARCH-08-dependency-graph.md` (§6.5 netingress row + v2.11→v2.12 version bump + changelog row, same commit as row 10 — F-DW-SP2-008 struck the v1.1 "not a version-bump" claim); row 5 = `TestRunRouter_ForcedExitPastDrainTimeout` EXTENDED (not `TestRunRouter_DrainEC003_UnresponsiveObserver` as a new duplicate test — F-DW-SP2-005 also drops the flaky `drain.ErrTimeout` assertion in favor of the EC-003 log marker only); row 1 gains `nodeConnHook`; row 10 (`netingress.go`) gains the package-doc rewrite at `:15–17` (F-DW-SP2-004, forbids-then-requires-the-import reversal, forward-edge/acyclic justification). **Other fixes:** ARCH-02 §3.1 citations corrected to § "Outer Header Format" throughout (ARCH-02 has no §3.1, F-DW-SP2-009); `peIfaceID` :528→:534 and `drainTimeoutFor` :491→:490 citations were already correct in the story body (no story-side fix needed, confirmed via sweep); ARCH-02/BC-2.01.005 added to `inputDocuments` for traceability completeness (placement note bc_traces parity; no new story-specific AC discharge). Frontmatter: version 1.1→1.2; `inputDocuments` re-pinned (placement note v1.2, VP-037 v1.5, BC-2.01.005 v1.2 added, ARCH-08 v2.11 added); `architecture_modules` comments refreshed (nodeConnHook, package-doc rewrite). Test-surface sweep terms and dispositions, final AC-001-vs-AC-004 ruling, and final counts: see harvest report to main. Estimated test surface: ~8 net-new (unchanged total from v1.1's stated ~8; composition shifted — see Estimated Test Surface section). |
| 1.1 | 2026-07-11 | Consumed placement note v1.1 (F-DW-SP1-001..012 remediation + PO rulings). PO rulings consumed: BC-2.09.002 v1.2 (best-effort delivery BINDING, Q3.P1 PROVISIONAL resolved to option 2), BC-2.01.004 v1.4 (terminal-consumer ctl carve-out), BC-2.01.008 v1.0 NEW (control_type schema home, Postcondition 4 forward-compat silent-ignore obligation). FO-RECV-FWD-001 consumed→DEFERRED (Q2-AMENDED, F-DW-SP1-002); nil ForwardFunc stays; interfaceSet unchanged. AC-002 respecified entirely to Q-SEAM seam contract (OnAccept lifecycle: NodeHandle, send channel, writer goroutine, cleanup; drop TestRunRouter_ForwardFunc_WiredToNodeSendMap; add TestNetingress_OnAccept_RegistersNodeHandle + TestRunRouter_NodeConnClose_CleansUpSendMap). AC-003 reshaped per Q-AC003 (Q-SINGLE-OBS makes "count 0→1" obsolete; observable = side-channel flag; single startup observer BEFORE connections); PROVISIONAL removed; AC-003 also gains Q-CTL-GUARD pin test (TestRouter_CtlFrame_ShortPayload_NoConnClose) + BC-2.01.008 PC-4 obligation. AC-004 postcondition 5 changed: VP-037.md partial-discharge note already landed (v1.4); lock stays false (NOT flip to true). AC-005 reshaped per Q-AC005: drainCoordHook + cfg.DrainTimeout=100ms replaces production-RegisterObserver path; PROVISIONAL note removed. FCL 9→10 rows (netingress.go new row 10); FCL row 1 + row 8 updated for Q2-AMENDED/BC-2.01.008 cross-ref. architecture_modules: removed internal/routing ForwardFunc-widening entry; added internal/netingress; fixed cmd/switchboard comment. bc_traces: added BC-2.01.008. inputDocuments: placement note pinned v1.1; BC-2.09.002 → v1.2; BC-2.01.004 → v1.4; BC-2.01.008 v1.0 added; VP-037 → v1.4. backlog_origin.notes updated. Tasks reworked (task 5 ForwardFunc replacement removed; netingress ServeConfig/NodeHandle/OnAccept task added; single startup observer task; VP-037 stays false task 12 explicit; ctl guard + pin test in task 5; ARCH-08 §6.5 same-commit task 11). Anchors Consumed: FO-RECV-FWD-001 row flipped to DEFERRED. Forward Obligations Consumed: FO-RECV-FWD-001 moved to Emitted (DEFERRED). Estimated test surface: ~6→~8. |
| 1.0 | 2026-07-11 | Elaborated from stub v0.2 consuming placement note v1.0. All 7 placement questions answered (Q1–Q7). ACs: AC-001 (DRAIN frame FrameTypeCtl + 4-byte payload; per-node send map; Q1 + Q2), AC-002 (FO-RECV-FWD-001 consumed — nil ForwardFunc replaced; Q2 binding), AC-003 (observer registration at connection-open; ARCH-08 lawfulness; FO-DRAIN-WIRE-001 forward-compat guard; Q3 — ACK mechanism PROVISIONAL), AC-004 (VP-037 via untagged TestE2E_RouterDrain_WireRoundTrip; Q4 binding), AC-005 (EC-003 production RegisterObserver path + drain.Signal recover() gap closure; Q5 binding). Design Constraints: 5 subsections with Q-citations. 2 PROVISIONAL rulings marked (ACK mechanism Q3.P1; test-file consolidation Q6). FCL: 9 rows per Q6 skeleton + doc-comment sweep obligations. Estimated test surface: ~6 net-new. FO-RECV-FWD-001 consumed; FO-DRAIN-WIRE-001 + FO-DRAIN-WIRE-002 emitted. Version: 1.0; status: ready; points: 5; acceptance_criteria_count: 5. |
| 0.2-backlog-stub | 2026-07-07 | Backlog stub at v0.2. Narrative, sketched ACs, non-goals, anchors. S-BL.PE-RECEIVE-LOOP added to depends_on (PO ruling F-P1-002 2026-07-07). Status: backlog. |
