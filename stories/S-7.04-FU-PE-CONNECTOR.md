---
artifact_id: S-7.04-FU-PE-CONNECTOR
document_type: story
level: ops
story_id: S-7.04-FU-PE-CONNECTOR
title: "Outbound TCP dial loop on PE graduation — upstream_routers connect-half and live-egress anchor"
status: ready
producer: story-writer
timestamp: 2026-07-08T00:00:00Z
version: "1.9"
phase: 2
epic: E-7
wave: backlog
priority: P2
scope_phase: PE
estimated_points: 8
bc_traces:
  - BC-2.09.003   # PC-9 upstream_routers APPLICATION connect-half (seam closed by S-7.04; dial loop ships here)
  - BC-2.09.001   # PC-2/PC-3 — upstream connections established; router becomes PE
  - BC-2.09.002   # PC-1 context — PE router drain migration requires live upstream connections
  - BC-2.02.008   # PC-3/EC-003 — split-horizon drop + E-FWD-001 event logged (AC-004 live-egress anchor)
  - BC-2.06.003   # PC-1 status enum {active, degraded, failed} — Failed-state event emission (AC-004 retransmit-driven path exhaustion)
vp_traces:
  - VP-037        # "drain within window" — co-activation with S-7.04-FU-DRAIN-WIRE; live egress required
  - VP-038        # "E→PE via config-only" — activation; live PE mode requires dial loop
subsystems: [deployment-operations, transport-layer]
architecture_modules:
  - cmd/switchboard
  - internal/config
  - internal/outerassembler
  - internal/upstreamdial   # NEW per Q4 — Connector, dial loop, keepalive ticker
  - internal/testenv        # testenv seam retirement per Q2/FO-2
tdd_mode: strict
cycle: v1.0.0-greenfield
depends_on:
  - S-7.04            # MERGED — upstreamRoutersFor seam, mode=PE emission, drain coordinator
  - S-7.04-FU-SIGHUP-RELOAD   # SIGHUP path must exist before PE mode can be graduated at runtime
  - S-BL.OA           # MERGED — outer assembler wire-frame primitive required for session bootstrap
  - S-BL.ARQ-TX       # MERGED — ARQ send-side retransmit path required for sustained send load
blocks:
  - S-7.04-FU-DRAIN-WIRE   # drain migration over SVTN only meaningful once upstream connections exist
inputDocuments:
  - '.factory/specs/behavioral-contracts/ss-09/BC-2.09.003.md'
  - '.factory/specs/behavioral-contracts/ss-09/BC-2.09.001.md'
  - '.factory/specs/behavioral-contracts/ss-09/BC-2.09.002.md'
  - '.factory/stories/S-7.04-DELIVERY.md'
  - '.factory/decisions/S-7.04-FU-PE-CONNECTOR-placement-note.md'
  - '.factory/stories/S-7.04-FU-SIGHUP-RELOAD-DELIVERY.md'
  - '.factory/specs/verification-properties/VP-037.md'
  - '.factory/specs/verification-properties/VP-038.md'
acceptance_criteria_count: 6
backlog_origin:
  source: S-7.04-DELIVERY
  deferred_from: S-7.04 PE-mode outbound connector + AC-006 connect-half
  drift_items_consumed:
    - S404-OBS-F          # E-FWD-001 rate-limit LATENT re-confirmation (live-egress anchor)
    - S404-LOW-1          # S-4.04 LOW findings: live-egress re-confirmation vehicle
    - S-7.04-PE-CONNECTOR-DEFERRED   # startup emission half shipped S-7.04; dial loop ships here
  notes: >
    S-7.04 AC-006 delivered the upstream_routers APPLICATION SEAM: `upstreamRoutersFor(cfg)`
    returns the resolved address list; `runRouter` emits `mode=PE upstream_routers=[...]` at
    startup. This is the *emission half*. What S-7.04 DELIVERY explicitly deferred is the
    *connect half*: the outbound TCP dial loop that actually establishes connections to the
    upstream router addresses returned by upstreamRoutersFor.

    The outbound dial loop requires: outer-header session bootstrap protocol (S-BL.OA delivered
    the wire-format primitive; session bootstrap over that wire is the connect-half work),
    peer-list maintenance (tracking connected vs. disconnected upstream routers), reconnect
    backoff for EC-001 (unreachable upstream) per BC-2.09.001 EC-001.

    LIVE-EGRESS ANCHOR: This story is the designated live-egress confirmation anchor for
    S404-OBS-F and S404-LOW-1. Four accumulated drift anchors land here:
    - S404-OBS-F: E-FWD-001 rate-limit is LATENT; the receive-side split-horizon-blocked log in
      routing is never exercised by the arqsend integration tests (arqsend is send-side; no path
      exhaustion). The live-egress story (sustained retransmit load via the outbound PE send loop)
      is the re-confirmation vehicle for E-FWD-001.
    - S404-LOW-1: 3 LOW + NITPICK (SEC-001 CRC32 accepted) from S-4.04 original review.
      Adjudicated at S-BL.ARQ-TX (PR #98): NOT triggered. Re-anchored here for live-daemon
      egress under sustained retransmit load.
    - ARQ-TX send-loop: the arqsend Retransmitter dispatches via netingress+routing in unit tests;
      the live-PE send loop exercises the same path end-to-end.
    - Retransmit-driven Failed trigger and Failed-state event emission: BC-2.06.003 (PC-1 status
      enum {active, degraded, failed}; Failed = liveness failure on previously-alive path) +
      BC-2.02.008 (PC-3/EC-003 split-horizon drop → E-FWD-001 logged); observable only when
      actual upstream connections exist.
---

# S-7.04-FU-PE-CONNECTOR: Outbound TCP Dial Loop on PE Graduation

## Narrative

- **As an** operator who has graduated a router from E to PE mode (via SIGHUP reload)
- **I want** the router to actually dial and maintain TCP connections to the configured
  upstream routers
- **So that** node sessions on this PE router have access to alternate path options
  (multi-hop routing) per BC-2.09.001 PC-2/PC-3

## Context

S-7.04 delivered the *emission seam* for PE graduation: `upstreamRoutersFor(cfg)` and
`mode=PE upstream_routers=[...]` startup emission. The `keepaliveIntervalFor` helper is
in place (normative-fence test `TestKeepaliveIntervalNotSweepDeadline` guards it against
conflation with `sweepDeadline`). What is explicitly deferred is the TCP dial loop that
consumes `upstreamRoutersFor(cfg)` and establishes actual upstream router connections.

The outer-header wire-frame primitive required for session bootstrap was delivered by
S-BL.OA (merged PR #96, `internal/outerassembler`). The ARQ retransmit path was
delivered by S-BL.ARQ-TX (merged PR #98, `internal/arqsend`). S-7.04-FU-SIGHUP-RELOAD
delivered the SIGHUP reload path (merged; HEAD `bacc59b`), including the transitional
testenv seam (`SetSighupCh`/`SendReloadSignal`) whose retirement is an explicit obligation
of this story (FO-2 from that DELIVERY).

This story also carries the **live-egress anchor** obligation. E-FWD-001 (rate-limit,
receive-side split-horizon log) and the S-4.04 LOW findings (3 LOW + SEC-001) have been
re-anchored here since the arqsend integration tests could not trigger them (send-side
tests, no path exhaustion). The PE dial loop under sustained retransmit is the first
vehicle that exercises the full send+forward path with actual upstream connections.

## Anchors Consumed (Four Live-Egress Anchors)

| Anchor | Verbatim ID | Source | Disposition |
|--------|-------------|--------|-------------|
| BC-2.09.003 PC-9 upstream_routers connect-half | BC-2.09.003 PC-9 | S-7.04 DELIVERY; emission half shipped | DISCHARGED — dial loop + bootstrapped connection shipped |
| S404-OBS-F — E-FWD-001 rate-limit re-confirmation | S404-OBS-F | STATE.md row; re-anchored to live-egress story | RE-ANCHORED → S-BL.PE-RECEIVE-LOOP (partial-discharge F-P1-002; exhaustion case requires PE receive/forward loop) |
| S404-LOW-1 — live-egress re-confirmation | S404-LOW-1 | STATE.md row; re-anchored to live-egress story | RE-ANCHORED → S-BL.PE-RECEIVE-LOOP (same partial-discharge; full send+forward path not yet exercised) |
| VP-037 + VP-038 activation | VP-037, VP-038 | deferred from S-7.04; PE dial loop enables live drain + mode tests | VP-038 DISCHARGED; VP-037 partial-discharge (blocked on S-7.04-FU-DRAIN-WIRE) |

---

## Design Constraints

### Dial-Loop Architecture and Package Placement (Q2, Q4)

**Binding (per placement note Q2 and Q4).**

The dial loop lives in a new `internal/upstreamdial` package as a `Connector` type.
`runRouter` constructs `Connector` at startup and passes the initial address list.
Reload passes a new address list via a channel. This follows the `internal/drain`
encapsulation precedent: `drainCoord` extracted drain coordination out of `runRouter`
for the same testability reason; `Connector` does the same for the dial loop.

**Package placement:** `internal/upstreamdial` at DAG position 19 (effectful, imports
`{halfchannel, outerassembler}` (corrected from `{frame, outerassembler}` per F-P1-001/ARCH-08 v2.7; story prose swept at v1.7 per F-P3-002 extension)). Current positions 19–22 renumber to 20–23 in the same commit.

```
| 19 | internal/upstreamdial | {halfchannel, outerassembler} (corrected from {frame, outerassembler} per F-P1-001/ARCH-08 v2.7; story prose swept at v1.7 per F-P3-002 extension) | effectful (network I/O) | Wave 7 (S-7.04-FU-PE-CONNECTOR) |
```

**ARCH-08 §6.4 registration is required before the first commit.** The story implementer
must update ARCH-08 §6.5 with the new row AND renumber positions 19–22 to 20–23.

**Forbidden edges:**

- `internal/upstreamdial` → `internal/drain` (drain is effectful-goroutine lifecycle; connector does not need it)
- `internal/upstreamdial` → `internal/routing` (routing is a boundary; connector does not participate in frame forwarding)
- `internal/upstreamdial` → `internal/testenv` (test-helper composition root; never imported by production code)

**Handle interface (proposed):**

```go
// In internal/upstreamdial:
type Handle interface {
    // ReloadAddrs reconciles the running set to the new address list.
    // Set-equal semantics (see Concurrency Contract section). Called from runRouter's select loop.
    ReloadAddrs(addrs []string)
    // Mode returns ModeE when no upstreams are connected, ModePE when >= 1.
    Mode() ConnMode
    // Stop cancels all dial goroutines and waits for them to exit.
    Stop()
}
```

Note on mode enum: `internal/upstreamdial` MUST define its own `ConnMode` type
(`ModeE`/`ModePE`) since `internal/testenv` is at DAG position 22/23 (test-helper
composition root — not importable by production code). `testenv` maps from
`upstreamdial.ConnMode` to `testenv.RouterMode` in the wiring glue.

### Concurrency Contract: Channel-Passed Snapshot (Q3, FO-4)

**Binding (per placement note Q3).**

`upstreamRouters` in the `runRouter` goroutine remains a local variable (consistent with
`mgmt_wire.go:483`). The `Connector` receives address updates via a **buffered
`chan []string`** (capacity 1). `runRouter`'s SIGHUP case, after computing the new
address list, sends a `[]string` snapshot on the channel using a fully non-blocking
send (F-P5-001: fast-path send, then non-blocking drain, then non-blocking resend):

```go
select { case c.addrsCh <- snap: return; default: }   // fast path
select { case <-c.addrsCh: default: }                  // drain (non-blocking)
select { case c.addrsCh <- snap: default: }            // resend (non-blocking)
```

The old Q3-prescribed pattern (`select { case ch <- v; default: _ = <-ch; ch <- v }`)
contained a BLOCKING inner receive (`_ = <-ch`) that could deadlock the caller: the
reconcile goroutine can drain `addrsCh` in the window between the failed fast-send and
the blocking drain, leaving the channel empty and wedging `runRouter`'s select loop
permanently (ctx.Done() and all future SIGHUPs unreachable). Corrected per placement
note Q3 v1.4 erratum (F-P5-001). The `Connector`'s reconcile goroutine reads from this
channel.

The `Connector` owns all reconnect state internally: the per-address dial state, the
per-address reconnect backoff timer, and the connected-count `atomic.Int32` that drives
`Mode()`. All state is accessed only from the `Connector`'s reconcile/reconnect
goroutine(s). The only cross-goroutine boundaries are:

- address update channel (`runRouter` → `Connector`, writes only)
- `Mode()` read (any goroutine — safe as `atomic.Load`, no mutex needed per go.md rule 12)

`runRouter` never reads the address list from the `Connector`; it maintains its own
local copy for emission purposes (BC-2.09.001 PC-1 mode line).

This design is consistent with the `sighupCh <-chan os.Signal` precedent from
S-7.04-FU-SIGHUP-RELOAD: signals flow in one direction through channels, state is not
shared.

### Session Bootstrap: "Connection Established" Definition (Q6)

**Binding (per placement note Q6 verbatim).**

BC-2.09.001 PC-2 ("router establishes connections to the configured upstream routers")
is satisfied when ALL THREE of the following succeed:

1. `net.Dial("tcp", addr)` returns `(conn, nil)`
2. `outerassembler.Assemble(env, cf)` returns `(wire, nil)` where:
   - `env.SrcAddr` — this router's node address (derived from its ephemeral Ed25519
     public key and SVTN ID per `frame.DeriveNodeAddress`)
   - `env.DstAddr` — the upstream router's expected node address (from config or
     derived at dial time)
   - `env.SVTNID` — the SVTN this PE router is serving
   - `env.FrameAuthKey` — derived via `hmac.DeriveKey` using the shared session key
   - `cf` — minimal `halfchannel.ChannelFrame` with `FrameTypePEConnect` payload
     (new constant to be defined, e.g., `frame.FrameTypePEConnect`)
3. `conn.Write(wire)` returns `(n, nil)` with `n == len(wire)`

A TCP connection that is not yet bootstrapped is NOT a "connection established" event for
BC purposes. The `connected-count atomic.Int32` increments only on step 3 success.

**Admission protocol:** The full `admission.GenerateChallenge`/`admission.AdmitNode`
challenge-response handshake is DEFERRED (class: not-core) to a follow-on story if not feasible within
the 8-point scope. The Q6 three-step "connection established" definition (TCP dial + Assemble + Write all nil) is the load-bearing contract; full admission challenge-response is a security enhancement tracked in Non-Goals. The story-writer assesses feasibility at implementation time. If
deferred, the dial bootstrap sends an unauthenticated PE-CONNECT frame and a
`Non-Goals` note records the deferral; if in-scope, the full `internal/admission` flow
is wired at dial time. Either way, the three-step definition above applies.

**Shipped deferral (implementation-era note):** The `Connector` is constructed with
zero-valued `outerassembler.Envelope` fields (`SrcAddr`/`DstAddr`/`SVTNID`/`FrameAuthKey`).
Full node-identity derivation (Ed25519 key material, `frame.DeriveNodeAddress`, HMAC key
derivation) is deferred as not-core to the session-bootstrap follow-on story. The
three-step establishment is satisfied with the zero envelope; the deferral is documented
in a comment in `mgmt_wire.go`.

### Construction-Time Testenv Seam Retirement (Q2, FO-2)

**Binding (per placement note Q2, consuming FO-2 from S-7.04-FU-SIGHUP-RELOAD DELIVERY).**

The transitional `SetSighupCh` setter (`testenv.go:302`) and 1-arg `SendReloadSignal`
(`testenv.go:326`) are explicitly deferred artifacts from S-7.04-FU-SIGHUP-RELOAD.
Their doc comments state: *"Construction-time wiring and real state tracking land with
the PE-CONNECTOR-era testenv integration (S-7.04-FU-PE-CONNECTOR)."*

This story fulfills that obligation:

1. `internal/upstreamdial.Connector` exposes a `Handle` at construction time (see
   interface above). `runRouter` constructs the `Connector` before the run loop and
   passes the handle into `testenv.RouterHandle` at construction.

2. `testenv.RouterHandle` is extended at construction: `StartRouter` passes the
   connector handle so that `RouterHandle.Mode()` delegates to the live
   `connector.Mode()` rather than the stub `r.mode` field.

3. `SetSighupCh` and `SendReloadSignal` MUST be retired (removed or replaced by the
   construction-time equivalent). If retirement creates build breaks in existing
   tests (e.g., `router_sighup_test.go`), those tests MUST be migrated to the new
   seam — they are in scope for this story.

4. `RouterHandle.Mode()` becomes a real accessor reflecting actual connection state.
   The VP-038 harness (`eRouter.Mode() != testenv.ModePE`) now exercises this
   live accessor.

---

## Acceptance Criteria

### AC-001 — PE graduation: outbound dial, session bootstrap, and set-equal reconciliation

**BC Anchors:** BC-2.09.001 PC-2/PC-3 (upstream connections established; router becomes
PE); BC-2.09.001 EC-002 (reorder MUST NOT trigger redial); per placement note Q1 and Q6.

**Precondition:** `runRouter` is executing with a PE config (non-empty
`upstreamRoutersFor(cfg)`). A `Connector` has been constructed with the initial address
list. A cooperative upstream router fixture (testenv loopback) is accepting TCP
connections at each configured address.

**Postconditions:**

1. For each address in `upstreamRoutersFor(cfg)`, the Connector dials a TCP connection:
   `net.Dial("tcp", addr)` returns `(conn, nil)`.
2. The Connector calls `outerassembler.Assemble(env, cf)` with the bootstrap envelope
   (Q6 definition: `env.SrcAddr` = this router's node address, `env.DstAddr` = upstream
   address, `env.SVTNID` = this router's SVTN, `cf` carries `FrameTypePEConnect`).
3. `conn.Write(wire)` returns `(n, nil)` with `n == len(wire)`. All three of (1), (2),
   (3) succeed before the connected-count increments by 1. A TCP connection that has not
   completed the bootstrap write is NOT a "connection established" event for BC purposes
   (per placement note Q6 verbatim definition).
4. `connector.Mode()` returns `ModePE` when connected count is ≥ 1.
5. On SIGHUP reload that changes the upstream address set to a different ordering of the
   same addresses (set-equal content): no existing connections are torn down and no new
   dials are initiated. The reconciler uses `upstreamRoutersAsSet(addrs []string) map[string]struct{}`
   in `router_config.go` to compute `added = loaded \ running` and `removed = running \
   loaded` as set differences (per placement note Q1 ruling: "a reorder MUST NOT
   trigger redial or teardown").
6. On SIGHUP reload that adds new addresses: Connector dials the added addresses.
   On reload that removes addresses: Connector tears down the removed connections.
7. `runRouter` has not returned; the daemon continues executing the run loop throughout.

**Set-equal vs. order-sensitive note (per placement note Q1):** The emission diff in
`runRouter`'s SIGHUP case (`equalStringSlices` at `router_config.go:81`) keeps
order-sensitive semantics unchanged — a spurious re-emission on reorder is acceptable
noise. The connection reconciliation diff MUST use set-equal semantics. The two
comparators may lawfully differ; the spec does not require emission and connection
triggers to share semantics.

**Test names:**

- `TestConnector_DialSuccess_ModePE` (unit, `internal/upstreamdial/connector_test.go`)
- `TestRunRouter_PE_DialAndConnect_UpstreamReachable` (integration, `cmd/switchboard/router_pe_connector_test.go`)
- `TestConnector_ReorderReuse_NoTeardown` (unit, `internal/upstreamdial/connector_test.go`)
- `TestRunRouter_PE_SetEqualReconciliation_NoTeardownOnReorder` (integration, `cmd/switchboard/router_pe_connector_test.go`)

**Test level:** unit (Connector) + integration (runRouter end-to-end)
**Test files:** `internal/upstreamdial/connector_test.go`, `cmd/switchboard/router_pe_connector_test.go`

Outline (integration):

```go
// testenv.NewWithRouters(t, ctx, 2); router[0] PE with router[1] as upstream
// confirm connector.Mode() == ModePE after startup
// send reload signal with same addrs in reversed order
// confirm no teardown observable: Mode() still ModePE, existing TCP conn still alive
// send reload signal adding a third addr (new fixture)
// confirm Mode() still ModePE; third connection eventually dials
```

---

### AC-002 — Unreachable upstream: EC-001 graceful handling with exponential backoff

**BC Anchors:** BC-2.09.001 EC-001 (partial PE mode; log verbatim; retry in background);
BC-2.09.001 EC-004 (all upstreams lost → ModeE fallback); per placement note Q5 (exact
backoff parameters).

**Precondition:** The Connector is started with at least one upstream address that is
not accepting TCP connections (port closed or unreachable). Optionally: one address is
reachable (to exercise partial-PE).

**Postconditions (per placement note Q5 — exact parameters are normative):**

1. The dial attempt fails with a net error. The Connector emits exactly:
   ```
   upstream router <addr> unreachable
   ```
   to `w` (the `io.Writer` passed to `runRouter`) on every failed dial attempt. The
   `addr` is the string from `upstreamRoutersFor`. The implementation MAY append
   `: <net-error>` for operator diagnostics; that suffix is NOT asserted by ACs.
   (Log contract verbatim from BC-2.09.001 EC-001.)
2. The connected-count atomic remains at 0 for the unreachable address. `runRouter`
   continues its run loop without returning; existing node sessions are unaffected.
3. The Connector retries in background with exponential backoff (per placement note
   Q5/Q7 as reconciled by F-P2-002 ruling (v1.3)):
   - **First retry delay:** `keepaliveInterval` (floored at `BackoffBase`=500ms);
     `BackoffBase` is a floor constant only, NOT the operative schedule base
   - **Exponential growth:** multiplicative from `keepaliveInterval` (floored at
     `BackoffBase`=500ms) on each successive failure
   - **Cap:** 30s
   - **Jitter:** ±25% uniform (breaks synchronization across multiple PE routers)
   - **Reset on success:** backoff resets to `keepaliveInterval` (floored at
     `BackoffBase`=500ms) on first successful dial
4. If at least one other upstream IS connected (`connected count ≥ 1`), `connector.Mode()`
   returns `ModePE` (partial-PE semantics: router is in PE mode once ≥1 upstream is
   connected). No additional `mode=partial-PE` mode line is emitted; the BC does not
   define that observable.
5. If ALL upstreams are unreachable and the connected count drops from ≥1 to zero (the
   final upstream connection is lost due to upstream-LOSS, not self-initiated teardown),
   the Connector emits to `w`:
   ```
   mode=E (no upstream_routers configured)
   ```
   (EC-004 fallback observable from BC-2.09.001 EC-004; emission fires on upstream-LOSS
   drop-to-zero only; self-initiated teardown (Stop) does not emit (F-P4-001, per
   BC-2.09.001 EC-004 trigger semantics).) `connector.Mode()` returns `ModeE`.

**Test names:**

- `TestConnector_DialFailure_EC001Log` (unit, `internal/upstreamdial/connector_test.go`)
- `TestConnector_BackoffConstants` (unit, `internal/upstreamdial/connector_test.go` — floor guard only: asserts the three Q5 constant values BackoffBase=500ms / BackoffCap=30s / BackoffJitterFraction=0.25; renamed from TestConnector_BackoffParameters per F-P2-002 split)
- `TestOperativeBase_TracksKeepalive` (unit, `internal/upstreamdial/connector_test.go` — pure-func extraction: 8 sub-tests covering above-floor, exact-floor, below-floor inputs; mutation-detectable: floor sub-cases fail if either dialLoop wiring site reverts to raw keepaliveInterval; per F-P2-002)
- `TestConnector_OperativeBackoffBase_TracksKeepalive` (unit, `internal/upstreamdial/connector_test.go` — coarse timing: 3 sub-tests with disjoint bands [1s], [2s], [100ms below-floor]; proves dial-loop wiring calls operativeBase; per F-P2-002)
- `TestConnector_BackoffParameters` (unit, `internal/upstreamdial/connector_test.go` — rewritten as reset-on-success wiring test: grows backoff to ~4s via 3 failures, connects (reset), drops connection, measures gap post-reset expects ~1000ms operative base (window [700ms, 1300ms]); BackoffBase mutant band [375ms, 625ms] disjoint below loWindow=700ms; pollForMode budget 15s; reset-on-success site now mutation-pinned (P3 F-P3-001); per F-P2-002)
- `TestConnector_AllUpstreamsUnreachable_ModeE` (unit, `internal/upstreamdial/connector_test.go`)
- `TestConnector_NoEC004OnGracefulStop` (unit, `internal/upstreamdial/connector_test.go` — **SHIPPED F-P4-001**: graceful-Stop polarity guard regression; connects to live fixture, ModePE, Stop() with listener healthy, asserts NO mode=E emission; stash-verified failing pre-fix)
- `TestRunRouter_PE_UnreachableUpstream_PartialPE` (integration, `cmd/switchboard/router_pe_connector_test.go` — one reachable + one unreachable upstream; Mode()=ModePE)

**Test level:** unit (Connector backoff + log) + integration (partial-PE end-to-end)
**Test files:** `internal/upstreamdial/connector_test.go`, `cmd/switchboard/router_pe_connector_test.go`

Outline (unit — EC-001 log):

```go
// Construct Connector with one address backed by a net.Listener that refuses connections
// Start Connector; poll for "upstream router <addr> unreachable" in writer output
// Assert Mode() == ModeE
// Open the listener; wait for successful dial; assert Mode() == ModePE; assert backoff reset
```

---

### AC-003 — Keepalive ticker lives in Connector (BC-2.09.003 PC-8)

**BC Anchors:** BC-2.09.003 PC-8 (`keepalive_interval` drives connection-level keepalive
and reconnect scheduling, NOT console eviction); per placement note Q7.

**Precondition:** `runRouter` computes `keepaliveIntervalFor(cfg)` at
`mgmt_wire.go:482` and passes it to `Connector` at construction (alongside the initial
address list and the update channel). An upstream fixture is accepting connections.

**Postconditions (per placement note Q7):**

1. The Connector owns a keepalive ticker constructed with `keepaliveIntervalFor(cfg)`
   as its base cadence. The ticker lives INSIDE `internal/upstreamdial.Connector`;
   it is NOT constructed in `runRouter`.
2. The ticker drives per-connection health probing (sends keepalive probe frames on
   established upstream connections to detect silently dead TCP connections that do not
   produce a read error).
3. The ticker drives reconnect scheduling: after a dial failure, the first retry uses
   the base `keepaliveInterval` before applying exponential growth (Q5 backoff from
   AC-002).
4. The ticker MUST NOT be wired into `sweepDeadline` (console eviction —
   `access.go`; semantically unrelated). The normative fence test
   `TestKeepaliveIntervalNotSweepDeadline` (in `router_config_test.go`) MUST remain
   unmodified and green across this story's changes. Its assertions MUST NOT be
   altered.

**Test names:**

- `TestKeepaliveIntervalNotSweepDeadline` (**existing normative fence** — must remain green; NOT modified)
- `TestConnector_KeepaliveTickerDrivesHealthProbe` (unit, `internal/upstreamdial/connector_test.go`)
- `TestRunRouter_PE_KeepalivePassedToConnector` (integration, `cmd/switchboard/router_pe_connector_test.go` — asserts Connector receives the `keepaliveIntervalFor` value, not a hardcoded constant)

**Test level:** unit (Connector ticker encapsulation) + normative fence (existing)
**Test files:** `internal/upstreamdial/connector_test.go`, `cmd/switchboard/router_pe_connector_test.go`

Outline (integration):

```go
// Write a PE config with keepalive_interval: 200ms to t.TempDir()
// Start runRouter; confirm connector receives 200ms ticker (not 1s default or sweepDeadline 60s)
// Confirm keepaliveIntervalFor(cfg) returns 200ms; sweepDeadline unaffected (TestKeepaliveIntervalNotSweepDeadline guard already enforces the fence)
```

---

### AC-004 — E-FWD-001 re-confirmation under sustained ARQ retransmit load

**BC Anchors:** BC-2.02.008 (PC-3/EC-003: split-horizon drop + E-FWD-001 event logged); BC-2.06.003 (PC-1 status enum {active, degraded, failed}: Failed-state event emission on retransmit-driven path exhaustion); S404-OBS-F (E-FWD-001 rate-limit re-confirmation); per placement note Q8 (loopback upstream fixture, arqsend sustained load).

**Partial discharge (class: unmet-deps — F-P1-002).** Two halves:

- **Discharged here:** Live PE upstream connection established (connected-count ≥ 1 after startup); no spurious E-FWD-001 under normal load (postcondition 2). Proven by `TestRunRouter_PE_EFWD001ReconfirmationUnderLoad`.
- **Re-anchored to `S-BL.PE-RECEIVE-LOOP`:** E-FWD-001 fires under sustained path-exhaustion load (postcondition 1, original). Unmet because E-FWD-001 is emitted only from `routing.FrameArrivalHandler.OnFrameArrival`; `runRouter`'s ingress path (`netingress.Serve → routing.RouteFrame`) never calls `OnFrameArrival`. The `Connector` in this story only dials, bootstraps, and keepalives — it has no receive/forward loop over PE connections. `arqsend.Retransmitter` is also not wired to `runRouter` in this story. The story that ships the PE-connection receive/forward loop (`S-BL.PE-RECEIVE-LOOP`) owns the exhaustion discharge. **S404-OBS-F and S404-LOW-1 re-anchor to `S-BL.PE-RECEIVE-LOOP`** (they exist to be re-confirmed at live egress with full send+forward path traversal; that path does not exist until the receive loop ships). S-BL.PE-RECEIVE-LOOP is the direct prerequisite for S-7.04-FU-DRAIN-WIRE (which needs an operational receive loop to broadcast DRAIN frames over).

**Precondition (discharged half):** `runRouter` is executing with a PE config (non-empty `upstreamRoutersFor(cfg)`). An upstream fixture is accepting TCP connections. A `Connector` has been constructed with the initial address list.

**Postconditions:**

1. ~~Under sustained ARQ retransmit load that exhausts the routing table's path count, the E-FWD-001 log line fires in `router[0]`'s writer output.~~ **RE-ANCHORED to S-BL.PE-RECEIVE-LOOP** (unmet-deps: receive/forward loop over PE connections not in this story's scope).
2. Under single-path happy-path / no-load conditions, E-FWD-001 does NOT fire in the router's writer output. **DISCHARGED.**
3. ~~The live PE send loop exercises the full send+forward path end-to-end. This constitutes the re-confirmation anchor for S404-OBS-F and S404-LOW-1.~~ **RE-ANCHORED to S-BL.PE-RECEIVE-LOOP.**

**Test names:**

- `TestRunRouter_PE_EFWD001ReconfirmationUnderLoad` (integration, `cmd/switchboard/router_pe_connector_test.go` — discharges postcondition 2 only; inline comment documents unmet-deps analysis for postcondition 1)

**Test level:** integration
**Test file:** `cmd/switchboard/router_pe_connector_test.go`

---

### AC-005 — VP-037/VP-038 verification_lock discharge

**BC Anchors:** VP-037 (drain-within-window, BC-2.09.002 postcondition); VP-038
(E→PE via config-only, BC-2.09.001 postcondition); per placement note Q8.

#### VP-037 (drain-within-window)

**Precondition:** `testenv.NewWithRouters(t, ctx, 2)` creates a two-router stack.
`router[0]` is a PE router with live upstream connection to `router[1]` (established by
this story's dial loop). `env.SendDrainSignal(t, 0)` is available (already exists at
`testenv.go:956` per HEAD `950285c`; the blocker noted in VP-037 v1.1 is CLEARED by
S-BL.TESTENV merge PR #110).

**Postconditions:**

1. `TestE2E_RouterDrain_NodesMigrateWithin2s` (VP-037 harness from VP-037.md): Session
   traffic resumes on the alternate router within 2 seconds after `SendDrainSignal(t, 0)`.
2. VP-037 `verification_lock` is flipped to `true` in the commit that makes this test pass.

**Partial discharge note (per placement note Q8):** If the full two-router drain-and-
migrate scenario cannot be completed within this story's 8-point scope because it depends
on the DRAIN broadcast wire protocol owned by S-7.04-FU-DRAIN-WIRE, VP-037 remains
`verification_lock: false`. In that case the story's Definition of Done states:
*"VP-037 unblocked but not yet discharged — blocked on S-7.04-FU-DRAIN-WIRE for the
DRAIN broadcast half."* The story delivers the live-egress infrastructure that makes
VP-037 achievable, not the complete proof. The implementer must choose at implementation
time and update this story's body accordingly.

#### VP-038 (E→PE via config-only)

**Precondition:** `TestE2E_EtoPEGraduationByConfigChange` (shipped in `router_pe_connector_test.go`)
calls `eRouter.Mode()` after `eRouter.Restart(...)`. `Restart()` creates a live `Connector` for
non-empty `UpstreamRouters` and polls until `connector.Mode() == ModePE` (3s timeout), via the
construction-time `SetConnector` seam from AC-006. The former stub `r.mode = ModePE` assignment
has been replaced.

**Postconditions:**

1. `TestE2E_EtoPEGraduationByConfigChange` (VP-038 harness from VP-038.md): After
   `eRouter.Restart(t, testenv.RouterConfig{UpstreamRouters: []string{peAddr}})`,
   `eRouter.Mode()` returns `testenv.ModePE` (reflects actual connection state, not
   the stub enum). `eRouter.SVTNID()` is unchanged.
2. VP-038 `verification_lock` is flipped to `true` in this story's merge commit.

**Test names:**

- `TestE2E_RouterDrain_NodesMigrateWithin2s` (VP-037 proof harness — see VP-037.md skeleton)
- `TestE2E_EtoPEGraduationByConfigChange` (VP-038 proof harness — see VP-038.md skeleton)

**Test level:** integration (both tests in `cmd/switchboard/router_pe_connector_test.go`; VP-037 skipped with partial-discharge note per AC-005)
**Test files:** `cmd/switchboard/router_pe_connector_test.go` (both VP-037 and VP-038 harnesses)

---

### AC-006 — Construction-time testenv seam retirement (Q2, FO-2)

**BC Anchors:** S-7.04-FU-SIGHUP-RELOAD DELIVERY FO-2 (construction-time wiring and
real state tracking deferred to PE-CONNECTOR-era testenv integration); per placement
note Q2.

**Precondition:** `internal/testenv/testenv.go` at HEAD `950285c` contains the
transitional `SetSighupCh` setter (`:302`) and 1-arg `SendReloadSignal` (`:326`)
post-hoc pattern. `RouterHandle.Mode()` reads from the stub `r.mode` field rather than
live connector state.

**Postconditions:**

1. `testenv.StartRouter` passes the `upstreamdial.Handle` into `RouterHandle` at
   construction time. No post-hoc setter pattern (`SetSighupCh`) is needed after
   construction.
2. `SetSighupCh` is retired from `testenv.go` (removed or replaced with the
   construction-time equivalent).
3. 1-arg `SendReloadSignal(t testing.TB)` is retired from `testenv.go` (removed entirely
   with the `sighupCh` field and `os`/`syscall` imports). `TestRunRouter_VP038_EtoPEViaConfigOnly`
   in `router_sighup_test.go` is migrated from the `SetSighupCh`/`SendReloadSignal` pattern
   to driving `rawSighupCh <- syscall.SIGHUP` directly — the same inline pattern all other
   9 sighup tests already used (those 9 required no migration; they were already seam-free).
4. `RouterHandle.Mode()` returns `testenv.ModeE` when `connector.Mode()` == `ModeE`
   (no upstreams connected); returns `testenv.ModePE` when `connector.Mode()` ==
   `ModePE` (≥1 upstream connected). This is the live accessor the VP-038 harness calls.
5. Existing tests that called `SetSighupCh`/`SendReloadSignal` continue to pass
   (migrated to the new seam within this story).

**Test names:**

- `TestRunRouter_SIGHUPReload_EtoPE` (**existing, migrated** — still passes after seam retirement)
- `TestRunRouter_VP038_EtoPEViaConfigOnly` (**existing, migrated** — `SetSighupCh`/`SendReloadSignal` seam removed; now drives `rawSighupCh <- syscall.SIGHUP` directly; emission scan (`mode=PE` line) remains the authoritative observable since this test starts `runRouter` externally and `RouterHandle.Mode()` is disconnected from the live goroutine)
- `TestRunRouter_PE_RouterHandleModeReflectsLiveState` (new integration test, `cmd/switchboard/router_pe_connector_test.go` — asserts `RouterHandle.Mode()` tracks the live connector state across connect/disconnect transitions)
- `TestConnector_Stop_Idempotent` (**SHIPPED F-P2-001**, `internal/upstreamdial/connector_test.go` — Stop() idempotency via sync.Once; two sequential + one concurrent Stop, no panic, all return)
- `TestRouterHandle_Restart_TwicePE` (**SHIPPED F-P2-001**, `internal/testenv/testenv_test.go` — exact adversary reproduction shape: StartRouter, Restart×2, t.Cleanup fires second Stop on conn1; no panic; proves idempotency in the Restart path that registers t.Cleanup alongside explicit Stop)

**Test level:** integration
**Test files:** `cmd/switchboard/router_sighup_test.go` (migrated), `cmd/switchboard/router_pe_connector_test.go` (new)

---

## Non-Goals

- Does not implement DRAIN message broadcast to connected nodes. That is `S-7.04-FU-DRAIN-WIRE`.
- Does not implement SIGHUP signal handling. That is `S-7.04-FU-SIGHUP-RELOAD` (MERGED).
- Does not implement RESYNC control-frame protocol for session re-establishment after
  migration. That is `S-BL.RESYNC-FRAME`. The Connector delivers the raw TCP `net.Conn`
  as an upstream channel; RESYNC is a higher-level session protocol that uses that channel.
- Does not register `drainCoord` observers on upstream connections. The `drainCoordHook`
  test seam in `router_config.go:76` remains unchanged. Drain observer registration
  belongs to `S-7.04-FU-DRAIN-WIRE`.
- Does not implement the full `internal/admission` challenge-response handshake at dial
  time unless the implementer assesses it as feasible within the 8-point scope (see
  Session Bootstrap design constraint above).

---

## Files-Changed List (FCL)

| # | File | Change | BC / VP Anchor |
|---|------|--------|----------------|
| 1 | `internal/upstreamdial/connector.go` (NEW) | `Connector` type; `Handle` interface with `ReloadAddrs([]string)`, `Mode() ConnMode`, `Stop()`; `ConnMode` enum (`ModeE`/`ModePE`); dial loop with `outerassembler.Assemble` bootstrap on success; reconnect backoff (schedule-base=`keepaliveInterval` floored at `BackoffBase`=500ms, cap=30s, jitter ±25%; `BackoffBase` is floor constant only per F-P2-002 ruling — not the operative schedule base); `atomic.Int32` connected-count; set-equal reconciliation inline in `Connector.reconcile`. **Post-P1 (OBS-2/F-P1-007):** `reconcileLoop` empty keepalive-ticker case removed — that case had no body and drove nothing; per-address keepalive is owned by `dialLoop`. **Post-P2 (F-P2-001):** `stopOnce sync.Once` field added; `Stop()` wraps `close(stopCh)` in `stopOnce.Do` — idempotent, all callers still block on `doneCh`. **Post-P2 (F-P2-002):** `operativeBase(keepaliveInterval time.Duration) time.Duration` pure function extracted; floors below-`BackoffBase` inputs at 500ms; wired at both dial-loop sites: initial backoff assignment (`:269`) and reset-on-success (`:337`). **Post-P4 (F-P4-001):** EC-004 drop-to-zero emission path at `:346-350` guarded with `&& ctx.Err() == nil` — graceful `Stop()` of a connected PE router no longer emits the spurious "mode=E (no upstream_routers configured)" line; genuine upstream-loss emission unchanged. Guard is symmetric with the existing EC-001 guard (`:284-287`). **Post-P5 (F-P5-001):** `ReloadAddrs` rewritten from single-select drop-oldest to fast-path send + non-blocking drain + non-blocking resend; old Q3-prescribed blocking inner receive removed; `TestConnector_ReloadAddrs_StormNoDeadlock` regression added (200k-iteration storm, 10s watchdog, 0.10s green). | BC-2.09.001 PC-2/PC-3, EC-001, EC-004; Q1, Q2, Q3, Q5, Q6 |
| 2 | `internal/upstreamdial/connector_test.go` (NEW) | Unit tests: `TestConnector_DialSuccess_ModePE`, `TestConnector_DialFailure_EC001Log`, `TestConnector_ReorderReuse_NoTeardown`, `TestConnector_BackoffParameters`, `TestConnector_AllUpstreamsUnreachable_ModeE`, `TestConnector_KeepaliveTickerDrivesHealthProbe` | BC-2.09.001 PC-2/EC-001; Q1, Q5, Q7 |
| 3 | `cmd/switchboard/mgmt_wire.go` | `runRouter`: construct `Connector` at startup with initial address list, `keepaliveInterval`, and buffered `chan []string`; SIGHUP case calls `connector.ReloadAddrs(newUpstreams)` (replacing direct `upstreamRouters` local-variable management for connection state); `connector.Stop()` in cleanup path. **Post-P1 (OBS-1/F-P1-007):** `peConnectorHook` var removed — was a Red Gate stub never wired; `runRouter` calls `upstreamdial.New` directly. | BC-2.09.001 PC-2/PC-3; BC-2.09.003 PC-8; Q2, Q3, Q7 |
| 4 | `cmd/switchboard/router_config.go` | ~~Add `upstreamRoutersAsSet(addrs []string) map[string]struct{}` helper~~ **DELETED (F-P1-007):** production-dead; set-equal semantics implemented inline in `Connector.reconcile`. Function removed from `router_config.go`. | BC-2.09.001 EC-002; Q1 |
| 5 | `internal/testenv/testenv.go` | Retire `SetSighupCh` and 1-arg `SendReloadSignal` entirely (methods and `sighupCh` field removed; `os`/`syscall` imports gone); add `SetConnector(upstreamdial.Handle)` construction-time seam; wire `RouterHandle.Mode()` to delegate to live `connector.Mode()` when connector non-nil (maps `upstreamdial.ConnMode` to `testenv.RouterMode`); `Restart()` creates a live `Connector` for non-empty `UpstreamRouters` and polls until `ModePE` (3s timeout); `newEnv` allocates a dynamic PE fixture listener instead of stub `"127.0.0.1:9999"` | Q2, FO-2; VP-038 harness prerequisite |
| 6 | `cmd/switchboard/router_sighup_test.go` | `TestRunRouter_VP038_EtoPEViaConfigOnly` migrated: `SetSighupCh`/`SendReloadSignal` seam retired; now drives `rawSighupCh <- syscall.SIGHUP` directly (same inline pattern used by all other SIGHUP tests). Emission scan remains the authoritative observable; `RouterHandle.Mode()` is not asserted (externally-started goroutine is disconnected from the handle). All 9 other `TestRunRouter_SIGHUPReload_*` tests were already seam-free — no changes required. | FO-2; AC-006 |
| 7 | `cmd/switchboard/router_pe_connector_test.go` (NEW) | Integration tests: `TestRunRouter_PE_DialAndConnect_UpstreamReachable`, `TestRunRouter_PE_SetEqualReconciliation_NoTeardownOnReorder`, `TestRunRouter_PE_UnreachableUpstream_PartialPE`, `TestRunRouter_PE_KeepalivePassedToConnector`, `TestRunRouter_PE_EFWD001ReconfirmationUnderLoad`, `TestRunRouter_PE_RouterHandleModeReflectsLiveState`, `TestE2E_EtoPEGraduationByConfigChange`, `TestE2E_RouterDrain_NodesMigrateWithin2s` | AC-001 through AC-006; VP-037, VP-038 |
| 8 | `.factory/specs/verification-properties/VP-037.md` | Flip `verification_lock: true` when `TestE2E_RouterDrain_NodesMigrateWithin2s` passes; or update `lifecycle_status` with partial-discharge note if blocked on S-7.04-FU-DRAIN-WIRE | VP-037 discharge per AC-005 |
| 9 | `.factory/specs/verification-properties/VP-038.md` | Flip `verification_lock: true` when `TestE2E_EtoPEGraduationByConfigChange` passes with live `RouterHandle.Mode()` | VP-038 discharge per AC-005 |
| 10 | `.factory/specs/architecture/ARCH-08-dependency-graph.md` | §6.4: register `internal/upstreamdial` at position 19 (`{halfchannel, outerassembler}` (corrected from `{frame, outerassembler}` per F-P1-001/ARCH-08 v2.7; story prose swept at v1.7 per F-P3-002 extension), effectful network I/O, Wave 7); §6.5: renumber current positions 19→20 (`internal/mgmt`), 20→21 (`internal/discovery`), 21→22 (`internal/svtnmgmttest`), 22→23 (`internal/testenv`) — must land in the same commit that introduces the new package | Q4 binding; ARCH-08 §6.4 registration protocol |
| 11 | `cmd/switchboard/router_config_test.go` | ~~Add `TestUpstreamRoutersAsSet`~~ **DELETED (F-P1-007):** `upstreamRoutersAsSet` helper deleted; its test deleted with it. Set-equal logic lives in `Connector.reconcile` (untestable here). | BC-2.09.001 EC-002; Q1 |
| 12 | `internal/testenv/testenv_test.go` (NEW — **added F-P2-001**) | `TestRouterHandle_Restart_TwicePE` — exact adversary reproduction shape: StartRouter, Restart×2, t.Cleanup fires second Stop on conn1; no panic; proves Stop idempotency in the Restart/t.Cleanup path. File absent from FCL through v1.5 (no testenv_test.go changes in P1); F-P2-001 regression test added in a14f849. | F-P2-001; AC-006 Stop-idempotency regression |

---

## Estimated Test Surface

The following test functions are expected across the two new test files and the
migrated/upgraded existing files.

**`internal/upstreamdial/connector_test.go` (NEW — unit):**

| Function | Proves |
|----------|--------|
| `TestConnector_DialSuccess_ModePE` | TCP dial + outerassembler bootstrap write → connected count increments; Mode() == ModePE |
| `TestConnector_DialFailure_EC001Log` | Failed dial → "upstream router <addr> unreachable" emitted verbatim; Mode() == ModeE |
| `TestConnector_ReorderReuse_NoTeardown` | Same addresses in different order → no teardown; upstreamRoutersAsSet set-equal |
| `TestConnector_BackoffConstants` | **SHIPPED (F-P2-002):** renamed from TestConnector_BackoffParameters; floor guard only — asserts the three Q5 constant values: BackoffBase=500ms, BackoffCap=30s, BackoffJitterFraction=0.25 |
| `TestOperativeBase_TracksKeepalive` | **SHIPPED (F-P2-002):** pure-function extraction; 8 sub-tests covering above-floor (1s, 600ms, 1200ms, 5s), exact-floor (500ms), and below-floor (100ms, 1ms, 499ms) inputs; mutation-detectable (floor sub-cases fail if either dialLoop wiring site reverts to raw keepaliveInterval) |
| `TestConnector_OperativeBackoffBase_TracksKeepalive` | **SHIPPED (F-P2-002):** coarse timing; 3 sub-tests with disjoint bands [600ms–1250ms] vs [1500ms–2500ms] vs [300ms–750ms]; proves both dialLoop wiring sites call operativeBase |
| `TestConnector_BackoffParameters` | **SHIPPED (F-P2-002, P3 F-P3-001):** rewritten as reset-on-success wiring test; grows backoff to ~4s via 3 failures, connects (reset), drops connection, measures gap post-reset expects ~1000ms (operative base, window [700ms, 1300ms]), fails if grown backoff carried forward (~4s); BackoffBase mutant band [375ms, 625ms] disjoint below loWindow=700ms; pollForMode budget 15s; reset-on-success site now mutation-pinned (P3 F-P3-001) |
| `TestConnector_AllUpstreamsUnreachable_ModeE` | All dials fail → Mode() == ModeE; EC-004 "mode=E" log |
| `TestConnector_KeepaliveTickerDrivesHealthProbe` | Keepalive ticker fires probe frames at configured interval. **Post-P1 (F-P1-003): reshaped — drains bootstrap frame in Read-1, requires second write in Read-2 within 4 keepalive intervals; test now fails if keepalive tick case is removed.** |
| `TestConnector_ReloadAddrs_AddsAndRemoves` | ReloadAddrs with added + removed addresses: new dials initiated; removed connections torn down |
| `TestNextBackoff_DoublingWithinJitterBand` | **NEW (F-P1-004):** backoff doubles within ±25% jitter band for multiple base inputs (1000 trials). Note (F-P2-002): this suite covers floor/cap/jitter arithmetic at generic bases; keepaliveInterval-coupling obligations belong to `TestConnector_BackoffParameters`. |
| `TestNextBackoff_CapClamp` | **NEW (F-P1-004):** result never exceeds BackoffCap for inputs at or above cap |
| `TestNextBackoff_FloorAtBase` | **NEW (F-P1-004):** result never falls below BackoffBase for sub-base inputs (floor=500ms constant guard per F-P2-002) |
| `TestNextBackoff_JitterBounds` | **NEW (F-P1-004):** 10 000 trials at BackoffBase stay within [0.75×2×base, 1.25×2×base] |
| `TestConnector_EC004_DropToZero_ModeEEmission` | **NEW (F-P1-006):** establishes live upstream (ModePE), closes server+listener, asserts EC-004 log fires within 3s |
| `TestConnector_NoEC004OnGracefulStop` | **SHIPPED (F-P4-001):** graceful-Stop polarity guard regression — connects to live fixture, reaches ModePE, calls Stop() while listener is healthy, asserts the EC-004 "mode=E" line does NOT appear; stash-verified failing pre-fix |
| `TestConnector_Stop_Idempotent` | **SHIPPED (F-P2-001):** Stop() idempotency — two sequential + one concurrent Stop() calls on the same Connector; no panic; all callers return; verifies sync.Once fix for "close of closed channel" panic |
| `TestConnector_ReloadAddrs_StormNoDeadlock` | **SHIPPED (F-P5-001):** 200k ReloadAddrs calls with the reconcile goroutine live; 10s watchdog goroutine aborts the test if it fires; storm completes in 0.10s post-fix; stash-verified wedging pre-fix; regression for the blocking-drain deadlock |

**`cmd/switchboard/router_pe_connector_test.go` (NEW — integration):**

| Function | Proves |
|----------|--------|
| `TestRunRouter_PE_DialAndConnect_UpstreamReachable` | AC-001: PE startup dials upstream fixture; Mode() == ModePE |
| `TestRunRouter_PE_SetEqualReconciliation_NoTeardownOnReorder` | AC-001 Q1: reload with same addrs reordered → no teardown |
| `TestRunRouter_PE_UnreachableUpstream_PartialPE` | AC-002: one reachable + one unreachable; Mode() == ModePE; EC-001 log |
| `TestRunRouter_PE_KeepalivePassedToConnector` | AC-003: keepaliveIntervalFor(cfg) value reaches Connector |
| `TestRunRouter_PE_EFWD001ReconfirmationUnderLoad` | AC-004: discharges postcondition 2 only (no spurious E-FWD-001 under normal load). Postcondition 1 (E-FWD-001 fires under path-exhaustion) re-anchored to `S-BL.PE-RECEIVE-LOOP` (unmet-deps, F-P1-002). |
| `TestRunRouter_PE_RouterHandleModeReflectsLiveState` | AC-006: RouterHandle.Mode() tracks live connector state. **Post-P1 (F-P1-005): hardcoded `127.0.0.1:9999` replaced with probe-and-close ephemeral port pattern (avoids port-collision in parallel test runs).** |
| `TestE2E_EtoPEGraduationByConfigChange` | VP-038 proof: E→PE via config restart; Mode() == ModePE; SVTN unchanged |
| `TestE2E_RouterDrain_NodesMigrateWithin2s` | VP-037 proof: drain signal; nodes migrate within 2s (or partial-discharge note) |

**Existing tests requiring migration or upgrade:**

| Function | File | Change |
|----------|------|--------|
| `TestRunRouter_VP038_EtoPEViaConfigOnly` | `router_sighup_test.go` | AC-006 migration: `SetSighupCh`/`SendReloadSignal` seam retired; migrated to `rawSighupCh <- syscall.SIGHUP` directly (same inline pattern already used by all other SIGHUP tests). Emission scan remains the authoritative observable; `RouterHandle.Mode()` is NOT asserted here (externally-started runRouter goroutine is disconnected from the handle). |
| All 9 other `TestRunRouter_SIGHUPReload_*` tests | `router_sighup_test.go` | Already used inline `rawSighupCh <- syscall.SIGHUP` pattern — no migration required (seam-free before this story). |
| `TestKeepaliveIntervalNotSweepDeadline` | `router_config_test.go` | **Must remain unmodified and green** (normative fence per Q7) |

**New test added to existing file:**

| Function | File | Proves |
|----------|------|--------|
| ~~`TestUpstreamRoutersAsSet`~~ | ~~`router_config_test.go`~~ | **DELETED (F-P1-007):** `upstreamRoutersAsSet` helper deleted; test deleted with it. |

**Estimated total test count:** 15–25 tests across new + migrated files, consistent
with placement note estimate of 15–25.

---

## Forward Obligations Consumed

| FO ID | Origin | Description | Consumed by | Notes |
|-------|--------|-------------|-------------|-------|
| FO-1 | S-7.04-FU-SIGHUP-RELOAD DELIVERY | `equalStringSlices` is order-sensitive; connection reconciliation needs set-equal semantics | AC-001 + FCL row 4 (`upstreamRoutersAsSet` helper in `router_config.go`) | Per placement note Q1: emission diff stays positional (no change); connection reconciliation MUST use set-equal. Two diffs lawfully differ. |
| FO-2 | S-7.04-FU-SIGHUP-RELOAD DELIVERY | `RouterHandle.Mode()` transitional seam (`SetSighupCh`/`SendReloadSignal`) must be retired; construction-time wiring and real state tracking land here | AC-006 + FCL row 5 (`testenv.go`) + FCL row 6 (`router_sighup_test.go` migration) | Per placement note Q2: `SetSighupCh` and 1-arg `SendReloadSignal` removed; `RouterHandle.Mode()` delegates to live `connector.Mode()`. |
| FO-3 | S-7.04-FU-SIGHUP-RELOAD DELIVERY | EC-003/E-CFG-001 rendering nuance — whether a BC amendment is warranted for cross-BC error rendering | Consumed: no spec change needed (per placement note Q9 ruling) | Ruling: "no spec-side narrowing needed for EC-003 in the reload+dial era. The existing rendering is correct and complete. The cross-BC note is a documentation artifact only, not a gap. No BC amendment." EC-003 (validation-time) and EC-001 (runtime) are disjoint and unambiguous. |
| FO-4 | S-7.04-FU-SIGHUP-RELOAD DELIVERY | `upstreamRouters` single-goroutine assumption → concurrency design when dial goroutines read concurrently with reload | Concurrency Contract design section + FCL row 1 (`Connector` implementation) | Per placement note Q3: `upstreamRouters` stays local var in `runRouter`; Connector receives updates via buffered `chan []string` (channel-passed snapshot); reconnect state is Connector-internal with `atomic.Int32` for `Mode()`. |
| FO-5 | S-7.04-FU-SIGHUP-RELOAD DELIVERY | VP-038 full production activation — seam-level discharge in S-7.04-FU-SIGHUP-RELOAD; full activation (live `RouterHandle.Mode()` + PE connector evidence) lands here | AC-005 (VP-038 subcondition) + AC-006 (Mode() live accessor) + FCL row 9 (VP-038.md `verification_lock` flip) | Per placement note Q8: VP-038 `verification_lock` flipped to true when `TestE2E_EtoPEGraduationByConfigChange` passes with live Mode() assertion. |

---

## Changelog

| Version | Date | Change |
|---------|------|--------|
| 0.1-backlog-stub | 2026-07-06 | Initial backlog stub. Status: backlog. 5 sketched ACs. No FCL, no design constraints. |
| 1.0-draft | 2026-07-07 | Elaborated to sprint-ready draft. Status: backlog → draft. ACs expanded to 6 normative ACs: AC-001 (BC-2.09.001 PC-2/PC-3 dial + session bootstrap + set-equal reconciliation), AC-002 (EC-001 backoff base=500ms/cap=30s/jitter±25% + partial-PE), AC-003 (BC-2.09.003 PC-8 keepalive in Connector), AC-004 (E-FWD-001 re-confirmation under sustained load), AC-005 (VP-037/VP-038 verification_lock discharge), AC-006 (Q2/FO-2 testenv seam retirement). Design Constraints sections added for dial-loop architecture (Q2/Q4), concurrency contract (Q3/FO-4), session bootstrap definition (Q6), and testenv seam retirement (Q2/FO-2). FCL: 10 rows including ARCH-08 §6.4/§6.5 registration row and internal/upstreamdial new files. FO-1 through FO-5 consumed. internal/upstreamdial added to architecture_modules. inputDocuments extended with placement note, SIGHUP DELIVERY, VP-037, VP-038. |
| 1.1-draft | 2026-07-07 | PO returned: D-1 BC-4.03 phantom citation corrected to BC-2.02.008/BC-2.06.003; D-2 admission deferral class added (not-core). |
| 1.1 | 2026-07-07 | PO re-review passed (D-1/D-2 verified fixed), status ready. |
| 1.2 | 2026-07-07 | Post-implementation FCL/test-surface sync (paired-edit rule): `TestUpstreamRoutersAsSet` + FCL row 11 (`router_config_test.go`) added; FCL rows 5–6 reconciled to shipped shape (SetSighupCh/SendReloadSignal fully removed, sighupCh field + os/syscall imports gone, SetConnector seam added, dynamic PE listener in newEnv; VP-038 test migrated to inline rawSighupCh — emission-based observable retained, not Mode(); 9 other sighup tests already seam-free); AC-006 postcondition 3 reconciled (1 test migrated, 9 already inline); Q6 shipped-deferral note added (zero-valued Envelope, documented in mgmt_wire.go); VP-038/AC-005 precondition updated to past tense (stub replaced); VP-037/VP-038 test file location clarified. Status remains ready. |
| 1.3 | 2026-07-07 | PO adjudication F-P1-002: AC-004 partial-discharge (class: unmet-deps). Postcondition 2 (no spurious E-FWD-001 under normal load) DISCHARGED by shipped test. Postcondition 1 (E-FWD-001 fires under path exhaustion) and S404-OBS-F/S404-LOW-1 RE-ANCHORED to S-BL.PE-RECEIVE-LOOP (owning story for PE-connection receive/forward loop through FrameArrivalHandler.OnFrameArrival; direct prerequisite for S-7.04-FU-DRAIN-WIRE). Anchors table disposition column added. |
| 1.4 | 2026-07-07 | Post-P1-remediation sync: dead helper rows removed (F-P1-007: `upstreamRoutersAsSet` + `TestUpstreamRoutersAsSet` deleted from FCL rows 4/11; `peConnectorHook` var removed OBS-1; `reconcileLoop` empty keepalive-ticker case removed OBS-2); new backoff/EC-004 tests added to test-surface table (F-P1-004: `TestNextBackoff_*` 4-test suite; F-P1-006: `TestConnector_EC004_DropToZero_ModeEEmission`); keepalive test isolation reshaped (F-P1-003: drain-bootstrap-then-require-second-write); ModeReflectsLiveState port collision fix noted (F-P1-005: probe-and-close ephemeral port). |
| 1.5 | 2026-07-07 | F-P2-002 ruling sync: AC-002 backoff semantics corrected to keepaliveInterval-based schedule (BackoffBase=500ms is floor constant only); test-strengthening obligation recorded. Specifically: AC-002 PC-3 rewritten — first retry delay = `keepaliveInterval` (floored at `BackoffBase`=500ms), exponential growth from that base, reset-on-success to `keepaliveInterval` (floored); `BackoffBase` is floor constant not schedule base; citation added "per placement note Q5/Q7 as reconciled by F-P2-002 ruling (v1.3)". AC-002 test-name description for `TestConnector_BackoffParameters` updated to reflect F-P2-002 strengthening obligation (tracks keepaliveInterval, not fixed 500ms). FCL row 1 corrected from "base=500ms" to "schedule-base=keepaliveInterval floored at BackoffBase=500ms". `TestNextBackoff_FloorAtBase` annotated as floor-constant guard. All remaining "500ms" occurrences are floor-semantics only. |
| 1.6 | 2026-07-07 | F-P2-001/F-P2-002 shipped-test true-up: Stop idempotency (stopOnce sync.Once) + 2 regression tests; backoff suite split (BackoffConstants/OperativeBase×2/reset-on-success rewrite); operativeBase pure func recorded in FCL. Specifically: (a) AC-002 test-names bullet split from 1 pending-obligation row into 4 shipped rows (TestConnector_BackoffConstants, TestOperativeBase_TracksKeepalive, TestConnector_OperativeBackoffBase_TracksKeepalive, TestConnector_BackoffParameters-rewritten); (b) test-surface table: TestConnector_BackoffParameters row replaced by 4 shipped rows; TestConnector_Stop_Idempotent added to connector_test.go section; (c) AC-006 test-names bullet: TestConnector_Stop_Idempotent + TestRouterHandle_Restart_TwicePE rows added (both F-P2-001, Stop idempotency, double-Restart reproduction); (d) FCL row 1: stopOnce field + Stop idempotency + operativeBase pure func added to prose; (e) FCL row 12 (NEW): internal/testenv/testenv_test.go — absent from FCL through v1.5, TestRouterHandle_Restart_TwicePE lives here (a14f849). |
| 1.7 | 2026-07-07 | pass-3 sync: F-P3-001 backoff test window retuned (testKeepalive 600ms→1s, window [400,850]→[700,1300]ms, BackoffBase mutant band [375,625] disjoint below loWindow=700ms, pollForMode budget 15s; reset site mutation-pinned); F-P3-002 extension — stale {frame, outerassembler} prose swept (3 locations) to {halfchannel, outerassembler}; F-P3-003 stub-comment framing removed where echoed (no occurrences found — no changes needed). |
| 1.8 | 2026-07-07 | pass-4 sync: F-P4-001 EC-004 graceful-Stop polarity guard (`ctx.Err() == nil` check symmetric with EC-001, `connector.go:346-350`) + `TestConnector_NoEC004OnGracefulStop` regression (stash-verified failing pre-fix). AC-002 postcondition 5 clarified with polarity note (emission fires on upstream-LOSS only; self-initiated teardown (Stop) does not emit). FCL row 1 updated with Post-P4 guard prose. |
| 1.9 | 2026-07-08 | pass-5 sync: F-P5-001 ReloadAddrs deadlock (Q3-prescribed pattern defect) — corrected to fast-path + non-blocking drain/resend per placement note Q3 v1.4 erratum; `TestConnector_ReloadAddrs_StormNoDeadlock` regression added (200k-call storm, live reconcile goroutine, 10s watchdog, stash-verified wedging pre-fix, 0.10s green); Concurrency Contract pattern quote swept (old blocking pattern replaced with corrected three-select form + F-P5-001 explanation); FCL row 1 updated with Post-P5 prose. |
