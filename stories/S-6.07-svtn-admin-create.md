---
artifact_id: S-6.07
document_type: story
level: ops
version: "1.0"
status: draft
producer: product-owner
timestamp: 2026-06-29T00:00:00
phase: 2
cycle: v1.0.0-greenfield
epic: E-6
wave: 6
points: 3
priority: P2
scope: E
depends_on: [S-6.02, S-6.06]
bc_traces: [BC-2.07.001]
vp_traces: [VP-048]
subsystems: [network-management]
---

# S-6.07: Register `admin.svtn.create` Handler and `sbctl admin svtn create` CLI Subcommand

## Summary

Register the `admin.svtn.create` RPC handler in `BuildAdminHandlers` (control-mode daemon only) and wire the `sbctl admin svtn create --name=<svtn-name>` CLI subcommand. Adds caller-key-role enforcement (BC-2.07.001 Inv-3) so only RoleControl callers may invoke the handler. Completes the RPC-reachability gap identified in S-6.02 lens3 F-003 / F-010.

## Context

S-6.02 implemented `SVTNManager.Create()` (the Go method + local side-effects). S-6.06 wired `admin.key.*` handlers into `BuildAdminHandlers`. Neither story registered an `admin.svtn.create` handler or CLI subcommand, leaving BC-2.07.001 PC-1 without end-to-end RPC reachability (SCOPE DECISION: S-6.02 lens3 F-003 + F-010, both HIGH). This story closes that gap.

Approved: HUMAN-APPROVED PATH B (2026-06-29 adversarial reconverge pass).

## Acceptance Criteria

| ID | Criterion | BC Trace |
|----|-----------|----------|
| AC-001 | `BuildAdminHandlers` registers `"admin.svtn.create"` handler (control-mode daemon only; same guard as `admin.key.*` handlers in S-6.06). Handler dispatches to `SVTNManager.Create()`. | BC-2.07.001 PC-1 |
| AC-002 | `sbctl admin svtn create --name=<svtn-name>` CLI subcommand sends `{"type":"admin.svtn.create","name":"<svtn-name>"}` to the daemon and prints the returned `svtn_id` and bootstrap fingerprint on success. | BC-2.07.001 PC-1 + PC-2 |
| AC-003 | Caller-key-role check (BC-2.07.001 Inv-3): if the authenticated caller's key role is not `RoleControl`, the handler returns `E-ADM-009` ("insufficient authority for operation admin.svtn.create: key <key_fingerprint> has role <role>"). The request is rejected before `SVTNManager.Create()` is called. | BC-2.07.001 Inv-3 |
| AC-004 | On success, `mgmt.Response` contains `ok: true` plus a `data` object with fields `svtn_id` (hex string) and `bootstrap_fingerprint` (hex string of the first control key's public key fingerprint). | BC-2.07.001 PC-1 + PC-2 |
| AC-005 | On duplicate SVTN name, handler propagates `E-SVTN-001` ("SVTN already exists: <id>") to the RPC response as `ok: false, error.code: E-RPC-011, error.message: "SVTN already exists: <name>"`. | BC-2.07.001 EC-001 |
| AC-006 | Unit test: control-role caller succeeds; non-control-role caller receives E-ADM-009; duplicate-name caller receives SVTN-exists error. | BC-2.07.001 PC-1 + Inv-3 + EC-001 |

## VP Contribution

| VP-ID | Property | This Story's Row |
|-------|----------|-----------------|
| VP-048 | SVTN create is idempotent for the first invocation; error on duplicate | Row 1: handler + CLI makes svtn create end-to-end RPC-reachable (previously missing) |

## Files

New files:
- `internal/mgmt/admin_svtn.go` — `adminSVTNCreateHandler(mgr *svtnmgmt.SVTNManager) mgmt.Handler`

Modified files:
- `internal/mgmt/handlers.go` (or equivalent `BuildAdminHandlers` registration site) — register `"admin.svtn.create"` handler for control-mode only
- `cmd/sbctl/admin.go` — add `svtn create` sub-subcommand
- `internal/mgmt/admin_svtn_test.go` — unit tests (AC-006)

## Architecture Anchors

- ARCH-04 `internal/svtnmgmt` — `SVTNManager.Create()` is the target method
- BC-2.07.001 PC-1 (Create) and PC-2 (Bootstrap) and Inv-3 (control-only)
- S-6.02 — must be merged (provides `SVTNManager.Create()`)
- S-6.06 — must be merged (provides `BuildAdminHandlers` registration pattern)

## Serialization

Depends on S-6.02 and S-6.06 both merged. Can run in parallel with other Wave-6 stories that do not edit `BuildAdminHandlers` or `cmd/sbctl/admin.go`. Must merge before S-W5.02 if S-W5.02 is extended to include svtn-create e2e coverage.

## Changelog

| Version | Date | Change |
|---------|------|--------|
| 1.0 | 2026-06-29 | Minted per HUMAN-APPROVED PATH B (S-6.02 + S-6.06 Pass-1 adversarial reconverge, F-003/F-010) |
