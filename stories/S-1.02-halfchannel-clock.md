---
artifact_id: S-1.02-halfchannel-clock
document_type: story
level: ops
story_id: S-1.02
title: "implement timeslice clock state machine in internal/halfchannel"
status: ready
producer: story-writer
timestamp: 2026-06-24T00:00:00
last_revised: 2026-06-24T00:00:00
revision: "1.1"
phase: 2
epic: E-1
wave: 1
priority: P0
scope_phase: E
estimated_points: 8
bc_traces:
  - BC-2.01.001
  - BC-2.01.002
  - BC-2.01.003
vp_traces: [VP-016, VP-017, VP-018, VP-041, VP-051, VP-053]
subsystems: [session-networking]
architecture_modules: [internal/halfchannel]
tdd_mode: strict
cycle: v1.0.0-greenfield
depends_on: [S-1.01]
blocks: [S-3.01, S-4.02, S-4.03]
inputDocuments:
  - '.factory/specs/prd.md'
  - '.factory/specs/behavioral-contracts/BC-INDEX.md'
  - '.factory/specs/behavioral-contracts/ss-01/BC-2.01.001.md'
  - '.factory/specs/behavioral-contracts/ss-01/BC-2.01.002.md'
  - '.factory/specs/behavioral-contracts/ss-01/BC-2.01.003.md'
  - '.factory/specs/architecture/ARCH-02-protocol-stack.md'
acceptance_criteria_count: 6
---

# S-1.02: Implement Timeslice Clock State Machine in internal/halfchannel

> **Execute:** `/vsdd-factory:deliver-story S-1.02`

## Narrative

- **As a** session endpoint (access node or console)
- **I want to** drive a timeslice clock that fires at regular intervals regardless of keystroke availability
- **So that** liveness is maintained and the downstream display never goes silent even during idle periods

## Acceptance Criteria

### AC-001 (traces to BC-2.01.001 postcondition 1)
`HalfChannel.Tick()` produces exactly one frame per call, regardless of whether payload is available.
- **Test:** `TestHalfChannelTick_OneFramePerCall`

### AC-002 (traces to BC-2.01.002 postcondition 1, 2)
When no payload is queued, `Tick()` produces an empty-tick frame with `FrameType == FrameTypeEmptyTick` (0x02) and zero-length payload. The test asserts `frame.FrameType == halfchannel.FrameTypeEmptyTick` and `len(frame.Payload) == 0`. (`ParseOuterHeader` validity is out-of-scope for the pure-core boundary — the outer-assembler is responsible for setting `OuterHeader.FrameType` from `ChannelFrame.FrameType`; see ARCH-09 and BC-2.01.005 invariant 1.)
- **Test:** `TestHalfChannelTick_EmptyFrameIsValid`

### AC-003 (traces to BC-2.01.003 postcondition 1)
Upstream and downstream `HalfChannel` instances maintain independent sequence counters. Ticking channel A does not advance channel B's sequence number.
- **Test:** `TestHalfChannelIndependentSequences`

### AC-004 (traces to BC-2.01.003 postcondition 2)
Each successive call to `Tick()` on the same half-channel increments the sequence number by exactly 1.
- **Test:** `TestHalfChannelSequenceIncrement`

### AC-005 (traces to BC-2.01.001 invariant 1)
Tick timing: p99 jitter of the tick interval must be ≤ 2ms (measured over 1000 ticks in test). This is the benchmark gate for VP-041.
- **Test:** `BenchmarkHalfChannelTickJitter`

### AC-006 (traces to BC-2.01.002 invariant 1)
K consecutive ticks with no payload produce K frames with contiguous sequence numbers (no gaps, no duplicates).
- **Test:** `TestHalfChannelEmptyTickSequence`

## Architecture Mapping

| Component | Module | Pure/Effectful |
|-----------|--------|---------------|
| HalfChannel | internal/halfchannel | pure-core (state machine) |

## Edge Cases

| ID | Scenario | Expected Behavior |
|----|----------|-------------------|
| EC-001 | Tick called before any payload enqueued | Empty-tick frame emitted; no panic |
| EC-002 | Sequence number wraps around uint32 max | Sequence wraps to 0; no overflow panic |
| EC-003 | Multiple payloads queued before one tick | Single tick emits one frame; remaining payloads queued for subsequent ticks |

## Purity Classification

| Module | Classification | Justification |
|--------|---------------|---------------|
| internal/halfchannel | pure-core | State machine only; no goroutines, no timers. Tick is called externally by the effectful layer. |

## Token Budget Estimate (MANDATORY)

| Context Source | Estimated Tokens |
|---------------|-----------------|
| This story spec | ~700 |
| BC-2.01.001.md | ~600 |
| BC-2.01.002.md | ~400 |
| BC-2.01.003.md | ~500 |
| ARCH-02 | ~800 |
| internal/frame (dependency) | ~400 |
| Test files | ~900 |
| Tool outputs overhead | ~300 |
| **Total** | **~4,600** |
| Agent context window | 200K |
| **Budget usage** | **~2.3%** |

## Tasks (MANDATORY)

1. [ ] Read BC-2.01.001, BC-2.01.002, BC-2.01.003 and ARCH-02 half-channel section
2. [ ] Write failing tests for AC-001 through AC-006 (test-writer)
3. [ ] Verify Red Gate: all tests fail before implementation
4. [ ] Create internal/halfchannel/halfchannel.go with HalfChannel struct
5. [ ] Implement Tick() method (AC-001, AC-002)
6. [ ] Implement independent sequence counters (AC-003, AC-004)
7. [ ] Implement empty-tick sequence property (AC-006)
8. [ ] Write benchmark for tick jitter (AC-005, VP-041)
9. [ ] Write property tests for sequence monotonicity (VP-016, VP-017, VP-018)
10. [ ] Verify no goroutines spawned inside halfchannel (purity gate)
11. [ ] just fmt && just lint pass

## Spec Patches (from adversary pass 1)

| Finding | Change | Rationale |
|---------|--------|-----------|
| F-002 (CRITICAL): AC-002 asserted `frame_type=data` (wrong) and `ParseOuterHeader` validity (out-of-scope for pure-core) | AC-002 revised: frame type corrected to `FrameTypeEmptyTick` (0x02); `ParseOuterHeader` clause replaced with direct `ChannelFrame.FrameType` assertion | BC-2.01.002 postcondition 2 specifies `frame type = EMPTY_TICK`; ARCH-09 classifies `internal/halfchannel` as pure-core; outer header assembly is the outer-assembler's responsibility per ARCH-09 and BC-2.01.005 invariant 1. Option A chosen: `ChannelFrame` must carry `FrameType byte` so the outer-assembler can set `OuterHeader.FrameType` correctly; without this field BC-2.06.002 (quality monitoring) is blocked. |
| F-002: `ChannelFrame` struct missing `FrameType` field | File Structure Requirements updated to mandate `FrameType byte` on `ChannelFrame` and package-level constants `FrameTypeData (0x01)` / `FrameTypeEmptyTick (0x02)` | BC-2.01.002 invariant 2: "The frame type field distinguishes empty-tick from data frames." |

## Previous Story Intelligence (MANDATORY)

| Story | Key Decisions | Patterns Established | Gotchas Discovered |
|-------|--------------|---------------------|-------------------|
| S-1.01 | internal/frame is DAG root; no internal imports | OuterHeader struct; table-driven tests | SHA-256 for address derivation, not Blake3 |

## Architecture Compliance Rules (MANDATORY)

| Rule | Source | Enforcement |
|------|--------|-------------|
| internal/halfchannel imports ONLY internal/frame | ARCH-08 topological order position 7 | go vet import check |
| Tick() has no goroutines / timers inside | ARCH-09 purity: pure-core | Code review; no time.Sleep/time.After |
| Tick interval range 5–50ms validated in Phase 3 | ADR-008 | Not enforced here; parameter is external |

## Library & Framework Requirements (MANDATORY)

| Tool | Version | Purpose |
|------|---------|---------|
| Go | 1.25.4 | Per go.mod |
| testing | stdlib | Benchmark for VP-041 tick jitter |

## File Structure Requirements (MANDATORY)

| File | Action | Purpose |
|------|--------|---------|
| internal/halfchannel/halfchannel.go | create | HalfChannel struct, Tick(), sequence state; ChannelFrame with FrameType byte field; FrameTypeData (0x01) and FrameTypeEmptyTick (0x02) package-level constants |
| internal/halfchannel/halfchannel_test.go | create | Unit + property + benchmark tests |
