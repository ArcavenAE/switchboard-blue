# S-BL.LOOPBACK-FULLSTACK R37 — 4-Leg Adversarial Reconvergence Pass (CLEAN — RECONVERGED) (2026-08-29)

## Context

Following the R36 4-leg reconvergence pass (CLEAN — zero findings, zero
spec artifacts changed; STORY-INDEX v4.173), the Step-4.5 convergence
counter stood at 2/3 — R36 was pass 2 of the fresh 3-consecutive-clean-pass
streak required against the v1.20 tip (the streak reset by the v1.19
human-gate edit; R33 and R34 were both remediation passes that stayed
0/3; R35 was pass 1). R37 is pass 3 of that streak — the reconvergence
pass.

**Dispatch:** POL-005 four-leg fresh-context rig against `.factory` HEAD
`ee72b0e` (the R36 record commit, and the worktree HEAD at dispatch time)
— story v1.20/`7967a2f`, placement note v1.16 UNCHANGED, STORY-INDEX
v4.173. develop unchanged at `2ce3a57`.

## Verdict

**CLEAN — all four legs, zero findings, zero spec artifacts changed. THREE
CONSECUTIVE CLEAN PASSES (R35/R36/R37) — the story RECONVERGES at v1.20.**

- **§1.8 Oracle: GREEN.** Story `input-hash` `7967a2f` computed fresh and
  verified equal to the story frontmatter. AC-013 compile-gate injection
  re-proof PASS: the SOUND gate (`go test -tags integration -run '^$'
  -count=1 ./internal/bench/`) CAUGHT the injected compile error (exit 1);
  the superseded OLD gate (`go build`) MISSED it (exit 0) against the same
  injected error, re-confirming the AC-013 v1.13 fix remains load-bearing.
  Bench blob restored byte-identical post-proof (`e53ab35f` before ==
  `e53ab35f` after, no residue/leak). develop unchanged `2ce3a57`.
  Story-test mutation-honesty check: N/A this pass — the story is
  spec-only, draft/unscheduled, with no delivered story code on develop
  to mutate.
- **Lens A (spec fidelity): CLEAN.** All 6 spec-fidelity axes checked,
  including AC↔BC bidirectional coverage (each of the 17 ACs traces to a
  declared BC and vice versa, no orphan on either side). The STORY-INDEX
  reference column's every live pin confirmed current against its source:
  placement note v1.16, VP-042 v1.3 changelog citation, ARCH-08 §6.5
  pos-23 v2.13, and the 6-BC enumeration. Triple-ledger parity (L57
  provenance / L72 status-note / formal changelog) holds 1:1 at v1.20.
  Two residual `v1.15`-era tokens in the placement note (an inline
  fix-provenance tag at L353 attributing a specific historical repair, and
  a convention-declaration cross-reference at L1401) were checked and
  adjudicated **frozen historical provenance, not live pins** — per the
  same discipline established at R33/R34 for the ref-column mega-cell
  (a citation naming what version a past fix landed AT is not required to
  track the note's current version); not a straggler.
- **Lens B (technical soundness): CLEAN.** All load-bearing facts
  re-derived fresh against real `develop@2ce3a57` source, independent of
  any prior pass's conclusions: per-half-channel-instance sequence
  numbering (no shared/global sequence space); `multipath.Frame` carries
  no `ChanSeq` field (re-confirmed by direct read of the type); the
  upstream `SendKeystroke`→`deliverUpstream` path uses in-place `failLoud`
  at the real call site `upstream.go:276`, never a masked
  `multipath.Send`-return-value check; `arq.OnAck`'s signature, its
  dedup-independent guard, and its related constants all re-confirmed
  against the actual `internal/arq` source; the lock order (`sinkMu` ⊃
  `downstreamHCMu` ⊃ `driver.mu`) re-confirmed acyclic (no deadlock);
  `wg.Add` calls precede their `go` statements in every spawn site cited;
  `RoundTrip`'s `done` channel is buffered-1 (liveness-safe against a
  late/lost receiver). The story's own v1.19→v1.20 edit history (BC-2.01.003
  anchor, AC-002(b) precision tightening) re-verified to introduce no new
  defect. Two standing observations — OBS-LENSB-R30-ONACK-EQUIV-LOOSE and
  OBS-LENSB-R10-DBLCREATESESSION — re-confirmed present, informational,
  correctly NOT reset by this pass.
- **Lens C (traceability/consistency): CLEAN.** All traceability and
  propagation mandates checked. Note-pin sweep: all 5 live citations to
  the placement note across story/STORY-INDEX read `v1.16`; the frozen
  historical-provenance anchors (L353, L1401 class) correctly left
  unmoved, matching Lens A's adjudication. The STORY-INDEX reference-column
  mega-cell (~L155) checked whole, not just its tail — F-R34-LENSC-01's
  fix (the note-pin advance to v1.16) remains fixed with no successor
  straggler; the F-R25 (leading-token) and F-R33 (ref-column undercount)
  failure classes not reintroduced anywhere in the cell. Story `input-hash`
  integrity holds (`7967a2f` matches computed). Triple-ledger parity
  (L57/L72/formal changelog) mirrors 1:1, consistent with Lens A.

## Findings and adjudication

None. Zero findings across all four legs.

## Dispatch-hygiene observation (project-content, not a story/engine defect)

The R37 Lens B dispatch tuple, as issued by the orchestrator, named the
story's source paths as `internal/transport/{halfchannel,multipath,arq}.go`.
The real paths are `internal/{halfchannel,multipath,arq}/…` (three
separate top-level packages, not files under a single `internal/transport`
package). This was an **orchestrator dispatch-prompt paraphrase error**,
not a defect in the story spec — the story's own `architecture_modules`
frontmatter field cites the correct paths throughout, and Lens B re-derived
its findings against the real files rather than the paraphrased ones, so
the verdict above is unaffected.

**Lesson for future POL-005 dispatch tuples on this story (and by
extension any story):** copy source paths verbatim from the story's
`architecture_modules` field when constructing a dispatch prompt — never
paraphrase or reconstruct them from memory of the package layout.

## Remediation

None required. This burst's only writes are (1) the routine
changelog/master-row recording that every reconvergence pass performs
(STORY-INDEX status-cell R37 clause + a new v4.174 changelog row), and
(2) this cycle record capturing the dispatch-hygiene lesson above — no
story, placement-note, or content-level STORY-INDEX defect was found.

| Artifact | Before | After |
|---|---|---|
| Placement note | v1.16 | v1.16 (UNCHANGED) |
| Story | v1.20 / `7967a2f` | v1.20 / `7967a2f` (UNCHANGED) |
| STORY-INDEX | v4.173 | **v4.174** |

Story input-hash unchanged (`7967a2f`) — no declared input of the story
changed this burst.

## Convergence counter — RECONVERGED

**Step-4.5 counter ADVANCES 2/3 → 3/3. THREE CONSECUTIVE CLEAN PASSES
(R35, R36, R37) — story S-BL.LOOPBACK-FULLSTACK RECONVERGED at
v1.20/`7967a2f`.** BC-5.39.001 is satisfied for this streak. The story
remains **draft/unscheduled** throughout ("author now, deliver later");
reconvergence is not a merge or delivery event — nothing merges without
human approval, and the story still owes a §1.7 perimeter re-audit before
the human approval gate can be attempted.

## Carried, unaffected by this burst

- **S1.7-SYSTEMIC-SUBSYSTEMS-REGISTRY** — open human-decision item
  (senior-architect scope), unaffected.
- **S1.7-A2-GAP2-BENCH-RECIPE** (missing `just bench-integration` recipe)
  — carried to delivery/scheduling, unaffected.
- **S1.7-F2-VP042-HARNESS**, **S1.7-F3-NOTE-L497-CITATION** — both remain
  deferred, for-human-review-at-approval-gate, unaffected.
- **SURVIVOR-R22** — standing non-defect disposition, unaffected.
- **F-R34-LENSC-01** — remains RESOLVED; re-verified fully propagated
  again this round (third consecutive clean re-verification, after R35's
  and R36's).

## Process observation

The recurring `[process-gap]` observation (three prior incomplete-
propagation stragglers on the hand-maintained STORY-INDEX reference-column
mega-cell) is NOT re-raised this round — all pins found current for the
third consecutive clean pass, across the full streak that just
reconverged the story. This is the strongest evidence yet that the class
is closed for now; the standing recommendation (a validator/generator at
the human gate) is not retired, but is not urgent either.

A new, distinct process observation is logged this round: **dispatch-tuple
source-path paraphrase** (see above) — a one-off orchestrator authoring
error, not a recurring pattern (first instance on this story), captured
here as a forward lesson rather than escalated.

## Next required work

1. **§1.7 fresh-context consistency-validator perimeter re-audit** against
   the newly-reconverged v1.20 spec (both mandatory gate-prerequisite
   steps — 3/3 reconvergence and the perimeter audit — must be satisfied
   before gate-ready status).
2. Only after the audit completes (and any findings it raises are
   resolved without requiring a further reconvergence streak) does the
   story return to gate-ready-pending-human-approval status.
3. The story remains draft/unscheduled; no code delivery is authorized
   by this reconvergence alone.
