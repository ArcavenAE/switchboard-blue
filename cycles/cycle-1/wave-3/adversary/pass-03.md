---
Title: Wave 3 Integration Gate — Adversarial Convergence Pass 3 (fresh context, independent, security-first angle)
Tree: develop @ b68e498
Verdict: NOT_CONVERGED — 0C / 1H / 4M / 3L (+3 OBS)

BLOCKING HIGH:
- F-1 (HIGH) internal/routing/routing.go:138-146 (package has no logger) — BC-2.05.008 PC-2 + E-ADM-016: "logged at the router before return". RouteFrame returns ErrHMACVerificationFailed on no-entry path (138) and verify-fail path (145) with NO log emission; grep of package shows zero log/slog/Logger refs. Postcondition both UNIMPLEMENTED and UNTESTED (tests assert only the sentinel; docstrings aspirational). Operator cannot detect sustained forgery; EC-006 counter has no router-side hook. Fix: injectable logger (constructor option, default stderr), emit E-ADM-016 svtn_id/src_addr before both returns (implementer) + fake-logger assertion (test-writer); if deferred, amend PC-2+VP-058 (product-owner/architect).
MEDIUM:
- F-2 (MEDIUM) cmd/switchboard/main.go:12-34 — version stub; wires none of the 5 subsystems (NewAccessNode/SessionConnector/WithKeystrokeSink only in internal/** + tests). Integration is package-API/test level only; latent wiring defect (e.g. forgetting SessionAuth, see F-3) uncatchable by binary tests. Scope adjudication: architect/product-owner.
- F-3 (MEDIUM) internal/session/upstream.go:156-159 — NewAccessNode(pub, nil) defaults Authorizer to NoOpAuthorizer{} (ALLOW-ALL) = fail-OPEN. Future caller passing nil silently disables all Tier-2 auth + read-only enforcement, no error/log. Asymmetric with fail-loud noSink default. Fix: default deny-all (ErrSessionAuthDenied) when auth==nil, or require explicit + WithAllowAllAuthorizer() test-only opt-in (implementer + architect sign-off).
- F-4 (MEDIUM) tmux control.go:597-635 (%output) vs pty_fallback.go:236-269 (ioRelay) — BC-2.04.001 PC-5/BC-2.04.002 failover seam: at ctrl→PTY swap, old ControlMode.frames buffered frames lost on Close() (consumer sees EOF); no drain/handover; both modes drop non-blockingly on full channel. Acceptable degraded-mode but not stated as contract; no test asserts downstream reachability ACROSS the transition. Fix: document at-swap frame-loss window in BC-2.04.002 (architect) + failover-continuity test (test-writer).
- F-5 (MEDIUM) routing.go:173-191 SVTNRoute — validates dest entry then no-op (_ = payload; _ = entry); forwarding is a stub; router never calls AccessNode.DeliverFrame; no bridge tmux.Frames()→ConsoleSet.Deliver. BC-2.04.006 PC-2 explicitly defers router multicast → in-spec-as-deferred, but "console receives downstream end-to-end" not demonstrable at this gate. Confirm later-wave deliverable (architect).
LOW:
- F-6 (LOW) pty_fallback.go:560-569 controlModeFailureLogMsg — substring matching on err.Error() for log-message selection; locale/platform fragile; mismatch → EC-specific operator log omitted (generic PTY-fallback line still fires). Production uses errors.Is first → only bites wrapped/legacy errors. Fix: sentinel-only matching (implementer).
- F-7 (LOW) routing.go:60 + RouteFrame/SVTNRoute are package functions taking *Router rather than methods — idiomatically odd; nil *Router → nil-deref at 124; method receiver safer + makes "exported methods" doc accurate. Pending intent verification (may be deliberate pure-function-over-forwarding-state choice). Implementer.
- F-8 (LOW) fanout.go:99-105, upstream.go:182-189 — upstream channel ownership enforced only by doc comment; Add returns bidirectional chan []byte a caller could close/race vs EvictStale close(us) → panic. Production routes via SendKeystroke (never touches channel) so exposure is test-harness only → LOW. Fix: return send-only chan<- []byte from Add (implementer).
OBS: OBS-1 test quality high (AC-005/006 wire real SendKeystroke+SessionAuth+captureSink, reject-then-accept dual assertions; routing_internal_test guards tautological-verify; VP-012 grep is genuine positive coverage). OBS-2 VP-012 layering honored (routing imports only {frame,admission,hmac}; ARCH-08 §6.5 edges respected). OBS-3 error-taxonomy v1.6 fidelity clean (E-SES-005 absent; E-ADM-006/007/016 map correctly; HMAC ordering matches PC-3/VP-058; wire-tag-before-zeroing guard present).
Self-validation: dropped 2 candidate findings on re-analysis (watchAndFallback sc.ctrl race — safe via go-stmt happens-before single active watcher; read-only empty-tick flooding — zero-length writes harmless per EC-004 v1.3).
Novelty: MEDIUM. F-1, F-3, F-4/F-5 substantive integration-level gaps per-story review would miss.
---
