# S-BL.LOOPBACK-FULLSTACK Step-4.5 Adversarial Re-Review — Round 13 (2026-08-28)

## POL-005 Dispatch-Integrity Tuple

- **repo_path:** `/Users/skippy/work/aae-orc/run/switchboard-blue/.factory`
- **branch:** `factory-artifacts`
- **expected_head / verified_head:** `bab12d0793b7177049fd4c9e2bfda1ecdebc3783` (confirmed via `git rev-parse HEAD` before dispatch)
- **artifacts under review:** story `S-BL.LOOPBACK-FULLSTACK` v1.11 (input-hash `1145d15`); placement-note v1.11; STORY-INDEX v4.149
- **note:** story/note/index content is identical to the `09d61c54` R11 remediation commit — the `bab12d07` R12-record commit that followed did not alter any of the three converging artifacts.

## Rig

Four-leg diverse-lens rig: verification oracle (§1.8 gates + citation check) + three independently-dispatched fresh-context adversarial lenses (spec-fidelity, technical-soundness/concurrency, traceability). Reviewed tip: story v1.11 / placement-note v1.11 / input-hash `1145d15` at factory tip `bab12d0793b7177049fd4c9e2bfda1ecdebc3783`.

## Findings

### Oracle (task `a0fc6a77c7c801b04`)

**GATES GREEN.** `go build ./...` and `go vet ./...` both exit 0 with zero output, run against code repo `develop` @ `2ce3a5795009209d4e5eebec8fd9051f26f78055`.

**CITATIONS ACCURATE.** All load-bearing citations verified exact against real source:
- `testenv.go`: `b.Helper()`@384, `NewLoopback`@383, `newEnv`@460, `t.Helper()`@461, `t.Cleanup`@475,@528, `Env.Close`@561, `NewWithRouters`@452 (with `t.Fatalf`@455)
- `upstream.go`: `SendKeystroke` gate ordering @285/@288/@289-290/@292
- `arq.go`: `OnAck`@201, `EnqueueSend`@339, window guard@220
- `halfchannel`: `Min`/`MaxTickInterval` 5-50ms
- `multipath`: `Send`@244, `Receive`@318
- `paths`: `PathTracker`@58

**Mutation-honesty: N/A** — spec-only draft, no scaffolding exists yet to mutate (no `loopback.go`).

F-ORACLE-R9-01 not re-raised — carried in survivor ledger per prior rounds' adjudication.

### Lens A — spec-fidelity (task `a1780a036af72060f`)

**NITPICK_ONLY.** The story artifact AND its STORY-INDEX row are fully CLEAN (all cross-surface version/anchor/count consistency verified at v1.11; POL-001 story-changelog 12 rows gap-free; POL-002 synced; input-hash consistent).

One NEW **NITPICK F-LENSA-R13-01**: STORY-INDEX.md changelog version-sequence discontinuity — the v4.145 row (L205) closes "Frontmatter version 4.144 → 4.145" but NO 4.144 changelog row exists (grep-confirmed: rows run 4.146, 4.145, then 4.143; token `4.144` appears only inside L205's own "from" clause), so the ledger jumps 4.143→4.145 citing a 4.144 predecessor with no row. PRE-EXISTING (R5-era 4.145 catch-up burst, well before R11/R13), INDEX-GLOBAL (STORY-INDEX's own document version-history, NOT a surface of S-BL.LOOPBACK-FULLSTACK, whose own version ledger is complete/gap-free), NON-GATING for this story. Fix: add the missing 4.144 row OR correct L205's "from" clause to "4.143 → 4.145".

### Lens B — technical-soundness/concurrency (task `a10a6c9bf0c839ec1`)

**CLEAN.** Every signature cited in the story/note verified EXACT against real `internal/` source (incl. new confirmations: `KeystrokeSink.SendInput`@`upstream.go:68` + "must not call back into AccessNode under any lock" doc, `WithKeystrokeSink`@104, NO `SetSink` (grep=∅), `Publisher.Publish`@`session.go:137`, `RegisterKey`, `RoleFull`, `paths.RankedPath`/`Rank`@375/392).

All concurrency invariants re-derive from shipped code (both tickers `CreateSession`-started; AC-017 single-goroutine-by-construction; `ErrConsoleNotFound` gate ordering; `recordingTB` embed; AC-016 window math; AC-005 dedup; lock ordering acyclic `sinkMu`→`downstreamHCMu`). Design body unchanged since v1.9/R7.

Non-blocking observation: `loopbackSink.SendInput` code SKETCH (L384) omits the `downstreamHCMu` acquisition, but H2 binding prose (L293-314) + AC-015 require it — governed, NOT a defect.

Zero findings.

### Lens C — traceability (task `a978ee852b33d88d3`)

**CLEAN.** All 8 version-ledger surfaces mutually consistent at v1.11; story↔note pins v1.11; changelog honesty verified (v1.11 row claims true, frozen v1.10 row intact); 17 ACs all BC-traced; input-hash `1145d15` correct; version-qualifier drift check — all v1.9/v1.10 occurrences FROZEN fix-tags correctly not moved; POL-002 synced.

Zero findings (not even a nitpick).

## Orchestrator Adjudication (PAT-04)

R13 end-state independently verified: factory tip `bab12d07` unchanged during review; worktree clean except `sidecar-learning.md`; converging artifacts (story, placement-note, STORY-INDEX) byte-identical to the HEAD-committed baseline; code repo `develop` @ `2ce3a57` unmutated.

F-LENSA-R13-01 CONFIRMED REAL on disk (STORY-INDEX has no 4.144 row; L205 cites it) but adjudged LEAVE-IT: NITPICK severity, single-lens, PRE-EXISTING, INDEX-GLOBAL, orthogonal to this story's spec — fixing it would edit an unrelated STORY-INDEX row and reset the counter for cosmetic index-history. Contrast R11's status-note defect (corroborated LOW on a real STORY surface → fixed+reset).

**PASS VERDICT: NITPICK_ONLY (artifacts untouched) → counts as clean.**

**Convergence counter: 1/3 → 2/3. R14 next.**

## Deferred/Survivor Ledger (carried to R14 + human approval gate)

| ID | Severity | Description | Disposition |
|----|----------|--------------|-------------|
| F-ORACLE-R9-01 | below-LOW | placement-note L476 `NewWithRouters` line-ref cites `testenv.go:454`; real line is 452 (off-by-2) | deferred to implementation-time re-grounding; non-load-bearing (Q7 illustrative aside, not an AC/gate/design-constraint) |
| F-LENSA-R13-01 | NITPICK | STORY-INDEX.md changelog version-sequence discontinuity — v4.145 row (L205) cites a "4.144 → 4.145" predecessor but no 4.144 row exists (3-row jump 4.146→4.145→4.143) | INDEX-GLOBAL, pre-existing (R5-era); route to a separate index-hygiene burst POST-convergence, or surface to the human — does NOT gate this story |
| R11 Lens B O-1 | informational | `multipath.Send` error-swallow when ≥1 path succeeds | adjudged non-defect — AC-017/AC-005 already co-locate the check correctly, only parenthetical wording is loose |
| R10 double-CreateSession | observation | no `sync.Once`/idempotence guard against a double-`CreateSession` call | adjudged non-defect — by-design single-session contract (every AC + VP-042 bench calls it once); surfaced for human confirmation at the approval gate |

## Convergence Trajectory

R11 (NOT CLEAN, reset 0) → R12 (CLEAN 1/3) → **R13 (NITPICK_ONLY 2/3)** → R14 next (potential 3/3 → CONVERGED).

Design stable since R7; recent churn pure version-ledger/index hygiene.
