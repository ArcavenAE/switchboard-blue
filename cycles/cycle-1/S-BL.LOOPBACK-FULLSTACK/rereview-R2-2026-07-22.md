# S-BL.LOOPBACK-FULLSTACK Step-4.5 Re-Review — Round 2 (2026-07-22)

**Reviewed tip:** story `a4585bb` (v1.3, input-hash 4c22ebb, 16 ACs) + placement-note `47858d7` (v1.3).
**Reviewers:** 3 fresh-context diverse-lens adversaries (A spec-fidelity, B technical-soundness, C traceability). All verified dispatch-integrity markers. Lens B (first attempt) died on an API timeout and was re-dispatched with hardened anti-stall.
**Verdict:** NOT CLEAN. Convergence counter reset to 0/3. All findings disk-verified (PAT-04) by the orchestrator against source before routing.

## Round-1 findings (9) — ALL CONFIRMED CLEARED in v1.3
Lens A + Lens C independently confirmed B-F1, B-F2, B-F3, C-F1, C-F2, A-M1, A-M2, A-L1, A-L2 are all cleared with no live stale sibling of the changed tokens. Lens B confirmed the three core fixes (B-F1 Publish-precondition, B-F3 CreateSession-time race-freedom, A-M1 unconditional-drain) are technically SOUND against source.

## Round-2 findings (new / residual on the v1.3 tip)

### MED (5) — reset the counter
- **B-1 (MED, RESIDUAL sibling of B-F2)** — `decodeRTID` arity mismatch. §M4 defines `decodeRTID(payload []byte) (id uint64, ok bool)` (2-value) but 1-value call sites survive: story L348 `id := decodeRTID(payload)`, L770 `decodeRTID(payload) == rt.id`, L948 (Task 11 / AC-014); note L276, L918 (canonical §B1 corrected block) + prose L332/L356/L943/L964. B-F2 fixed the `encodeRTID` twin's arity and left `decodeRTID`. Won't compile at the load-bearing AC-014 regression test. Fix: all call sites 2-value; document the `rtSeq.Add(1)` starts-at-1 coupling that makes the id=0 sentinel safe.
- **B-2 (MED, NEW from B-F3)** — window-elimination rationale wrong for the PREFERRED option. `HalfChannel.Tick()` (halfchannel.go:118) increments seq on EVERY tick incl. empty (FrameTypeEmptyTick); OnAck fires only on data ticks. So note L1156-1158 / story L398-399 "when the downstream ticker first fires, chanSeq = 1" is false for CreateSession-time start — chanSeq = number of empty ticks elapsed since CreateSession. Invariant holds only for the non-preferred sync.Once first-SendKeystroke option. Latent (benchmark-as-written dodges it) but the preferred option is justified by a window property it does not provide. Fix: correct the rationale; note the real invariant per option.
- **B-3 (MED, NEW from AC-016)** — AC-016's two suggested fault-injection methods are both unsound. Option A (start ticker at NewLoopback) contradicts B-F3 (forbidden). Option B (test-goroutine OnAck) races the ticker's OnAck on the shared downstreamARQ — violates arq single-writer contract (arq.go pkg doc) and would fail AC-015 (`just test-race`). A sound method exists (build driver, do NOT start ticker, empty-Tick downstreamHC past 64, enqueue one payload, invoke the downstream tick body synchronously so EnqueueSend(66)+OnAck(66) fires with nextExpected=0) but AC-016 describes neither it nor the directly-callable `onDownstreamTick` seam it requires. Fix: replace AC-016's injection guidance with the sound method + name the test seam.
- **N-1 (MED, NEW from B-F3)** — note §M2 self-contradiction. L1160-1169 asserts "There is no third option: CreateSession-time (preferred) or sync.Once-guarded first-SendKeystroke," then L1171-1174 opens "An acceptable simpler alternative: reset downstreamARQ.nextExpected..." The story's §M2 correctly omits this paragraph (story↔note divergence). Defused inline (arq has no Reset) so implementer-risk is low, but it is a live contradiction in the binding note. Fix: delete/subordinate L1171-1174.
- **B-4 (MED, [process-gap], NEW-adjacent)** — upstream error path has no loud-failure AC, asymmetric with AC-016. `driver.accessNode.SendKeystroke` (story L284) can return ErrConsoleNotFound/ErrSessionMismatch/authorizer-error; `deliverUpstream` returns error; no AC/clause requires the upstream ticker to check `multipath.Send`'s return, so an upstream failure is swallowed → manifests only as a WaitForEcho timeout (the SOUL.md §4 silent-failure mode AC-016 exists to prevent, downstream only). v1.3 made the downstream leg loud (AC-016) but left the sibling upstream leg silent. Fix: add a symmetric upstream-error loud-failure AC (AC-017) + require the upstream tick to check the delivery error.

### LOW (2)
- **A-N2 (LOW)** — story v1.3 changelog row omits the input-hash treatment note that v1.1/v1.2 rows carried; hash WAS correctly recomputed to 4c22ebb (orchestrator independently confirmed), so this is a changelog-completeness gap, not a stale hash.
- **C-LOW1 (LOW)** — AC-016 has no Tasks cross-reference; AC-014→Task 11, AC-015→Task 12 do. Underlying behavior is under Task 6 ("check err loudly (M2)"). Add AC-016 (and new AC-017) Task cross-refs.

### NITPICK (2, advisory, pre-existing — not v1.3 regressions)
- `sh.` receiver (driver's own Publisher/SessionAuth/AccessNode triple) used in the H3 provisioning block without being defined; identical in both artifacts, predates v1.3.
- `decodeRTID` written 1-value in some prose vs 2-value canonical — subsumed by B-1's fix.

## Process-gap disposition (S-7.02)
B-4 is tagged `[process-gap]` by Lens B (loud-failure fix applied to one leg not propagated to the sibling leg — a partial-fix-propagation discipline gap). It is being fixed IN this story (AC-017), so it is codified as a content fix within the cycle, not deferred. No open follow-up story or deferral required for B-4 provided the AC-017 fix lands. No other process-gap findings this round.

## Routing
- **architect (note v1.4):** B-1 (decodeRTID in note canonical block + prose), B-2 (window rationale), B-3 (sound injection method + name the seam), N-1 (delete contradiction).
- **story-writer (story v1.4):** B-1 (story call sites), B-3 (AC-016 rewrite), B-4 (new AC-017 + upstream error check), A-N2 (changelog), C-LOW1 (Task cross-refs), + transcribe note v1.4 decisions. AC count 16→17.
- Trend: R1 = 1 HIGH + 6 MED + 2 LOW; R2 = 5 MED + 2 LOW. Core design sound both rounds. Converging.
