---
# S-BL.LOOPBACK-FULLSTACK Step-4.5 Re-Review — Round 4 (2026-07-22)

**Reviewed tip:** story `fb86689` (v1.5, input-hash 50d0b05, 17 ACs) + placement-note `8ad90ad` (v1.5).
**Reviewers:** 3 fresh-context diverse-lens adversaries (A spec-fidelity, B technical-soundness, C traceability). All verified dispatch-integrity markers (stale-version tripwire list 1.1-1.4 + hashes absent from frontmatter).
**Verdict:** NOT CLEAN (2 LOW + nitpicks). Convergence counter reset to 0/3. All findings disk-verified (PAT-04).

## Round-3 findings — ALL confirmed cleared in v1.5
All 3 lenses independently confirmed F-B-1 (tick-free startLoopbackTicker), F-1 (Task-11 decodeRTID 2-value + honest changelog), F-B-4 (errCh dropped), F-2/C (Task 14 ref), F-2/A (AC-017 Task 5+13), F-B-2/F-B-2b cleared. Lens B reconfirmed the F-B-1 tick-free rewrite is SOUND: race-free (every HC access under its per-direction mutex), deadlock-free (no nested lock acquisition — mutex released after Tick, before arq calls), double-tick eliminated, AC-016 seam type-checks. Lens C confirmed changelog HONESTY — the R3 false-claim pattern did NOT recur; every v1.5 changelog claim verified present.

## Round-4 findings

### LOW (2) — reset the counter
- **F-B-LENSB-01 (LOW, Lens B — RESIDUAL, pre-v1.5)** — upstream ticker start-timing is inconsistent with AC-017's own test. Story L404 / note L1182 say "The upstream ticker MAY start at construction (it has no EnqueueSend dependency)." AC-017's fault-injection test (story L868-875) triggers ErrConsoleNotFound by calling SendKeystroke BEFORE CreateSession — which only surfaces via failLoud if the upstream ticker is ALREADY RUNNING to process the enqueued keystroke. An implementer who starts the upstream ticker at CreateSession (symmetric with downstream's M2 lazy start) would make AC-017's test enqueue-then-never-process → hang to timeout instead of firing failLoud. The design permits a choice that breaks its own AC. FIX (architect design call): resolve the tension — PREFERRED, mirror the B-3/AC-016 pattern: respecify AC-017's test to invoke the `onUpstreamTick()` seam DIRECTLY/synchronously (ticker-timing-independent, like AC-016 invokes onDownstreamTick() directly) — this also requires naming onUpstreamTick() as a directly-callable seam (it already exists as a method in §H2). ALTERNATIVE: make "upstream ticker MUST start at construction" binding. Story-writer transcribes the chosen design into AC-017 + the upstream-ticker-start spec.
- **L-C-1 / F-A-2 (LOW per Lens C, NITPICK per Lens A — RESIDUAL, incomplete propagation of F-B-1 rename)** — note §H2 concrete-shape block comment lead-ins L1044 (`// In upstream ticker onTick callback:`) and L1060 (`// In downstream ticker onTick callback:`) still use the retired `onTick` descriptor after F-B-1 renamed the ticker parameter to `tickBody func()`. The methods below them (`onUpstreamTick()`/`onDownstreamTick()`) are correctly named and correct in body; only the comment word "onTick" is stale. Blast radius 1 file (story does NOT reproduce these lead-ins). Not a changelog overclaim (no v1.5 row claims to have edited these). FIX (architect, note-only): reword L1044/L1060 to reference `tickBody` / the seam method names.

### NITPICK (2, advisory)
- **F-A-1 (NITPICK, Lens A — RESIDUAL edge)** — RoundTrip doc-comment purpose sentence (story L447 / note L337, byte-identical): "callers can assert decodeRTID(payload) == rt.id (AC-014 load-bearing part)" retains the 1-value code token. The binding/transcribable 2-value assertion is correct immediately adjacent (story L468/L804, note L361). Semantic-English carve-out; flagged because it contains a copyable code token. FIX (both, cosmetic): reword to "the payload decodes to rt.id" or make it 2-value.
- **F-B-LENSB-02 (NITPICK, Lens B — RESIDUAL, shipped-idiom)** — `failLoud`=`t.Errorf` from a ticker goroutine could hit Go's "Log after Test completed" panic in the narrow window after the test body returns but before `t.Cleanup(env.Close)` runs `close(closeCh)`. Does not manifest in happy path (OnAck never errors post-return; empty ticks never failLoud); identical shape to shipped AttachConsole/AttachProbe goroutines. NO CHANGE required; documented for awareness. (If the architect wishes, add one sentence noting teardown ordering, but this matches established house pattern.)

## Process-gap disposition (S-7.02)
No `[process-gap]`-tagged findings this round. Nothing to defer.

## Trend
v1.1 spec-review: 1 BLOCKER + 3 HIGH. R1(v1.2): 1 HIGH + 6 MED + 2 LOW. R2(v1.3): 5 MED + 2 LOW. R3(v1.4): 1 HIGH + 1 MED + 2 LOW. R4(v1.5): 2 LOW + 2 NITPICK — zero HIGH, zero MED. Strong convergence: the design is settled (F-B-1 tick-free rewrite confirmed sound by Lens B); R4's findings are edge-consistency + doc-comment drift. The decodeRTID straggler is at ZERO live code sites. Next round (R5, on v1.6) has a strong chance of being the first clean pass; 3 consecutive clean passes still required.

## Routing
- **architect (note v1.6):** F-B-LENSB-01 (upstream-ticker/AC-017 design decision — prefer onUpstreamTick() direct-invocation seam), L-C-1 (note L1044/L1060 comment reword), F-A-1 (note L337 doc-comment).
- **story-writer (story v1.6):** transcribe the F-B-LENSB-01 AC-017 decision + upstream-ticker-start spec, F-A-1 (story L447 doc-comment). AC count stays 17 (no new AC unless the architect's AC-017 respec adds one — expected: no).
- F-B-LENSB-02: no change (advisory).
