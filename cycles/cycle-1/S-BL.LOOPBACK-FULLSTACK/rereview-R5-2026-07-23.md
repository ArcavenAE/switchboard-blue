---
# S-BL.LOOPBACK-FULLSTACK Step-4.5 Re-Review — Round 5 (2026-07-23)

**Reviewed tip:** story `23fcbef` (v1.6, input-hash 65ffc11, 17 ACs) + placement-note `e6a2fea` (v1.7).
**Reviewers:** 3 fresh-context diverse-lens adversaries (A spec-fidelity, B technical-soundness, C traceability). All verified dispatch-integrity markers (note v1.7 / story v1.6 — different versions expected; note took an extra bump for the F-A-1 straggler).
**Verdict:** NOT CLEAN (1 MED + 2 LOW + nitpicks). Convergence counter reset to 0/3. All findings disk-verified (PAT-04).

## Lens verdicts
- **Lens A (spec-fidelity): NITPICK_ONLY.** All 3 R4 findings (F-B-LENSB-01, L-C-1, F-A-1) faithfully cleared; changelog honest (no overclaim); AC-017→BC-2.01.001 traced; onUpstreamTick/onDownstreamTick seams consistent. One NITPICK (note L967, §H1 Defect-description carries a 1-value decodeRTID token — WITHIN the dated-defect-description carve-out, not a live spec token; optional cleanup).
- **Lens C (traceability): NITPICK_ONLY.** Incomplete-propagation sweep clean; the recurring decodeRTID 1-value straggler class is fully closed across both artifacts (note-L518 straggler fixed at v1.7 L519); changelog honest; all 17 ACs trace; no regression. One LOW/NITPICK (F-C-1: story v1.6 changelog verb "Transcribe note v1.6" vs status-note/frontmatter "v1.7" — pending-intent wording drift).
- **Lens B (technical-soundness): NOT CLEAN — 1 MED + 2 LOW.** Confirmed the AC-017 direct-onUpstreamTick() seam is single-goroutine sound and F-A-1/L-C-1 propagated correctly, but the v1.6 AC-017 respec exposed 3 implementability issues:

## Round-5 findings (Lens B)

### MED (1) — resets the counter
- **F-LENSB-B-01 (MED, NEW from v1.6 AC-017 respec)** — H3 provisioning-timing latitude contradicts AC-017's un-provisioned-driver premise. H3 (story L182 / note L1088) says provisioning "`LoopbackEnv.CreateSession` (or the `loopbackDriver` constructor) MUST include..." — permitting provisioning in the constructor. But AC-017's test (story L881-883) requires: build the driver, then call SendKeystroke BEFORE CreateSession, expecting `ErrConsoleNotFound` because "no console registered yet." If an implementer takes the H3 constructor option, building the driver (AC-017 step 1) provisions the console, step 2's premise is false, SendKeystroke SUCCEEDS, failLoud never fires, AC-017 step-4 assertion fails. The story contains a construction choice (permitted by H3) that breaks one of its own ACs. Asymmetric vs AC-016 (which enqueues directly into downstreamHC + drives OnAck, never calls accessNode.SendKeystroke, provisioning-independent). Sub-issue: the driver lifecycle is unpinned — the design never states whether the accessNode triple + multipath are built at NewLoopback (AC-017 works) or lazily at CreateSession (AC-017 step 1 impossible / onUpstreamTick nil-derefs upstreamMP). FIX (architect): H3 must remove the "(or the loopbackDriver constructor)" provisioning option — specify the constructor builds the Publisher/SessionAuth/AccessNode triple UNPROVISIONED, and provisioning (Publish/RegisterKey/Attach) happens ONLY in CreateSession; explicitly pin that driver+accessNode+multipath are constructable in an un-provisioned state before CreateSession (AC-017 depends on it). Story-writer transcribes.

### LOW (2)
- **F-LENSB-B-02 (LOW, NEW)** — AC-017 silently depends on SendKeystroke performing NO session-existence validation. AC-017 calls SendKeystroke before CreateSession (so with a zero/fabricated sessionID); it relies on SendKeystroke minting a RoundTrip + encoding + enqueuing (Q3) without validating the session. Consistent with Q3 but not stated as binding — an implementer adding a defensive "session must exist" guard with t.Fatalf would abort AC-017 before onUpstreamTick runs. FIX (architect/story): state that SendKeystroke does NOT validate session existence (enqueues unconditionally), because AC-017 deliberately invokes it pre-CreateSession.
- **F-LENSB-B-03 (LOW, RESIDUAL — pre-exists from AC-016's v1.4 synchronous-seam respec; AC-017 inherits it)** — the fault-injection tests (AC-016 step 5, AC-017 step 4) assert `failLoud` FIRED as a PASSING test, but failLoud=t.Errorf marks the enclosing test FAILED. Asserting failLoud fired requires constructing the driver with a spy/recording testing.TB (feasible in a white-box in-package test) rather than the real *testing.T. Neither AC specifies this. FIX (architect/story): state once (covering both AC-016 and AC-017) that the fault-injection tests construct the driver with a recording testing.TB stub so failLoud's t.Errorf is captured and asserted, not failing the enclosing test.

### NITPICK (2, advisory)
- **A-L967 (NITPICK, Lens A)** — note L967 (§H1 Defect-description) phrases the AC-014(b) requirement with the 1-value `decodeRTID(payload) == rt.id` form. Within the dated-defect-description carve-out (not a live spec token); optional cleanup to semantic English for full self-consistency.
- **F-C-1 (LOW/NITPICK pending-intent, Lens C)** — story v1.6 changelog row opens "Transcribe note v1.6" while the status-note blockquote + frontmatter pin say v1.7; frontmatter (most precise) correctly distinguishes v1.6 R4 substance + v1.7 F-A-1 propagation. Align the changelog verb to v1.7 for consistency.

## Process-gap disposition (S-7.02)
No `[process-gap]`-tagged findings this round. F-LENSB-B-01/02/03 are content/implementability defects in the AC-017 respec, fixable in-cycle.

## Trend
v1.1 spec-review: 1 BLOCKER + 3 HIGH. R1(v1.2): 1 HIGH + 6 MED + 2 LOW. R2(v1.3): 5 MED + 2 LOW. R3(v1.4): 1 HIGH + 1 MED + 2 LOW. R4(v1.5): 2 LOW + 2 NITPICK. R5(v1.6/v1.7): 1 MED + 2 LOW + 2 NITPICK. Two of three lenses (A, C) are at NITPICK_ONLY; the design is confirmed sound by Lens B every round. The remaining findings are Lens B reaching into the third-order implementability of the AC-017 direct-seam respec (each fix to AC-017 has surfaced the next layer: R3 F-B-1 tick-free ticker → R4 F-B-LENSB-01 upstream-timing → R5 F-LENSB-B-01 provisioning-timing). The decodeRTID straggler class is fully eradicated. This is a draft/unscheduled story (author-now-deliver-later); convergence has materially improved it from the v1.1 NO-GO but has not yet reached 3 consecutive clean passes.

## Routing (if convergence continues)
- **architect (note):** F-LENSB-B-01 (H3 provisioning-timing + driver-lifecycle pinning), F-LENSB-B-02 (SendKeystroke no-session-validation), F-LENSB-B-03 (recording testing.TB stub for AC-016+AC-017 fault-injection tests, in §M2), A-L967 (optional §H1 defect-desc cleanup).
- **story-writer (story):** transcribe the above into H3/AC-016/AC-017/SendKeystroke; F-C-1 (changelog verb v1.6→v1.7). AC count stays 17.
