# S-BL.LOOPBACK-FULLSTACK Step-4.5 — R22 Fresh-Context Reconvergence Pass

**Date:** 2026-08-29
**Pass:** R22 — first fresh reconvergence pass since the R21 remediation; the first CLEAN pass since the R21 v1.17 fix landed
**POL-005 dispatch tuple verified against tip:** `a86bf5d6` (`a86bf5d65db5b2eec84dfe65cdc2ccea38131067`, record commit for R21; artifacts at this tip: story v1.17, placement-note v1.15, input-hash `4902d5d`, STORY-INDEX v4.155)
**Verdict:** **CLEAN.** Convergence counter ADVANCES 0/3 → 1/3.

---

## §1.8 Oracle — GREEN

- `go build ./...` clean across all packages (exit 0).
- `go vet` clean (two invocations, plain + `-tags integration`), exit 0 both.
- AC-013 compile-gate injection re-proof sound: a deliberate compile error was injected into `internal/bench`, then hash-verified restore confirmed. Bench-file before/after HEAD-blob hash `e53ab35fbb9a61b474545547b5580e248e7d38d1` (probe restored, zero residue; orchestrator independently disk-verified). The fixed gate (`go test -tags integration -run '^$' -count=1 ./internal/bench/`) CATCHES the injection (NEW gate exit 1); the old vacuous gate (`go build -tags integration ./internal/bench/...`) MISSES it (OLD gate exit 0). F-R15-LENSB-01's fix independently reconfirmed sound yet again.
- Binding-RUN method (the AC-013 benchmark invocation) emitted `p99_rtt_ms=0.07992`.
- Mutation-honesty: N/A (spec-only draft, no implementation to mutate).

## Lens A — Spec Fidelity: CLEAN

Zero findings. Ledger parity (the R20-declared convention — L57 provenance / L72 status-note mirror the formal changelog's finding-set 1:1 per version, v1.13 forward) re-derived holding across v1.13–v1.17.

**Non-gating [process-gap] observation:** the triple-ledger parity convention (the L57 `inputDocuments` clause / L72 status-note entry / formal changelog row — three hand-maintained copies of the same finding-set, per version) is itself the dominant defect source of the recent reconvergence loop (R19/R20 both fired on ledger-parity drift, not spec-content defects). Proposes a tooling candidate: a single-source-of-truth changelog with L57/L72 summaries mechanically generated from it, rather than hand-authored in parallel. Non-gating; carried to the deferred/observation ledger for human review, not actioned this round.

Cosmetic: the parity-convention scope is phrased two ways across its two declaration sites (L57 preamble and the Changelog section header note) — both conforming, no substantive divergence, not raised as a finding.

## Lens B — Technical Soundness: CLEAN (3 non-gating observations)

Zero findings. All 7 mandated soundness claims re-derived against real `develop@2ce3a57` source; the v1.17 downstream-`OnAck` re-anchoring is complete and consistent across all 8 sites and matches real code; zero new defect introduced by the R21 fix; lock ordering acyclic.

- **O-1 (LOW):** `arq.New`'s config `DropTimeout`/`TickInterval`/`DegradationBufSize` are not pinned by the story. Harmless — defaults are safe and neither TLPKTDROP nor the degradation path is ever exercised by any AC or the AC-013 bench. A clarifying implementer line would help but is not load-bearing.
- **O-2 (LOW):** `recordingTB` overrides only `Errorf`. Latent coupling if `failLoud` were ever changed to call `Fatalf` instead — but the story already binds `failLoud=Errorf` throughout, so this is a documented assumption holding, not a live gap.
- **O-3 (INFO):** the merged AC-013 bench file still carries the pre-AC-013 `Env`-method API. Expected — this is the pre-state the story's Task 10 targets, not a defect.

## Lens C — Traceability: NITPICK_ONLY

All 9 mandated traceability sweeps CLEAN on the story body itself: 8-site propagation (R21's fix), ledger parity v1.13–v1.17, naked-line-citation sweep, version-qualifier sweep, `go build` claim sweep, phantom-branch sweep, AC→BC sweep, input-hash sweep, cross-artifact sweep.

One LOW finding + one observation, **both against the placement note's L358-370 subsection** (not the story):

- **F-R22-LENSC-01 (LOW):** note L363 ("once per received (post-dedup) downstream frame") reads as the exact framing the story's v1.17 changelog disowns (R21's fix explicitly dropped this "post-dedup" cadence language from the story's Design Constraints prose, replacing it with the tick-body-after-`Send` framing).
- **O-1 (accompanying observation):** the L358 subsection heading still says "`arqClient.OnAck` call-contract decision," a stale pre-single-instance-topology name (per the AC-001 REVISED verdict, the driver has one shared `downstreamARQ`, not a distinct `arqClient`), and the surrounding prose carries a "flagged for architect sign-off" marker from before AC-001 was discharged.

## Architect adjudication — F-R22-LENSC-01 + O-1: NON-DEFECT / documented survivor

**Ruling: (A) NON-DEFECT.** Both items are **RETAINED pre-Addendum-Q4 body text**, sitting under the L271-285 AC-001-supersession banner. The staleness is fully absorbed by three independent facts:

1. The corrected pseudocode governing actual implementation sits *above* this subsection (the Q4 diagram, already repaired at R21) — no reader building the driver from the operative artifact encounters the stale L358-370 text as an instruction.
2. The Q4 Addendum's own cadence-consistent "one frame per tick" language is what a reader following the story's own internal cross-references is directed to; the stale subsection is superseded prose, not a live second source of truth.
3. Addendum Constraint #4 explicitly delegates this exact wording-correction to story-writer scope — and R21's 8-site fix DISCHARGED that delegation in the story's own body. The note's L358-370 subsection was never in scope for that discharge; it is retained historical narrative by design, the same class as the frozen v1.x changelog rows elsewhere in both documents (§2.9 convention: frozen prose is not edited in place).

No reader building from the operative artifact (the story) is misled by the note's retained pre-Addendum wording. **Note NOT edited.** Input-hash stays `4902d5d`. No cascade into the story.

## Defect taxonomy

Not applicable — round verdict is CLEAN. F-R22-LENSC-01 and its accompanying O-1 are adjudicated NON-DEFECT (documented survivor), not a genuine defect of either artifact.

## Round verdict

**CLEAN. Convergence counter ADVANCES 0/3 → 1/3.** Zero spec artifacts changed this round — story stays v1.17/`4902d5d`, note stays v1.15, STORY-INDEX administrative-only bump to v4.156 (progress-recording, not a content edit). This is the round the counter legitimately starts climbing again after the R21 fix.

## Orchestrator disk-verifications (all independently confirmed)

- `.factory` (factory-artifacts) HEAD unchanged at `a86bf5d65db5b2eec84dfe65cdc2ccea38131067` through all four legs and the architect adjudication (no commit landed until this state-manager burst).
- Probe-restore hash `e53ab35f...` confirmed against the on-disk `internal/bench` file post-restore — zero residue from the injection probe.
- Story frontmatter confirmed unchanged: `version: "1.17"`, `input-hash: "4902d5d"`.
- Placement-note frontmatter confirmed unchanged: `version: "1.15"`.
- STORY-INDEX.md confirmed unchanged at `version: "4.155"` prior to this burst's administrative bump.
- Working tree porcelain confirmed to show ONLY the two known-disjoint auto-files (`regression-state.json`, `sidecar-learning.md`) prior to this burst's writes.

## Full survivor ledger carried forward to R23+

F-ORACLE-R9-01, F-LENSA-R13-01, R14-O-1, R11 Lens B O-1, OBS-LENSB-R10-DBLCREATESESSION, DRIFT-BC-2.02.002-ARCH-03-DEDUP-KEY (routed separately in Open Drift Items), OBS-R16-LENSA-DATE, F-R16-LENSC-01, OBS-R17-LENSA-01, R18-EQUIV-L256-SWEEP, O-1-STORY-INDEX-COUNT-DRIFT, R19-M2-REJECTED-OPTION-LOOSE-BOUND, LEDGER-PARITY-CONVENTION-DECLARED, O-R20-LENSB-01, O-R20-LENSB-02, plus **new this round**: **SURVIVOR-R22** (note L358-370 `arqClient.OnAck` subsection — banner-superseded retained pre-Addendum reasoning, architect-ruled (A) NON-DEFECT; future fresh-context lenses re-flagging this same L358/L363 language are dispositioned (A) NON-DEFECT WITHOUT resetting the convergence counter — revisit only if evidence surfaces that an implementer consumed the raw note in isolation, bypassing the story, and produced dedup-gated `OnAck` code), Lens A's `[process-gap]` ledger-tooling observation (non-gating, for-human-review), Lens B O-1/O-2/O-3 (non-gating, for-human-review).

## Next

R23 fresh-context 4-leg rig (§1.8 Oracle + 3 diverse lenses A/B/C) against the tip after this state-manager's record commit for R22 — story v1.17 (unchanged), placement-note v1.15 (unchanged), input-hash `4902d5d` (unchanged), STORY-INDEX v4.156 — carrying the POL-005 dispatch-integrity tuple. A fresh-context lens re-flagging the SURVIVOR-R22 L358/L363 note language is pre-dispositioned (A) NON-DEFECT per this record and does NOT reset the counter. Needs 2 more consecutive clean-or-better passes (R23/R24) to reach 3/3 and reconverge, THEN a re-run of the §1.7 consistency audit against v1.17, THEN the human approval gate.
