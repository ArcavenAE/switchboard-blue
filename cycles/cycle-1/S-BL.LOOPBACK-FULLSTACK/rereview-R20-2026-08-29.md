# S-BL.LOOPBACK-FULLSTACK Step-4.5 — R20 Fresh-Context Reconvergence Pass

**Date:** 2026-08-29
**Pass:** R20 — second fresh reconvergence pass since the R18 remediation (following R19 NOT CLEAN)
**POL-005 dispatch tuple verified against tip:** `1c8c0924` (record commit for R19; artifacts at this tip: story v1.15, placement-note v1.15, input-hash `4902d5d`, STORY-INDEX v4.153)
**Verdict:** **NOT CLEAN.** Convergence counter STAYS 0/3.

---

## §1.8 Oracle — GREEN

- `go build ./...` and `go vet -tags integration` clean across all packages.
- AC-013 compile-gate soundness re-proven a **fifth** time via fresh injection: a deliberate compile error was injected into `internal/bench`, then hash-verified restore confirmed (`e53ab35f`). The fixed gate (`go test -tags integration -run '^$' -count=1 ./internal/bench/`) catches the injected error (exit 1); the old vacuous gate (`go build -tags integration`) misses it (exit 0). F-R15-LENSB-01's fix independently reconfirmed sound a fifth consecutive time.
- Binding RUN method emits `p99_rtt_ms=0.1258`.
- Mutation-honesty: N/A (spec-only draft, no implementation to mutate).

## Lens A — Spec Fidelity: NOT CLEAN

- **F-R20-LENSA-01 (MED, NEW):** The story's `inputDocuments` L57 provenance comment's v1.14 clause omitted item **O-2**, though O-2 is present at L72 (status-note entry) and in the formal changelog table's v1.14 row. A ledger-completeness gap — L57 under-enumerated relative to its own sibling surfaces.
- **F-R20-LENSA-02 (LOW, NEW):** F-R19-LENSB-01 is labeled MED at L57/L72, but LOW is the authoritative severity (per the R19 record and cycle-record `rereview-R19-2026-08-29.md`, where F-R19-LENSB-01 was dispositioned LOW). A severity-transcription mismatch between the ledger surfaces and the authoritative pass record.

## Lens B — Technical Soundness: CLEAN

Zero findings. The **spec content itself has converged on technical soundness** — this is the load-bearing result of this pass. All previously-scrutinized technical anchors were re-derived SOUND against real source on this pass:

- AC-013 compile-gate (the fixed gate genuinely exercises the target benchmark; re-confirmed via the fifth injection-probe above).
- AC-017's in-place `failLoud` check (both in the Q3/Q4 diagrams and in the underlying `multipath.Send`/`SendKeystroke` call sites).
- §M2's window-safety math (`chanSeq=E+1` per `halfchannel.Tick()`'s pre-increment, the off-by-one fixed at v1.14 remains correctly stated).
- Dedup semantics (`multipath.Send`'s `sent>=1⇒nil` aggregation, the upstream `failLoud` placement fixed at v1.14 remains sound against it).
- Acyclic lock-ordering (per-direction mutex discipline, §H2).
- The v1.15 Q3-diagram propagation (the in-place `failLoud` step added at R19 was independently re-verified correct and consistent with the Q4 diagram).

Two non-gating observations recorded (not findings — do not gate; carried to the survivor ledger):

- **O-R20-LENSB-01** (documented non-defect): the downstream `OnAck` placement differs structurally between the note's Q4 diagram (ticker-body, after `Send`) and the story's Q4/AC-005 prose (reads as inside `deliverUpstream`/per-path). Both were independently verified to produce `OnAck` count==1 (idempotent — the `arq.go:220` guard passes at `chanSeq−nextExpected==0`), so neither form is a correctness defect. The note's ticker-body form governs (per its binding-rule status). A one-line pin in the story clarifying this is optional and non-gating; this is not a correctness bug and should not be re-litigated as one.
- **O-R20-LENSB-02** (NITPICK, documented): the `loopbackSink.SendInput` code sketch omits the `downstreamHCMu` acquisition comment that H2/Task 6 bind correctly elsewhere in the story. Cosmetic — an implementer following Task 6's binding prose gets the locking right regardless. Non-gating.

**This CLEAN Lens B result is the pass's central signal:** across R18→R19→R20, every technical-soundness finding has now been resolved and re-derived sound on fresh re-review. The residual NOT CLEAN verdict for R20 as a whole is entirely a ledger-completeness/traceability class of defect (Lens A + Lens C), not a content-soundness defect.

## Lens C — Traceability: NOT CLEAN

- **F-R20-LENSC-01 (MED, NEW):** Identical defect to F-R20-LENSA-01 — L57's v1.14 clause omits O-2, present at L72 and the formal changelog. Lens C independently derived this from the traceability angle (L57 provenance comment must trace 1:1 to the formal changelog row it summarizes) without visibility into Lens A's parallel finding.
- **F-R20-LENSC-02 (MED, NEW):** Identical defect to F-R20-LENSA-02 — the F-R19-LENSB-01 severity mismatch (MED at L57/L72 vs LOW authoritative), independently derived from the traceability angle (severity labels cited in ledger rows must trace to the authoritative pass-record disposition).

Lens A and Lens C **independently corroborated the same two underlying defects** via different reasoning paths (spec-internal-fidelity vs cross-artifact-traceability) — a strong signal both are genuine, not lens-specific false positives.

- **[process-gap] observation (Lens C):** no declared rule exists for what L57's provenance comment must enumerate relative to the formal changelog table. This is the **root condition** generating the recurring ledger-drift class seen across R19 (F-R19-LENSA-01: L57/L72 omitted F-R18-LENSB-02) and now R20 (L57 omits O-2, plus the severity-transcription slip). Without a declared parity rule, each remediation burst re-derives what L57/L72 "should" contain from scratch and drifts independently from the formal changelog.

## Orchestrator adjudication (PAT-04)

All four findings (F-R20-LENSA-01/02, F-R20-LENSC-01/02) accepted as genuine — two underlying defects, each independently corroborated by two lenses via different reasoning paths, both disk-verified against the live story text and the formal changelog table. No finding downgraded to preserve a wanted convergence. Lens B's CLEAN verdict + two non-gating observations independently disk-verified and accepted as-is.

**PASS VERDICT: NOT CLEAN.** **Convergence counter: STAYS 0/3** — counter was already 0/3 from R18's reset; a fresh pass surfacing gating findings, and the remediation below independently editing the converging artifacts, both independently hold it at 0/3.

## Remediation (same session, story-writer commit `2a0439b8`)

Story v1.15→v1.16 + STORY-INDEX v4.153→v4.154 (`2a0439b8f87b3e8008669d7b0768d5156530be98`):

- **L57 backfilled with O-2** — the v1.14 provenance clause now enumerates all 4 items, matching the formal changelog's v1.14 row.
- **F-R19-LENSB-01 severity corrected MED→LOW** in both L57 and L72, matching the authoritative pass-record disposition (closes F-R20-LENSA-02 / F-R20-LENSC-02).
- **Root-cause fix — declared a ledger-parity convention:** L57 (provenance comment) and L72 (status-note entry) now mirror the formal-changelog row's finding-set 1:1, per version, from v1.13 forward. Versions before v1.13 are grandfathered (pre-dating the convention's practical enforceability). This directly addresses Lens C's `[process-gap]` observation — future remediation bursts have a declared rule to check ledger parity against, rather than re-deriving completeness from scratch each pass.
- **Reconciled the v1.13 clause under the new convention:** L57's v1.13 entry was found to also omit findings present in the formal changelog (F-R15-LENSA-01, F-R15-LENSC-01) — added to bring it into compliance with the newly-declared convention.
- **Frozen v1.15 formal-row left unedited per §2.9** (the v1.15 formal changelog row's own false-parity claim, dated and frozen, is not rewritten in place); the correction instead lands forward — the v1.16 changelog row states the correction, and the L57/L72 v1.15 entries carry a softened cross-reference noting the retroactive fix, rather than rewriting frozen historical prose.

The placement-note is **UNCHANGED at v1.15** — this round's findings were entirely story/STORY-INDEX-side ledger defects; the note was CLEAN this round (no finding touched note content). Input-hash is correspondingly UNCHANGED at `4902d5d` (no note-side edit to trigger recomputation).

## Theme (session-review note, not a follow-up story)

The ledger-drift class recurred for three consecutive rounds: R18 remediation → R19 finding (L57/L72 omission) → R19 remediation → R20 finding (L57 omission + severity slip). Root cause: the story carries **four parallel, un-reconciled ledger surfaces** (L57 provenance comment, L72 status-note, the formal changelog table, and STORY-INDEX's own row) with no declared parity rule between them — each remediation burst was manually re-deriving what each surface "should" say, and drifting.

R20 addressed the root cause directly: the parity convention declared in this remediation (L57/L72 mirror the formal changelog 1:1 per version, v1.13 forward) closes the generating condition, not just this round's two symptom findings. This is judged a **content/spec-artifact-design issue specific to this story's over-complex ledger apparatus**, not a vsdd-factory engine defect — resolved in-cycle via the parity-convention fix, with no follow-up story or upstream issue warranted.

## Survivor ledger — three new items this round

1. **LEDGER-PARITY-CONVENTION-DECLARED** (resolved-in-cycle, root-cause fix) — closes the recurring ledger-drift class going forward; future passes check ledger parity against this convention rather than re-deriving from scratch. The Lens C `[process-gap]` observation is closed by this fix.
2. **O-R20-LENSB-01** (documented non-defect) — `OnAck` placement structural difference between note Q4 (ticker-body) and story Q4/AC-005 (deliverUpstream-adjacent); both verified idempotent (count==1); note's ticker-body form governs; non-gating, do not re-litigate as a correctness bug.
3. **O-R20-LENSB-02** (NITPICK) — `loopbackSink.SendInput` code sketch omits the `downstreamHCMu` acquisition comment carried correctly elsewhere (H2/Task 6). Cosmetic, non-gating.

## Full survivor ledger carried forward to R21+

F-ORACLE-R9-01, F-LENSA-R13-01, R14-O-1, R11 Lens B O-1, OBS-LENSB-R10-DBLCREATESESSION, DRIFT-BC-2.02.002-ARCH-03-DEDUP-KEY (routed separately in Open Drift Items), OBS-R16-LENSA-DATE, F-R16-LENSC-01, OBS-R17-LENSA-01, R18-EQUIV-L256-SWEEP, O-1-STORY-INDEX-COUNT-DRIFT, R19-M2-REJECTED-OPTION-LOOSE-BOUND, plus the three new items above.

## Next

R21 fresh-context 4-leg rig (§1.8 Oracle + 3 diverse lenses A/B/C) against the tip after the state-manager's record commit for R20 — story v1.16, placement-note v1.15 (unchanged), input-hash `4902d5d` (unchanged), STORY-INDEX v4.154 — carrying the POL-005 dispatch-integrity tuple. Needs 3 consecutive clean-or-better passes (R21/R22/R23) to reconverge, THEN a re-run of the §1.7 consistency audit against v1.16, THEN the human approval gate.
