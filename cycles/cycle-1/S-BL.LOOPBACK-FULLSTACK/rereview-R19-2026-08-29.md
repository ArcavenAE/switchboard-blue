# S-BL.LOOPBACK-FULLSTACK Step-4.5 Adversarial Re-Review — Round 19 (2026-08-29)

## POL-005 Dispatch-Integrity Tuple

- **repo_path:** `/Users/skippy/work/aae-orc/run/switchboard-blue/.factory`
- **branch:** `factory-artifacts`
- **expected_head / verified_head:** `7b8b655ade292bd93e971c08bbdcace461590103` (confirmed via `git -C <abs> rev-parse HEAD` before dispatch)
- **artifacts under review:** story `S-BL.LOOPBACK-FULLSTACK` v1.14 (input-hash `06d5209`); placement-note v1.14; STORY-INDEX v4.152
- **known-disjoint auto-maintained mods (disclosed, not touched):** `.factory/sidecar-learning.md`, `.factory/regression-state.json`

## Rig

Four-leg diverse-lens rig: verification oracle (§1.8 gates + citation check + a fourth compile-gate soundness re-probe) + three independently-dispatched fresh-context adversarial lenses (spec-fidelity, technical-soundness, traceability). First fresh reconvergence pass since the R18 remediation (counter was reset 2/3→0/3 by R18). Reviewed tip: story v1.14 / placement-note v1.14 / input-hash `06d5209` / STORY-INDEX v4.152 at factory tip `7b8b655ade292bd93e971c08bbdcace461590103`.

## Findings

### §1.8 Oracle (devops-engineer) — GREEN

`go build ./...` and `go vet -tags integration ./...` both clean.

**Compile-gate soundness re-proven a fourth time.** The Oracle re-ran the R16/R17/R18 independent-derivation approach rather than trusting any prior round's own probe: a fresh injection probe against `internal/bench`, hash-verified restore afterward (restored-content hash `e53ab35f`).

- Old gate (`go build -tags integration ./internal/bench/...`) against the injected error: **exit 0** — misses it, consistent with F-R15-LENSB-01's original vacuity finding.
- New gate (`go test -tags integration -run '^$' -count=1 ./internal/bench/`) against the same injected error: **exit 1** — catches it.
- File restored to its pre-injection content; hash re-verified equal to the pre-probe hash (`e53ab35f`).

The binding RUN method independently emits `p99_rtt_ms=0.1293`. Citations accurate. Mutation-honesty check N/A — spec-only draft, no shipped scaffolding to mutate.

Zero findings.

### Lens A — spec-fidelity — NOT CLEAN

**NEW finding F-R19-LENSA-01 (MED):** the story's `inputDocuments` L57 provenance comment and the L72 status-note v1.14 changelog entry each OMITTED F-R18-LENSB-02 (the §M2 window-safety off-by-one) though the formal changelog table (row "1.14") and STORY-INDEX both carried it — recurring the R11 status-note-drift class (a fix landing in the formal changelog row without being backfilled into the narrative provenance comment and the in-body status note). Both sites corrected to list all three v1.14 items (F-R18-LENSC-01, F-R18-LENSB-01, F-R18-LENSB-02).

**Non-gating O-1 (pre-existing, disclosure only):** STORY-INDEX.md L134 header ("Backlog: 14 active") contradicts "13 active" at L36/L23 and L134's own reconciliation note; separately the L36 "active" enumeration still counts stories delivered elsewhere (S-BL.DISCOVERY-WIRE PR #128, S-7.04-FU-DRAIN-WIRE PR #120). Pre-existing, NOT caused or touched by this burst — deferred to a separate PAT-05 STORY-INDEX reconciliation, out of LOOPBACK scope. See `O-1-STORY-INDEX-COUNT-DRIFT` in the Open Drift Items ledger.

### Lens B — technical-soundness — NITPICK_ONLY

**NEW finding F-R19-LENSB-01 (LOW):** the Q3 upstream-flow diagram's `SendKeystroke` step lacked the in-place `failLoud` check that AC-017/Task 5/Task 13/the note's own Q3 prose/the Q4 downstream diagram all already carry — an incomplete propagation of the v1.14 F-R18-LENSB-01 fix (the fix reached every prose site but missed the diagram itself). Corrected: the diagram now shows the in-place `failLoud` step immediately after `driver.accessNode.SendKeystroke`, mirroring the Q4 downstream diagram's symmetric `if err != nil { driver.failLoud(err); return }` pattern.

All 6 technical anchors otherwise re-derived SOUND against real source: `multipath.Send`'s `sent>=1⇒nil` aggregation semantics, the in-place upstream `failLoud` placement (F-R18-LENSB-01's fix holds), the §M2 window-safety "plus one" correction (F-R18-LENSB-02's fix holds and is arithmetically sound against `halfchannel.Tick()`'s pre-increment), AC-016's `E=65→chanSeq=66` boundary, AC-017's single-goroutine-by-construction guarantee, and the `recordingTB` embed-real-t contract.

**Non-gating observation, R19-M2-REJECTED-OPTION-LOOSE-BOUND (documented non-defect):** the §M2 rejected-option prose describing the discarded construction-time-start alternative ("more than 64 ... intervals later") is a loose approximation of the exact `E>=64` boundary. It sits in DISCARDED-option prose, not the binding design; Lens B examined it explicitly and ruled it immaterial/non-defect. Not carried as an open item — do not re-litigate.

### Lens C — traceability — NOT CLEAN

**NEW finding F-R19-LENSC-01 (MED):** a stale naked cross-artifact citation, "story L442", stood in the placement-note's live §M2 `sync.Once` prose (note `L1459`), resolving on disk to unrelated content — `closeOnce sync.Once`'s actual call sites are at story `L498`/`L666`, not `L442`. This falsified the v1.14 changelog row's completeness claim ("Whole-file grep found no other naked `L<digits>` binding-rule citations in live prose"): that sweep scoped itself to binding-rule-authority citations (the `L256` mutex-rule class) and did not cover this citation, which functions as an example/navigational pointer, not a binding-rule citation — a real gap in the v1.14 sweep's stated scope. Corrected to the clean parenthetical `` (`closeOnce sync.Once`) `` — dropping the naked cross-reference entirely — matching the already-clean form at note `L1808` and story `L498`.

A full whole-class re-sweep (every naked `story L<digits>` / `note L<digits>` / bare `L<digits>` citation in the note, ~90 occurrences) was performed as part of the remediation: this one live-prose naked citation (fixed); the frozen v1.6-section `story L868-875` AC-017 citation (dated repair-section provenance, left untouched per §2.9); the remainder are either disk-verified external-source `file.go:NNN`/`ARCH-03 line NNN` anchors or sit inside frozen dated changelog rows / repair-addendum sections describing historical defects at the line numbers then current. Zero live-prose naked line citations remain anywhere in the note.

## Orchestrator Adjudication (PAT-04)

R19 end-state independently verified against dispatch tip `7b8b655a`: factory tip unchanged throughout the review window; worktree clean except the two disclosed disjoint auto-maintained files (`sidecar-learning.md`, `regression-state.json`); all three findings (F-R19-LENSA-01 MED, F-R19-LENSC-01 MED, F-R19-LENSB-01 LOW) independently disk-verified against the live story/note text and the cited source lines — none downgraded to preserve a wanted convergence.

**PASS VERDICT: NOT CLEAN — 2 MED + 1 LOW across Lens A, Lens B, and Lens C.** §1.8 Oracle CLEAN.

**Convergence counter: STAYS 0/3** — the counter was already 0/3 from R18's reset; a fresh pass finding gating findings AND the remediation editing the converging artifacts both independently keep it at 0/3 (no positive count to lose).

**Common theme across all three R19 findings:** every one is an incomplete-propagation residual of the v1.14 (R18) remediation — each v1.14 fix cleared its cited sites but left a sibling site unswept (L442 outside the L256 sweep's declared scope; L57/L72 provenance/status-note omitted from the formal-changelog backfill; the Q3 diagram outside the AC-017/Task-5/Task-13 prose propagation). This is a content-quality observation about remediation completeness, not an engine or process defect — no follow-up story is warranted; the disposition is a suggestion that future remediation bursts include a whole-class propagation sweep as standard practice, recorded here for visibility.

## Remediation (same session, two signed commits)

- **architect** — placement-note v1.14 → v1.15, commit `5d33a263981e64e78534f20106c9575a9bb7ac2d`: de-anchored the stale `story L442` citation at note `L1459` to the clean parenthetical `` (`closeOnce sync.Once`) `` matching note `L1808`/story `L498` — closes F-R19-LENSC-01. Performed the whole-file naked-line-citation sweep (classified every citation; confirmed `L868-875` is a frozen v1.6-section citation and all other instances are either changelog/provenance edit-location refs or disk-verified external-source anchors; zero live-prose naked line citations remain).
- **story-writer** — story v1.14 → v1.15 + STORY-INDEX v4.152 → v4.153, commit `9e8954d866452c4199e6bc529bfc9765d2cefd89`: backfilled the `inputDocuments` L57 provenance comment and the L72 status-note v1.14 entry with the missing F-R18-LENSB-02 item, matching the formal changelog's v1.14 row — closes F-R19-LENSA-01. Added the in-place `failLoud` step to the Q3 upstream-flow diagram (mirrors Q4's downstream diagram and AC-017/Task 5/Task 13's already-correct prose) — closes F-R19-LENSB-01. Advanced 4 additional live note-version pins v1.14→v1.15 (version-qualifier-drift propagation). Input-hash recomputed `06d5209` → `4902d5d` (orchestrator-independently-recomputed, confirmed). STORY-INDEX backlog-count aggregates untouched (O-1 scope guard held — this burst does not fix STORY-INDEX's pre-existing count drift). AC count unchanged (17), points unchanged (8).

## Survivor / Deferred Ledger (carried forward to R20 and the eventual human approval gate)

| ID | Severity | Description | Disposition |
|----|----------|--------------|-------------|
| F-ORACLE-R9-01 | below-LOW | placement-note L476 `NewWithRouters` line-ref cites `testenv.go:454`; real line is 452 (off-by-2) | deferred to implementation-time re-grounding |
| F-LENSA-R13-01 | NITPICK | STORY-INDEX.md changelog version-sequence discontinuity — v4.145 row cites a missing 4.144 predecessor | INDEX-GLOBAL, pre-existing (R5-era); route to a post-convergence index-hygiene sweep |
| R14-O-1 | below-LOW | STORY-INDEX L155 prose "17 ACs total post-R5 remediation" imprecision | INDEX-GLOBAL, defensible-as-written non-defect; same index-hygiene sweep as F-LENSA-R13-01 |
| R11 Lens B O-1 | informational | `multipath.Send` error-swallow when ≥1 path succeeds | adjudged non-defect (distinct from F-R18-LENSB-01) |
| OBS-LENSB-R10-DBLCREATESESSION | observation | no `sync.Once`/idempotence guard against a double-`CreateSession` call | adjudged non-defect — by-design single-call contract; for-human-review-at-approval-gate |
| OBS-R16-LENSA-DATE | below-LOW | placement-note v1.13 changelog row dated 2026-08-28, story/STORY-INDEX v1.13 rows dated 2026-08-29 (session-clock midnight rollover) | disclosure-only, non-defect — honest sequencing artifact, carry to human approval gate |
| F-R16-LENSC-01 | non-defect | story vs placement-note AC-013 framing latitude ("must compile the TEST BINARY" vs "convenience pre-check"); command text identical | disclosure-only, no divergence — not a finding |
| OBS-R17-LENSA-01 | non-gating, non-defect | story AC-013 names the metric `p99_rtt_ms` explicitly, placement-note refers to it generically as "the p99 metric" | disclosure-only, authorial-specificity latitude, same class as F-R16-LENSC-01 — carry to human approval gate |
| R18-EQUIV-L256-SWEEP | non-gating, procedural note | the F-R18-LENSC-01 de-anchoring touched 2 `L256` citations inside the placement-note's v1.5-R3-narrative section (part of the note's 4 total note-side sites) | citation-correctness sweep only (v1.12 Finding #1 multi-section precedent) — naked line numbers replaced with mechanism anchors; NOT a substantive historical rewrite; do not re-litigate |
| O-1-STORY-INDEX-COUNT-DRIFT | deferred, out-of-LOOPBACK-scope | STORY-INDEX L134 header "Backlog: 14 active" contradicts "13 active" at L36/L23; separately L36's "active" enumeration still counts stories delivered elsewhere (S-BL.DISCOVERY-WIRE PR #128, S-7.04-FU-DRAIN-WIRE PR #120) | pre-existing, NOT caused/touched by the LOOPBACK v1.15 burst — deferred to a separate PAT-05 STORY-INDEX reconciliation; do not gate LOOPBACK on it |
| R19-M2-REJECTED-OPTION-LOOSE-BOUND | documented non-defect | §M2's rejected construction-time-start prose ("more than 64 ... intervals later") is a loose approximation of the exact `E>=64` boundary, in DISCARDED-option prose | Lens B (R19) examined and explicitly ruled immaterial/non-defect; do not re-litigate |

## Convergence Trajectory

R16 (CLEAN — 0/3→1/3) → R17 (CLEAN — 1/3→2/3) → R18 (NOT CLEAN — 2 MED + 1 LOW, remediated v1.14, counter RESET 2/3→0/3) → **R19 (NOT CLEAN — 2 MED + 1 LOW, remediated v1.15, counter STAYS 0/3)** → R20 next.

All three R19 findings are incomplete-propagation residuals of the v1.14 (R18) remediation, not late-arriving repeats of any earlier-round class — a distinct failure mode from R15/R16/R17/R18's genuinely-new design or arithmetic defects. Needs three fresh consecutive clean-or-better passes (R20/R21/R22) against the v1.15 artifacts to reconverge, THEN a re-run of the §1.7 consistency audit against v1.15, THEN the human approval gate.
