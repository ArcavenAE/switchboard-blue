# S-BL.LOOPBACK-FULLSTACK Step-4.5 R29 Reconvergence — NOT CLEAN → counter RESETS 1/3→0/3

**Date:** 2026-08-29
**Dispatch:** POL-005 four-leg fresh-context rig (§1.8 Oracle + 3 diverse lenses A/B/C), dispatched concurrently against `.factory` HEAD `4c4671030b2e6c5f0767f18f229bd47f6076ca28` (the R28 record commit) — story v1.18/`4902d5d`, placement-note v1.15 UNCHANGED, STORY-INDEX v4.163.

## Verdict

**NOT CLEAN** — three of four legs CLEAN; Lens C raised one MEDIUM traceability finding (F-C-01). Second pass of the 3-consecutive-clean-pass streak begun at R28 does NOT complete; the streak resets.

## Per-leg results

**§1.8 Oracle: CLEAN.** Executed the AC-013 compile-gate injection re-proof: the sound gate `go test -tags integration -run '^$' -count=1 ./internal/bench/` CAUGHT a deliberately injected compile error; the old vacuous gate `go build -tags integration ./internal/bench/...` MISSED it — independently re-confirms F-R15-LENSB-01's fix sound. Bench probe file restored to HEAD-blob `e53ab35f`, no residue leaked to the working tree.

**Lens A (spec-fidelity + registry-conformance): CLEAN.** Zero findings. Registry-conformance exact against ARCH-INDEX (`subsystems: [session-networking, multipath-forwarding]`). Triple-ledger parity holds — L57 provenance / L72 status-note / formal changelog (L1398) agree 1:1 on the SS-06-drop reasoning, no straggler. Changelog honesty clean. The R28 header-cleanup (duplicate Changelog `| Version | Date | Change |` header + separator removed) confirmed non-disruptive to row content or ordering.

**Lens B (technical soundness): CLEAN.** All 8 load-bearing `develop@2ce3a57` source-facts re-derived exact: `multipath.Frame` carries no `ChanSeq`; `Send` nil-mask handling; `halfchannel.Tick` pre-increment; `arq.OnAck(uint32, [8]byte)` signature with the 64-frame window; instance-local payload recovery; `SendKeystroke` gating order; `wg.Add`-before-`go`; the bench compile-gate contract. Q4 downstream `OnAck` placement re-confirmed race-free and deadlock-free under the acyclic lock order `sinkMu` ⊃ `downstreamHCMu` ⊃ `driver.mu`. One LOW observation carried, not a finding: placement-note L497 stale `access.go:460` citation (S1.7-F3-NOTE-L497-CITATION), known-deferred, unchanged status, no reset.

**Lens C (traceability): NOT CLEAN — F-C-01 (MEDIUM).**

### F-C-01 — STORY-INDEX changelog gap: missing `| 4.144 |` row

**Severity:** MEDIUM
**Category:** STORY-INDEX traceability / changelog completeness (POL-001)

The STORY-INDEX changelog skips a version between two adjacent rows: `| 4.145 | 2026-08-28 | ...` is immediately followed by `| 4.143 | 2026-07-21 | ...` — no `| 4.144 |` row exists anywhere in the table, disk-confirmed. The R28 row (v4.145) itself narrates "Frontmatter version 4.144 → 4.145", so the 4.144 state is independently attested as having existed but its changelog row was never written — a POL-001 (real frontmatter bump without a corresponding changelog row) violation.

## Orchestrator adjudication (PAT-04 git-evidence protocol)

The orchestrator ran `git log -S` against both candidate row strings before accepting or dismissing F-C-01, per the "verify the premise" discipline — an unchecked finding is a claim about history, and history is checkable.

1. **Factual core: TRUE.** Disk-confirmed — the 4.145→4.143 skip is real; `| 4.144 |` does not appear anywhere in `stories/STORY-INDEX.md` prior to this burst.
2. **Premise "sole gap, introduced NEW by R28": REFUTED.**
   - `git log -S'| 4.144 |' -- stories/STORY-INDEX.md` returns **zero commits** — the row never existed at any point in history, including before R28. R28 (commit `4c46710`) is therefore **exonerated**: its diff removed only the duplicate Changelog header/separator (2 lines) plus a frontmatter version bump (4.162→4.163); it deleted zero changelog rows. The gap is **RESIDUAL** (pre-existing), not something R28 introduced.
   - The gap is **not a sole anomaly**. The same git-evidence sweep applied to the rest of the ledger surfaced a second, structurally identical gap: `| 4.128 | 2026-07-19 | ...` is immediately followed by `| 4.126 | 2026-07-18 | ...` — no `| 4.127 |` row. `git log -S'| 4.127 |' -- stories/STORY-INDEX.md` also returns zero commits.
3. **Classification: both are POL-001 (formalized MEDIUM) violations** — real frontmatter version bumps whose changelog rows were never written:
   - `4.143 → 4.144` at commit `e0f65a6` (2026-07-22, S-BL.NODE-IDENTIFY-SVTNID-CONSISTENCY DELIVERED row-sync, PR #130 @ af8eb17).
   - `4.126 → 4.127` at commit `e7cdd90` (2026-07-18, UnbindInterface 3-arg signature errata cascade, rulings v1.2 / BC-2.01.010 v1.2, architect commit `d050552`).
4. **Disposition: NOT CLEAN.** Per §1.3 (fix-the-whole-class, not just the raised instance) and consistent with R25-precedent (fix in place rather than defer when the fix is mechanical and evidence-backed): **backfill both rows from git evidence**, each carrying its true historical date and an explicit reconstruction tag distinguishing it from a contemporaneous row. **Convergence counter RESETS 1/3 → 0/3.**

## Fix applied

Two changelog rows backfilled into `stories/STORY-INDEX.md`, both dated to their true historical bump date and tagged `*(Row backfilled 2026-08-29 per POL-001 / R29 Lens C F-C-01 ...)*`:

- `| 4.144 | 2026-07-22 | ... |` — inserted between the existing `| 4.145 |` and `| 4.143 |` rows.
- `| 4.127 | 2026-07-18 | ... |` — inserted between the existing `| 4.128 |` and `| 4.126 |` rows.

STORY-INDEX frontmatter bumped `4.163 → 4.164`; a new top changelog row (`| 4.164 |`) records this R29 pass and the backfill. The changelog is now **fully dense-sequential from 4.164 down to 4.00** — no single-version skips remain anywhere in the ledger.

Zero edits to the story file or placement note — story stays v1.18/`4902d5d`/17 ACs, note stays v1.15. F-C-01 conceded zero spec impact from the start (it is a pure index-traceability gap, not a story-content defect).

## Recurring process observation

State-manager index-sync commits do not always add a changelog row when they bump frontmatter — this is the **same failure family** as the standing `RECURRING [process-gap]` triple-ledger-parity observation (L57 provenance / L72 status-note / formal changelog re-flagged non-blocking across ~8 prior rounds): hand-maintained parallel ledgers drift without a single generative source. F-C-01 is a second, independent instantiation of that family (a version-to-changelog-row invariant, rather than a ledger-to-ledger parity invariant), and strengthens the same standing recommendation: a single-source-of-truth changelog generator would make both classes of drift structurally impossible rather than relying on discipline. Not actioned unilaterally this round — carried to the human approval gate and the S-7.02 cycle-close checklist alongside the existing triple-ledger recommendation. Tagged `[process-gap]`.

## Known deferred items re-observed (non-blocking, no reset, not re-raised)

- **SURVIVOR-R22** — standing, unaffected by this pass.
- **S1.7-F3-NOTE-L497-CITATION** (placement-note L497 stale `access.go:460` citation) — re-observed by Lens B this round per above, deferred status unchanged, did not contribute to the reset.
- **RECURRING [process-gap] triple-ledger parity** — not independently re-raised as a fresh finding by any leg this round (F-C-01 is a distinct, new-to-this-round finding in the same family, not a re-flag of the standing item).

None of the documented survivors were re-raised as findings and none contributed to the counter reset — the reset is attributable solely to F-C-01.

## Orchestrator disk-verifications

- `.factory` HEAD `4c4671030b2e6c5f0767f18f229bd47f6076ca28` confirmed as the R28 record commit and origin-synced before dispatch.
- Story frontmatter: `version: "1.18"`, `input-hash: "4902d5d"`, `subsystems: [session-networking, multipath-forwarding]`, 17 ACs — confirmed unchanged (no edit this round).
- Placement-note frontmatter unchanged at `version: "1.15"`.
- STORY-INDEX.md confirmed at `version: "4.163"` prior to this burst's bump to `4.164`.
- `git log -S'| 4.144 |' -- stories/STORY-INDEX.md` and `git log -S'| 4.127 |' -- stories/STORY-INDEX.md` both confirmed zero commits (rows never existed) prior to this burst's backfill.
- `git show 4c4671030b2e6c5f0767f18f229bd47f6076ca28 -- stories/STORY-INDEX.md` confirmed the R28 diff contains zero changelog-row deletions (header/separator + frontmatter-bump only) — R28 exonerated.
- Post-backfill changelog version sequence confirmed dense-sequential 4.164→4.00, no single-version skips, via full-file version extraction.
- Working-tree porcelain confirmed to show only the two known-disjoint auto-files (`regression-state.json`, `sidecar-learning.md`) prior to this burst's writes.
- Oracle probe restore confirmed hash-identical to HEAD blob `e53ab35f`, zero residue.

## Next

R30 — fresh-context adversarial reconvergence pass (§1.8 Oracle + 3 diverse lenses A/B/C) against the unchanged v1.18 tip, pass 1 of a new 3-consecutive-clean-pass streak. Needs 3 consecutive clean-or-better passes to reconverge, THEN a re-run of the §1.7 consistency audit against v1.18, THEN the human approval gate.
