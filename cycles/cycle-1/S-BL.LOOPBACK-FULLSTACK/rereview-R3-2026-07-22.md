---
# S-BL.LOOPBACK-FULLSTACK Step-4.5 Re-Review — Round 3 (2026-07-22)

**Reviewed tip:** story `54187e7` (v1.4, input-hash 8ef2387, 17 ACs) + placement-note `c0ce54d` (v1.4).
**Reviewers:** 3 fresh-context diverse-lens adversaries (A spec-fidelity, B technical-soundness, C traceability). All verified dispatch-integrity markers (full stale-version tripwire list: 1.1/1.2/1.3 + hashes d621ea4/41b1472/4c22ebb absent from frontmatter).
**Verdict:** NOT CLEAN. Convergence counter reset to 0/3. All findings disk-verified (PAT-04) against source.

## Round-2 findings — ALL confirmed cleared in v1.4
Lens A + Lens C confirmed B-1 (code sites), B-2, B-3, N-1, B-4, A-N2, C-LOW1 all cleared, story↔note consistent, no live stale sibling of the changed tokens (chanSeq=1 only on non-preferred option; AC-016 unsound options only in "must NOT use" text; "acceptable simpler alternative" only in history). Lens B confirmed B-1/B-2/B-3/B-4 individually sound in the paths they specify.

## Round-3 findings

### HIGH (1) — resets the counter; blocks convergence
- **F-B-1 (HIGH, Lens B — RESIDUAL v1.2 §H2 + NEW v1.4 B-3 false-composition claim)** — `startLoopbackTicker` (note Q6 L397-417) is architecturally incompatible with the `onDownstreamTick()` seam (note §H2 L1033-1054). `startLoopbackTicker` takes `onTick func(halfchannel.ChannelFrame)` and calls `onTick(hc.Tick())` — the HELPER ticks the half-channel, in the ticker goroutine, with NO mutex. The §H2 seam `onDownstreamTick()` is a no-arg method that ticks internally UNDER `downstreamHCMu`. v1.4 B-3 (L1237-1244, esp. L1240) asserts `onDownstreamTick()` is "the same body that the ticker goroutine calls from `startLoopbackTicker`" — which (a) does not type-check (`func()` ≠ `func(ChannelFrame)`), and (b) if adapted naively, either double-ticks (`hc.Tick()` in helper AND inside seam → seq 2×, ARQ window corruption / spurious ErrAckOutOfWindow) or runs the helper's `hc.Tick()` OUTSIDE the mutex → races the mutex-guarded `SendInput→downstreamHC.Enqueue` → DATA RACE → fails AC-015 (`just test-race`, the story's own mandatory gate). H2's binding rule (note L256: "Every Enqueue and Tick call site on each half-channel acquires the corresponding mutex") is directly violated by the unguarded `hc.Tick()` in `startLoopbackTicker`. Disk-verified real. FIX (architect): make `startLoopbackTicker` tick-free — take a no-arg `tickBody func()` (drop the internal `hc.Tick()`; keep `hc` only for closure capture if needed) — route both directions through the internal-tick `onUpstreamTick()`/`onDownstreamTick()` seam methods that lock internally, and RETRACT the L1240 "same body … from startLoopbackTicker" phrasing. This survived 3 prior passes because no lens had drilled the production tick-wiring against H2 until R3.

### MED (1) — resets the counter
- **F-1 (MED, Lens A + Lens C — RESIDUAL of R2 B-1 + false changelog claim)** — story L1002 (Task 11 body) still renders the AC-014 assertion as the 1-value `decodeRTID(payload) == rt.id` (no `[v1.4 B-1]` tag), though `decodeRTID` returns `(uint64, bool)`. The v1.4 story changelog (L1077) explicitly CLAIMS "AC-014 test assertion, and Task-11 body updated to 2-value form" — but Task 11 was missed. Two issues: (1) the residual 1-value form (the exact anti-pattern B-1 exists to eradicate), (2) a false changelog claim (asserts a fix that wasn't applied — audit-trail drift, POL-001-adjacent). Implementation risk low (the canonical AC-014 Test at L788 is correct 2-value), but the changelog inaccuracy blocks a clean verdict. FIX (story-writer): rewrite L1002 to 2-value (mirror L788), OR narrow the changelog claim to match reality (do the former — make Task 11 consistent).

### LOW (2)
- **F-B-4 (LOW, Lens B — RESIDUAL M2 alternative, unchanged)** — the `driver.errCh chan error` (buffered 1) offered as an "acceptable alternative" to `failLoud` (note L1145 / story L391/L815) can deadlock: with AC-017 adding an upstream failLoud path symmetric to AC-016's downstream, both ticker goroutines can call failLoud concurrently; if the errCh variant is chosen, the first send fills buffer-1 and a concurrent second send blocks forever → `Env.Close()`'s `wg.Wait()` hangs (dual upstream+downstream failure, a bug-on-bug). Primary `t.Errorf` path is safe. FIX (architect): drop the errCh alternative OR specify buffered-≥2 with a guaranteed drain.
- **F-2/C (LOW, Lens C — RESIDUAL stale cross-ref, worsened by v1.4)** — story L1064 Forward Obligation says "per AC-013/Task 12, is run once manually" but the run-once step is now Task 14 (Task 12=race-detector/AC-015, Task 13=upstream-error/AC-017 added v1.4). Ref was correct at v1.1, drifted +1 at v1.2 (race task inserted), +1 more at v1.4 (Task 13 inserted). FIX (story-writer): "Task 12" → "Task 14" at L1064; reference by name not ordinal to prevent recurrence.

### NITPICK (2, advisory)
- **F-2/A (Lens A)** — AC-017 "Implements: Task 5" but its test (`TestLoopbackDriver_UpstreamDeliveryError_SurfacesLoud`) is in Task 13; make it "Implements: Task 5, Task 13" (story-writer, fold into F-2 fix).
- **F-B-2b (Lens B)** — CreateSession window prose says chanSeq = "number of empty ticks elapsed"; because the data tick itself increments seq, first data chanSeq is N+1 not N; the derived ≤64 safety bound is still correct, cosmetic only (architect, fold into F-B-2 note touch if editing that area).
- **F-B-2 (LOW→treated as nitpick-adjacent, Lens B)** — first-SendKeystroke "chanSeq=1" is config-dependent (holds only when downstream interval > upstream round-trip, true for VP-042); qualify as config-dependent (architect).

## Process-gap disposition (S-7.02)
No `[process-gap]`-tagged findings this round (R2's B-4 process-gap was fixed in-cycle via AC-017; F-B-1 is a content/design-integration defect, not a prompt/hook/rule gap). Nothing to defer.

## Trend
v1.1 spec-review: 1 BLOCKER + 3 HIGH. R1(v1.2): 1 HIGH + 6 MED + 2 LOW. R2(v1.3): 5 MED + 2 LOW. R3(v1.4): 1 HIGH + 1 MED + 2 LOW. The R3 HIGH is a newly-reached production-wiring defect (deeper drill), not a regression — severity is non-monotonic because each round's fresh lenses reach new perimeter. The decodeRTID straggler has decayed one site per pass (v1.2 all → v1.3 3 sites → v1.4 1 site → R4 target 0). Core design remains sound; no re-architecture needed — F-B-1 is a helper-signature + wiring fix.

## Routing
- **architect (note v1.5):** F-B-1 (startLoopbackTicker tick-free + route via seam methods + retract L1240 claim), F-B-4 (errCh drop-or-buffer≥2), F-B-2 (qualify chanSeq=1 as config-dependent), F-B-2b (off-by-one prose).
- **story-writer (story v1.5):** F-1 (Task 11 → 2-value + honest changelog), F-2/C (Forward Obligation Task 12→14), F-2/A (AC-017 Implements: Task 5, Task 13), + transcribe note v1.5 F-B-1 wiring decision into the story's startLoopbackTicker/Q6 block + AC-015/AC-016 references. AC count stays 17.
