---
artifact_id: W-6.C-wave-adversary-pass-4-Adv-A
document_type: wave-adversarial-review
scope: wave-integration
wave: W-6.C
tranche: C
stories: [S-6.05, S-7.03]
lens: [L1]
develop_tip_claimed: "7fe3e29"
develop_tip_observed: "7fe3e29e4358df16e4e2f1de65a4e0d972540b4a"
pass_number: 4
attempt_number: 1
sub_adversary: Adv-A
verdict: CONVERGENT_L1
findings:
  critical: 0
  high: 0
  medium: 0
  low: 0
observations: 2
reviewer_context: fresh
prior_passes_read: false
worktree_identity_tuple_verified: true
dispatch_integrity_failure: false
timestamp: 2026-07-02T00:00:00Z
---

# Wave Adversary Pass 4 — W-6.C — Adv-A (L1)

## Preflight

- `.git/HEAD` → `ref: refs/heads/develop` (verified)
- `.git/refs/heads/develop` → `7fe3e29e4358df16e4e2f1de65a4e0d972540b4a` (matches claimed prefix `7fe3e29`)
- Worktree basename: `switchboard-blue` (matches expected)
- `worktree_identity_tuple_verified: true`; proceeding.

## Scope note

L1 only (cross-story correctness). Adv-B owns L2/L3. Fresh context; no prior-pass reviews or sprint-state read. Evidence gathered via 5 focused Reads + 6 Greps against develop tip `7fe3e29`.

## Q1–Q5

- **Q1 — Cascade deferral coherence: PASS.** All three sources cite the same follow-on story `S-BL.SESSION-DRAIN` with matching rationale: `internal/svtnmgmt/svtnmgmt.go:770-771` (godoc: "Session-terminated notification for active sessions is deferred to S-BL.SESSION-DRAIN (AC-002 out-of-scope per story S-6.05 v1.5)"); `.factory/stories/S-6.05-svtn-destroy.md:90` and `:313` (DEFER-AC002-SESSION-DRAIN with cross-subsystem-coupling rationale under Forbidden Dependencies); `.factory/cycles/cycle-1/wave-schedule.md:188-193` (active-console-attach cascade deferred to `S-BL.SESSION-DRAIN`, HS-006 does not exercise, manual-eval-only for W-6.C). Coherent triple.
- **Q2 — `--json` envelope shape: PASS.** Both stories route through `connectAndRun` in `cmd/sbctl/client.go:346`, which emits exactly one `writeSuccess`/`writeError` via `cmd/sbctl/main.go:95` / `:111` on every path (dial, auth, dispatch). Destroy's trailing `"destroyed SVTN: %s\n"` at `cmd/sbctl/admin.go:273-275` is properly gated on `!useJSON`, matching the peer-command pattern documented in the same block (`svtn create`, `key register`, `key revoke`, `key expire` all return `connectAndRun` directly). No JSON envelope corruption vector on the destroy path. Console attach/detach/switch (`console.go:102`, `:120`, `:151`) all `return connectAndRun(...)` with no post-print. Consistent envelope contract.
- **Q3 — Confirm-gate scoping: PASS.** `runDestroyConfirmGate` is defined at `cmd/sbctl/admin.go:303` and called at only one site: `cmd/sbctl/admin.go:257` inside `runAdminSvtnDestroy`. Grep confirms zero call-sites elsewhere in `cmd/sbctl`. Console commands (`console.go` runConsoleAttach/Detach/Switch) do not invoke the gate. Correctly scoped; no bleed.
- **Q4 — Error-taxonomy collision: PASS.** S-6.05 uses `E-SVTN-003` (SVTN not found) and `E-ADM-011` Variant 2 (destroy unauthorized, Go-API defense-in-depth). S-7.03 uses `E-SES-001` (session not found), `E-SES-004` (not attached), and `E-ADM-006` (console-role auth denied). Zero overlap in story-specific code spaces. Shared infra codes (`E-CFG-010`, `E-NET-001`, `E-ADM-010`, `E-RPC-001`) all emit from `connectAndRun` (`client.go:351,366,378,381,387`) with identical semantics for both story call-sites; no divergent semantics.
- **Q5 — Auth-chain integration: PASS.** Both stories thread through `connectAndRun` → `Authenticate(ctx, conn, privKey)` (`client.go:371`, ADR-012 challenge/response) → server-side role gate. S-6.05 destroy server-side calls `resolveAndVerifyCallerRole` at `cmd/switchboard/admin_handlers.go:773` (RoleControl-only outer gate for `admin.svtn.destroy`, per the godoc at line 737 explicitly contrasting with the key-expire stricter gate). S-7.03 console handlers call `verifyConsoleCallerRole` at `cmd/switchboard/console_handlers.go:89,110,~130` (RoleControl OR RoleConsole → E-ADM-006 at line 77). S-7.03 does not bypass the mgmt-plane auth prerequisite that S-6.05 depends on; both use the same Authenticate front-door and dispatch to separate handler-role gates appropriate to the command semantic.

## Findings

None.

## Observations

- **O-A-1** (informational): The destroy path's trailing plain-text line (`admin.go:273-275`) already carries an in-code comment ("F-P7L1-MED-1 fix") documenting the historical envelope-corruption bug. The gating is explicit and correct. No action.
- **O-A-2** (informational): S-6.05 defense-in-depth Go-API gate at `svtnmgmt.go:779` is redundant with the handler gate at `admin_handlers.go:773`, and this redundancy is deliberate per RULING-W6TB-A §3 (comment lines 774-778). Both gates return distinct sentinels (E-ADM-011 vs the handler's own error path), which is architecturally correct for the two entry surfaces (Go API vs. RPC). No cross-story concern.

## Verdict

**CONVERGENT_L1.** All five cross-story-correctness questions pass with grounded file:line evidence. Zero critical/high/medium/low findings. Two informational observations that do not affect convergence. Wave W-6.C is L1-clean at Pass 4 closing.
