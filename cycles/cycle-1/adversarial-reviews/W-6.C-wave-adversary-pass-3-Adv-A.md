---
artifact_id: W-6.C-wave-adversary-pass-3-Adv-A
document_type: wave-adversarial-review
scope: wave-integration
wave: W-6.C
tranche: C
stories: [S-6.05, S-7.03]
lens: [L1]
develop_tip_claimed: "7fe3e29"
develop_tip_observed: "7fe3e29e4358df16e4e2f1de65a4e0d972540b4a"
pass_number: 3
attempt_number: 1
sub_adversary: Adv-A
verdict: CONVERGENT_L1
findings:
  critical: 0
  high: 0
  medium: 0
  low: 0
observations: 3
reviewer_context: fresh
prior_passes_read: false
worktree_identity_tuple_verified: true
dispatch_integrity_failure: false
timestamp: 2026-07-02T00:00:00Z
---

# Wave-6 Tranche C — Wave Adversarial Review — Pass 3 (Adv-A, L1)

## Preflight

- `.git/HEAD` = `refs/heads/develop` — OK
- `.git/refs/heads/develop` = `7fe3e29e4358df16e4e2f1de65a4e0d972540b4a` — matches claimed wave-tip `7fe3e29` — OK
- Worktree basename = `switchboard-blue` — OK
- No prior-pass reviews read

## Q1 — Cross-story auth model coherence (ADR-004 admission-key + ADR-012 challenge-response)

**PASS.** Both stories route management traffic through the identical `connectAndRun` path in `cmd/sbctl/client.go:346-393`. The chain is: key-load (E-CFG-010) → dial (E-NET-001) → `Authenticate` challenge-response (E-ADM-010) → dispatch. S-6.05 destroy dispatches `admin.svtn.destroy` at `cmd/sbctl/admin.go:262`; S-7.03 console commands dispatch `console.{attach,detach,switch}` at `cmd/sbctl/console.go:102,120,151`. All four use the same `connectAndRun(ctx, target, keyPath, useJSON, command, rpcArgs, sio)` signature. Story specs both name ADR-006/ADR-012 as the transport contract (S-6.05:270; S-7.03:46,76). ADR-004 admission-key model (RoleControl) applies at server side: S-6.05 gates destroy on `RoleControl` (`internal/svtnmgmt/svtnmgmt.go:779` + `cmd/switchboard/admin_handlers.go:419` returning `E-ADM-011`); S-7.03 gates console commands on `RoleControl | RoleConsole` (`cmd/switchboard/console_handlers.go:68-79` returning `E-ADM-006`). No collision, no divergence.

## Q2 — Shared `--json` envelope semantics

**PASS.** Both stories terminate in the same `writeSuccess`/`writeError` in `cmd/sbctl/main.go:95-123`, which delegates envelope construction to `newSuccessEnvelope`/`newErrorEnvelope` at `cmd/sbctl/client.go:104,109`. S-6.05 destroy correctly gates its post-print (`cmd/sbctl/admin.go:273: if !useJSON`) so the plain-text confirmation line does not corrupt the JSON envelope. S-7.03 console commands return `connectAndRun` directly with no post-print (`console.go:102,120,151`), so no divergence risk. Envelope shape `{ok:true|false, error:{code,message}|null, data:{...}|null}` verified in `client_test.go:1034-1090`.

## Q3 — Confirm-gate scope

**PASS.** The five-path confirm gate in `runDestroyConfirmGate` (`cmd/sbctl/admin.go:303-345`) is called only from `runAdminSvtnDestroy` at line 257. S-7.03 console commands go through `runConsole` → `runConsoleAttach/Detach/Switch` (`console.go:59-152`) with no reference to `runDestroyConfirmGate`, `confirmSVTNShortIDValid`, `--confirm`, `--yes`, or `E-CFG-006`. Verified: grepped `cmd/sbctl/console.go` shows no confirm-gate imports or symbols. The gate is architecturally scoped to `admin svtn destroy` only.

## Q4 — Error-taxonomy non-overlap

**PASS.** Namespaces are disjoint:
- **S-6.05 destroy:** `E-SVTN-001`, `E-SVTN-003` (`admin.go:217`), `E-ADM-011` (`admin_handlers.go:419`), `E-CFG-006` (`admin.go:306`).
- **S-7.03 console:** `E-SES-001` (`console.go:86,134`), `E-SES-004` (`console.go:112,135`), `E-ADM-006` (`console_handlers.go:68,75,79` — CONSOLE role gate, distinct from S-6.05's `E-ADM-011` CONTROL gate).

Shared infrastructure codes (`E-CFG-010`, `E-NET-001`, `E-ADM-010`, `E-RPC-001`, `E-RPC-011`) belong to the transport/auth layer. No story-specific code collides.

## Q5 — Cascade-deferral consistency

**PASS.** The destroy-with-active-console-attach cascade is coherently deferred to `S-BL.SESSION-DRAIN` across all four surfaces:
- **Code:** `internal/svtnmgmt/svtnmgmt.go:770-771` explicit deferral marker.
- **Story S-6.05:** line 90, line 313 (`DEFER-AC002-SESSION-DRAIN`).
- **Schedule:** `wave-schedule.md:187-193`.
- **Holdout wave-6.md:** does NOT exercise the cascade (steps 4-8 test discovery + console on independent sessions).

## Findings

None. All five L1 questions PASS.

## Observations (coherent-shape notes, not defects)

1. **Shared-infrastructure vs story-specific code namespaces are cleanly separated.** `E-CFG-*`, `E-NET-*`, `E-ADM-010`, `E-RPC-*` transport-layer codes; `E-SVTN-*`, `E-SES-*`, and the role-gate `E-ADM-006`/`E-ADM-011` pair are story-specific. The taxonomy scales.

2. **The `if !useJSON` gate at `admin.go:273` is the load-bearing envelope-integrity guard.** S-7.03 avoids the risk architecturally by never post-printing. If a future console command needs an operator-friendly summary line, the S-6.05 pattern is the established solution.

3. **`E-ADM-006`/`E-ADM-011` role-gate distinction is worth flagging for future adversarial passes.** Both are "authorization denied" but scope differently: `E-ADM-011` control-role-required, `E-ADM-006` control-or-console-required. The last-triplet numbering avoids collision.

## Verdict

**CONVERGENT_L1.** All five cross-story integration questions PASS with specific file:line evidence. Zero findings. Three observations are coherent-shape notes on the well-designed sharing pattern.
