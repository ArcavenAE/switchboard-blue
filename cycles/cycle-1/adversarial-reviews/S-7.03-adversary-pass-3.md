---
artifact_id: S-7.03-adversary-pass-3
document_type: adversarial-review
story_id: S-7.03
pass: 3
lens: [1, 2, 3]
producer: adversary
timestamp: 2026-07-02T00:00:00
findings:
  L1: {HIGH: 0, MED: 0, LOW: 0}
  L2: {HIGH: 0, MED: 0, LOW: 0}
  L3: {HIGH: 0, MED: 1, LOW: 0}
  total: {HIGH: 0, MED: 1, LOW: 0}
verdict: L1+L2 CLEAN; L3 has 1 MED finding
input_hash_impl: ce96e58
input_hash_story: S-7.03 v1.3
input_hash_bc: BC-2.08.001 v1.2
input_hash_vp: VP-050
---

# Adversarial Review — S-7.03 Pass 3

**Scope:** S-7.03 v1.3 (console remote control via sbctl), feature HEAD `ce96e58`, worktree `/Users/skippy/work/aae-orc/run/switchboard-blue/.worktrees/S-7.03/`.
**Lenses:** L1 Correctness/Coverage, L2 Test Rigor, L3 Traceability/Governance.
**Policy suite:** POL-001 (changelog completeness), POL-002 (story-index-row-sync), POL-003 candidate (BC/VP↔Story row version sync).

---

## Lens 1 — Correctness / Coverage

**Result: CLEAN.**

Pass-2 fix-burst objectives corroborated at the feature checkout:

- Register-before-serve invariant honored. runConsole in cmd/switchboard/mgmt_wire.go populates admission.AdmittedKeySet via parsePEMOperatorKeys and calls BuildConsoleHandlers before serveMgmtServer. F-P2L1-001 no longer reproducible.
- AdmittedKeySet is populated (not empty). runConsole iterates cfg.AuthorizedOperatorKeys, registers each with RoleConsole at zeroSVTN = [16]byte{}. F-P2L1-002 resolved.
- Atomic switch semantics preserved. HandleConsoleSwitch (internal/session/remote.go) validates target session via reg.Exists before mutating; the mutex-guarded critical section sets state.current = req.SessionName (not ""). F-P2L1-003 resolved.
- TOCTOU acknowledgment documented as intentional. HandleConsoleAttach/Switch note that reg.Exists is called outside the state lock; state is string (not *Session), so a late-arriving deletion cannot corrupt state. Consequence is bounded: at most a spurious E-SES-001 on subsequent operation. Aligns with story AC-006 state-only design.
- Caller pubkey is authenticated, not fabricated. verifyConsoleCallerRole (console_handlers.go) reads mgmt.CallerPubkey(ctx) — the caller pubkey injected by the mgmt handshake — not any request-body field. Defense-in-depth (Tier-1 OperatorKeySet ⊕ Tier-2 AdmittedKeySet) intact.
- Forbidden imports absent. cmd/sbctl/console.go imports only context, flag, fmt. ARCH-08 §6.6 clean.
- Wire format: consoleAttachArgs/consoleSwitchArgs are stable JSON structs with session_name field. Response bodies echo the acted-on session name (verified in console_handlers_e2e_test.go).

**Severity counts: 0 HIGH, 0 MED, 0 LOW.**

---

## Lens 2 — Test Rigor

**Result: CLEAN.**

- E2E tests exercise real transport. TestConsoleRemote_E2E_VP050 in cmd/switchboard/console_handlers_e2e_test.go runs a real mgmt.Server, real Ed25519 handshake, and asserts session_name echoes back through the full attach→detach→re-attach→switch→detach cycle. Not build-tagged, so it runs in default go test ./....
- F-P2L2-007 fix propagated. cmd/sbctl/console_test.go uses strings.Contains(err.Error(), "E-SES-001:") (colon suffix) — E-SES-0010 false-match precluded.
- E-RPC-011 → domain-code lift verified. sendAdminRPCAsKey (used by the E-ADM-006 admission-denied E2E tests via the pattern in admin_handlers_e2e_test.go:439-458) extracts the domain code prefix from error.message when outer error.code == "E-RPC-011". Assertion code == "E-ADM-006" therefore proves both that the handler emitted E-ADM-006 AND that mgmt.go:703 wrapped it as expected. F-P2L2-006 resolved.
- Race test is genuinely concurrent. TestConsoleState_ConcurrentAttachDetachSwitchIsRaceFree (internal/session/remote_test.go) launches 100 goroutines × 30 ops each and asserts terminal state is either "" or a known session — invariant a torn write would break. Not sequential.
- Sentinel-error identity checked, not string. remote_test.go uses errors.Is(err, session.ErrSessionNotFound). Correct pattern.
- Positive-coverage assertion count is runtime-computed (per CI-as-code axis): E2E tests use t.Fatalf on missing session_name echo — the assertion is non-vacuous.

**Severity counts: 0 HIGH, 0 MED, 0 LOW.**

---

## Lens 3 — Traceability / Governance

**Result: 1 MED finding.**

### F-P3L3-MED-001 — BC-2.08.001 and VP-050 traceability rows drift on POL-003 candidate

**Confidence: HIGH.**

Two traceability rows still cite the story at v1.2 after the v1.3 fix-burst bumped it:

- `.factory/specs/behavioral-contracts/ss-08/BC-2.08.001.md:113`
  `| Stories | S-7.03 v1.2 (console remote-control...)` — should read `v1.3`.
- `.factory/specs/verification-properties/VP-050.md:42`
  `| S-7.03 | v1.2 | implementing story...` — should read `v1.3`.

The v1.3 changelog entry in `.factory/stories/S-7.03-console-remote-control.md` claims "F-P2L3-03: BC-2.08.001 traceability row wording updated — retraction has landed as v1.2 (final)." That edit only removed the transitional "pending Inv-3 retraction" wording; it did NOT bump the story version reference from v1.2 to v1.3 in either BC-2.08.001 or VP-050.

STORY-INDEX.md:76 correctly shows v1.3, so POL-002 (story-index-row-sync) is intact. The drift is scoped to the POL-003 candidate axis (BC/VP→Story row version sync).

Story body BC table (line 67 in the story) also references BC-2.08.001 v1.2 — which is correct (BC is at v1.2). The reverse pointer (BC→Story) is what drifts.

**Severity rationale — MED, not HIGH:**
- No implementation or test miscarriage; content of both rows is otherwise accurate.
- Bidirectional traceability is broken in one direction (BC→Story, VP→Story), which is what POL-003 candidate exists to prevent.
- Same defect appears in two artifacts from the same fix-burst — pattern flag applies; if repeated across another cycle, escalate.
- Fix is mechanical: update two row cells; no artifact-body impact.

**Suggested fix locus:** replace `S-7.03 v1.2` → `S-7.03 v1.3` at both cited lines and re-render.

---

## Novelty Assessment

Novelty: MEDIUM. F-P3L3-MED-001 is a fresh finding not addressed by Pass-2's F-P2L3-01/02/03 sweep. Pass-2's changelog-wording sweep touched BC-2.08.001's row prose but left the version numeral unbumped — the exact class POL-003 candidate is intended to catch. Lenses 1 and 2 are clean at genuinely-new-findings-zero; no regressions from Pass-2 fixes. Recommend accepting Pass-3 as converged on L1/L2 and gating on the L3 fix before pass N+1.

---

## Summary — Severity Counts

| Lens | HIGH | MED | LOW |
|------|------|-----|-----|
| L1 Correctness/Coverage | 0 | 0 | 0 |
| L2 Test Rigor | 0 | 0 | 0 |
| L3 Traceability/Governance | 0 | 1 | 0 |
| **Total** | **0** | **1** | **0** |

**Recommendation:** address F-P3L3-MED-001 (two-line edit at BC-2.08.001:113 and VP-050:42) and this Pass-3 delta converges. No implementation changes required.
