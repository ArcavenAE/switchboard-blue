---
artifact_id: VP-055
document_type: verification-property
level: L4
version: "1.3"
status: draft
producer: architect
timestamp: 2026-06-23T00:00:00
phase: 1c-refinement
traces_to: '.factory/specs/architecture/ARCH-INDEX.md'
inputDocuments:
  - '.factory/specs/behavioral-contracts/ss-03/BC-2.03.003.md'
  - '.factory/specs/architecture/ARCH-01-core-services.md'
source_bc: BC-2.03.003
module: internal/discovery
proof_method: proptest
feasibility: feasible
verification_lock: true
proof_completed_date: 2026-07-06
lifecycle_status: implemented
implementing_story: S-7.02
implementing_stories:
  - S-7.02
lifecycle_history:
  - date: 2026-07-06
    from: active
    to: implemented
    reason: "S-7.02 shipped on develop f09fe73; three-property gopter split per RULING-W6TB-J v1.2: internal/discovery/discovery_test.go:1247 TestPropPresenceAdvertisement_RoundTrip + :1422 TestPropPresenceAdvertisement_RejectsEmptyOrInvalidUTF8 + :1506 TestPropPresenceAdvertisement_TruncatesOversize discharge codec round-trip + validation + truncation properties (BC-2.03.003 PC-1/PC-2/PC-3/EC-001)."
    agent: formal-verifier (phase6-vp-sweep)
introduced: v0.1.1
modified:
  - date: 2026-07-01
    version: "1.1"
    actor: spec-steward
    change: >
      S-7.02 LENS-3 traceability backfill (RULING-W6TB-D): implementing_story set
      to S-7.02; Story Trace section added.
  - date: 2026-07-01
    version: "1.2"
    actor: architect
    change: >
      RULING-W6TB-J (O-P4L3-01): split "reject invalid name" property into
      (i) RejectsEmptyOrInvalidUTF8 (empty and invalid-UTF-8 inputs → error) and
      (ii) TruncatesOversize (>255-byte valid UTF-8 inputs → truncated to ≤255 bytes
      with "…" suffix, err == nil). Aligns with S-7.02 AC-004b truncation semantics
      and BC-2.03.003 v1.3 EC-001. Round-trip property unchanged.
  - date: 2026-07-06
    version: "1.3"
    actor: formal-verifier (phase6-vp-sweep)
    change: >
      VP sweep batch 3: PROVEN. active → implemented. verification_lock: true.
      Discharging tests at internal/discovery/discovery_test.go:1247
      TestPropPresenceAdvertisement_RoundTrip + :1422 RejectsEmptyOrInvalidUTF8
      + :1506 TruncatesOversize (develop f09fe73).
---

# VP-055: Presence Advertisement Payload — Required Fields Present and Round-Trip Stable

## Property Statement

Every presence advertisement frame emitted by an access node contains all three
required fields:
- a session name string (length 1..255 bytes, valid UTF-8)
- an attachment status enum (`open | attached | read_only`)
- a current quality indicator enum (`green | yellow | red`)

Parse-then-serialize round-trips to identity: `Decode(Encode(adv)) == adv`.

Formally:

∀ PresenceAdvertisement adv with valid fields:
  let encoded = adv.Encode()
  let decoded, err = Decode(encoded)
  assert err == nil
  assert decoded.SessionName    == adv.SessionName
  assert decoded.AttachStatus   == adv.AttachStatus
  assert decoded.QualityIndicator == adv.QualityIndicator

∀ SessionName s where len(s) ∈ [1, 255] and utf8.Valid(s):
  adv.SessionName = s → Encode succeeds (no error)

∀ SessionName s where len(s) == 0 or !utf8.Valid(s):
  adv.SessionName = s → Encode returns validation error (reject-empty-or-invalid-utf8)

∀ SessionName s where len(s) > 255:
  let encoded, err = adv with SessionName = s → Encode()
  assert err == nil                                          (truncation, not rejection)
  assert len(encoded_name) <= 255                           (output within bounds)
  assert utf8.Valid(encoded_name)                           (output is valid UTF-8)
  assert strings.HasSuffix(encoded_name, "…")              (ellipsis appended)
  let prefix = encoded_name[:len(encoded_name)-3]           (strip 3-byte ellipsis)
  assert utf8.Valid([]byte(prefix))                         (prefix rune-boundary safe)
  assert strings.HasPrefix(s, prefix)                       (prefix is a true prefix of input)

∀ QualityIndicator q ∈ {green, yellow, red}:
  PresenceAdvertisement{QualityIndicator: q}.Encode() succeeds

## Rationale

VP-044 (`Presence advertisement includes required fields`) tests the advertisement
via the integration path (advertisement published by a running access node). It
verifies the observable behavior but does not exercise the Encode/Decode codec in
isolation. Without VP-055:

1. A codec bug (e.g., attachment status serialized as integer but deserialized as
   string enum) would be invisible to VP-044 if the integration test only checks
   the outer observable behavior.
2. BC-2.03.003 postcondition 1 specifies `{session_name, attached, quality}` as
   the *minimum* payload — VP-055 verifies the codec enforces these constraints
   independently of the network transport.
3. BC-2.03.003 postcondition 2 constrains session names to UTF-8 with a 255-byte
   maximum — a constraint testable only by proptest over the full UTF-8 character
   space.

The round-trip property (encode → decode → identity) is the standard correctness
criterion for codecs and is directly testable with gopter.

## Source Contract

- **BC:** BC-2.03.003 — Presence advertisement includes session name, attachment status, and quality indicator
- **Postcondition 1:** "Each session entry contains exactly:
  {session_name: string, attached: bool, quality: green|yellow|red}."
- **Postcondition 2:** "Session names are UTF-8 encoded, maximum 255 bytes."
- **Postcondition 3:** "Quality indicator reflects current path quality."
- **EC-001:** "Session name > 255 bytes: truncated with '…' indicator." (BC-2.03.003 v1.3: oversize names truncated to ≤252 bytes + "…", err == nil)

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| proptest | gopter v0.2.9+ | no | arbitrary UTF-8 session names (1–255 bytes); all 3 attachment statuses; all 3 quality indicators; round-trip identity; invalid input rejection |

## Proof Harness Skeleton

```go
package discovery_test

import (
	"strings"
	"testing"
	"unicode/utf8"

	"github.com/leanovate/gopter"
	"github.com/leanovate/gopter/gen"
	"github.com/leanovate/gopter/prop"

	"github.com/arcavenae/switchboard/internal/discovery"
)

// TestPropPresenceAdvertisement_RoundTrip verifies that arbitrary valid
// presence advertisements round-trip through Encode/Decode to identity.
func TestPropPresenceAdvertisement_RoundTrip(t *testing.T) {
	properties := gopter.NewProperties(nil)

	// Generate a valid UTF-8 session name of length 1..255 bytes.
	genSessionName := gen.AnyString().SuchThat(func(s string) bool {
		b := []byte(s)
		return len(b) >= 1 && len(b) <= 255 && utf8.Valid(b)
	})

	genAttachStatus := gen.OneConstOf(
		discovery.AttachStatusOpen,
		discovery.AttachStatusAttached,
		discovery.AttachStatusReadOnly,
	)

	genQuality := gen.OneConstOf(
		discovery.QualityGreen,
		discovery.QualityYellow,
		discovery.QualityRed,
	)

	properties.Property(
		"encode/decode round-trip preserves all fields",
		prop.ForAll(
			func(name string, status discovery.AttachStatus, quality discovery.QualityIndicator) bool {
				adv := discovery.PresenceAdvertisement{
					SessionName:     name,
					AttachStatus:    status,
					QualityIndicator: quality,
				}

				encoded, err := adv.Encode()
				if err != nil {
					return false
				}

				decoded, err := discovery.Decode(encoded)
				if err != nil {
					return false
				}

				return decoded.SessionName == adv.SessionName &&
					decoded.AttachStatus == adv.AttachStatus &&
					decoded.QualityIndicator == adv.QualityIndicator
			},
			genSessionName,
			genAttachStatus,
			genQuality,
		),
	)

	properties.TestingRun(t)
}

// TestPropPresenceAdvertisement_RejectsEmptyOrInvalidUTF8 verifies that Encode
// returns a validation error for session names that are empty or not valid UTF-8.
// (VP-055 v1.2 — RULING-W6TB-J: oversize names are truncated, not rejected.)
func TestPropPresenceAdvertisement_RejectsEmptyOrInvalidUTF8(t *testing.T) {
	properties := gopter.NewProperties(nil)

	// Generate names that are empty or contain invalid UTF-8 byte sequences.
	genInvalidName := gen.OneGenOf(
		gen.Const(""), // empty
		gen.SliceOf(gen.UInt8()).Map(func(bs []byte) string {
			// Inject an invalid UTF-8 byte sequence (0xFF is never valid UTF-8).
			return string(append(bs, 0xFF))
		}),
	)

	properties.Property(
		"encode rejects session names that are empty or contain invalid UTF-8",
		prop.ForAll(
			func(name string) bool {
				adv := discovery.PresenceAdvertisement{
					SessionName:      name,
					AttachStatus:     discovery.AttachStatusOpen,
					QualityIndicator: discovery.QualityGreen,
				}
				_, err := adv.Encode()
				return err != nil // must return an error
			},
			genInvalidName,
		),
	)

	properties.TestingRun(t)
}

// TestPropPresenceAdvertisement_TruncatesOversize verifies that Encode truncates
// session names exceeding 255 bytes to a valid UTF-8 rune boundary ≤252 bytes,
// appends "…" (U+2026, 3 UTF-8 bytes), and returns err == nil.
// (VP-055 v1.2 — RULING-W6TB-J; BC-2.03.003 v1.3 EC-001.)
func TestPropPresenceAdvertisement_TruncatesOversize(t *testing.T) {
	properties := gopter.NewProperties(nil)

	// Generate valid UTF-8 strings that exceed 255 bytes.
	genOversizeName := gen.AnyString().Map(func(s string) string {
		for len([]byte(s)) <= 255 {
			s += "x"
		}
		return s
	}).SuchThat(func(s string) bool {
		return utf8.ValidString(s) && len([]byte(s)) > 255
	})

	ellipsis := "…" // U+2026, 3 UTF-8 bytes

	properties.Property(
		"encode truncates oversize names to ≤255 bytes of valid UTF-8 with ellipsis",
		prop.ForAll(
			func(name string) bool {
				adv := discovery.PresenceAdvertisement{
					SessionName:      name,
					AttachStatus:     discovery.AttachStatusOpen,
					QualityIndicator: discovery.QualityGreen,
				}
				encoded, err := adv.Encode()
				if err != nil {
					return false // truncation must not error
				}

				decoded, decErr := discovery.Decode(encoded)
				if decErr != nil {
					return false
				}
				result := decoded.SessionName

				// Output must be within the 255-byte limit.
				if len([]byte(result)) > 255 {
					return false
				}
				// Output must be valid UTF-8.
				if !utf8.ValidString(result) {
					return false
				}
				// Output must end with "…".
				if !strings.HasSuffix(result, ellipsis) {
					return false
				}
				// Prefix (minus ellipsis) must be a valid UTF-8 prefix of the input.
				prefix := result[:len(result)-len(ellipsis)]
				if !utf8.ValidString(prefix) {
					return false
				}
				return strings.HasPrefix(name, prefix)
			},
			genOversizeName,
		),
	)

	properties.TestingRun(t)
}
```

## Feasibility Assessment

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Input space size | bounded via constraints | UTF-8 names 1–255 bytes; 3×3 enum combinations; gopter shrinks on failure |
| Proof complexity | low | pure codec: Encode/Decode are pure functions with no I/O; no concurrency |
| Tool support | gopter | Go 1.25.4 compatible; standard string generators |
| Estimated proof time | < 1 min | no I/O; codec operations are O(n) on name length |
| Invalid input coverage (v1.2) | three generators | (i) RejectsEmptyOrInvalidUTF8: empty string + invalid-UTF-8 byte injection; (ii) TruncatesOversize: valid UTF-8 padded to >255 bytes; (iii) RoundTrip: valid UTF-8 1–255 bytes |

### Feasibility by Property (v1.2)

| Property | Generator | Bounded? | Complexity |
|----------|-----------|----------|-----------|
| RejectsEmptyOrInvalidUTF8 | empty string + invalid-UTF-8 byte injection | no | low |
| TruncatesOversize | valid UTF-8 strings padded to >255 bytes | no | low–medium |
| RoundTrip (unchanged) | valid UTF-8 names 1–255 bytes | no | low |

## Story Trace

| Property | Owning Story | Wave | Depends On |
|----------|-------------|------|-----------|
| Presence advertisement payload round-trip: required fields present, stable, and codec-correct | S-7.02 | Wave 6 | S-6.02 |

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-06-23 | architect |
| v1.1 | 2026-07-01 | spec-steward (S-7.02 LENS-3 traceability backfill per RULING-W6TB-D: implementing_story + Story Trace added) |
| v1.2 | 2026-07-01 | architect (RULING-W6TB-J, O-P4L3-01): split "reject invalid name" property into (i) RejectsEmptyOrInvalidUTF8 (empty and invalid-UTF-8 inputs → error) and (ii) TruncatesOversize (>255-byte valid UTF-8 inputs → truncated to ≤255 bytes with "…" suffix, err == nil). Aligns with S-7.02 AC-004b truncation semantics and BC-2.03.003 v1.3 EC-001. Round-trip property unchanged. |
| v1.3 — audited (Phase 6 VP sweep). Ruling: **PROVEN**. Discharging evidence: `internal/discovery/discovery_test.go:1247 TestPropPresenceAdvertisement_RoundTrip` + `:1422 TestPropPresenceAdvertisement_RejectsEmptyOrInvalidUTF8` + `:1506 TestPropPresenceAdvertisement_TruncatesOversize` (three-property RULING-W6TB-J split, BC-2.03.003 PC-1/PC-2/PC-3/EC-001). Story: S-7.02. active → implemented; lock set. | 2026-07-06 | formal-verifier (phase6-vp-sweep) |
