---
artifact_id: VP-042
document_type: verification-property
level: L4
version: "1.3"
status: draft
producer: architect
timestamp: 2026-06-23T00:00:00
phase: 1b
traces_to: '.factory/specs/architecture/ARCH-INDEX.md'
inputDocuments:
  - '.factory/specs/behavioral-contracts/ss-01/BC-2.01.001.md'
  - '.factory/specs/behavioral-contracts/ss-02/BC-2.02.001.md'
  - '.factory/specs/architecture/ARCH-07-verification-architecture.md'
source_bc: BC-2.01.001
module: internal/halfchannel
proof_method: benchmark
feasibility: feasible
verification_lock: false
proof_completed_date: null
lifecycle_status: active
introduced: v0.1.0
modified:
  - version: "1.1"
    date: 2026-06-28
    author: architect
    reason: "Wave 4 fresh-context audit L-2: VP-042 is unowned (no story vp_traces it). S-4.02 correctly dropped it per RULING-002 (it is a halfchannel-integration benchmark, not a replay property). Added ownership-intent annotation and backlog stub record."
  - 2026-07-06T00:00:00  # v1.2 — audited (VP sweep batch 3, UNPROVEN-BLOCKED: unowned VP; S-BL.BENCH backlog stub)
  - 2026-07-06T12:00:00  # v1.3 — ADOPTED by S-BL.BENCH (merged PR #109 cd67394), partial evidence: BenchmarkKeystrokeEcho_P99 (internal/bench) measures in-process loopback echo p99 ~0.002ms vs 100ms limit — honest LOWER BOUND only (declared divergence: inline echo path bypasses arq/multipath/tick-scheduling; VP skeleton calls testenv.NewLoopback). Lock deferred to a testenv-integrated measurement post S-BL.TESTENV. No longer unowned.
deprecated: null
---

# VP-042: Keystroke-to-Echo p99 ≤ 100ms

## Property Statement

Over 500 keystroke-to-echo round trips at tick_interval=10ms upstream,
50ms downstream over a single-hop LAN:
  p99(round_trip_latency) ≤ 100ms

This covers NFR-001: the 100ms keystroke-to-echo budget is the primary
interactive latency SLO. Exceedances make the terminal feel unresponsive.

## Source Contract

- **BC:** BC-2.01.001 — Timeslice clock fires on every tick regardless of data availability
- **BC:** BC-2.02.001 — Duplicate-and-race: same frame sent on two fastest paths simultaneously
- **Postcondition/Invariant:** NFR-001 — "Keystroke-to-echo p99 ≤ 100ms at 10ms upstream / 50ms downstream tick."

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| benchmark | go test -bench (testing.B) | yes | in-process loopback stack; 500 keystroke/echo round trips; measure wall-clock latency; compute p99 |

## Proof Harness Skeleton

```go
package integration_test

import (
	"context"
	"sort"
	"testing"
	"time"

	"github.com/arcavenae/switchboard/internal/testenv"
)

// BenchmarkKeystrokeToEcho_P99 measures the p99 keystroke-to-echo latency
// over a single-hop loopback stack at the reference tick intervals.
func BenchmarkKeystrokeToEcho_P99(b *testing.B) {
	const (
		upstreamInterval   = 10 * time.Millisecond
		downstreamInterval = 50 * time.Millisecond
		samples            = 500
		maxP99             = 100 * time.Millisecond
	)

	ctx := context.Background()
	env := testenv.NewLoopback(b, ctx, testenv.LoopbackConfig{
		TickIntervalUpstream:   upstreamInterval,
		TickIntervalDownstream: downstreamInterval,
	})
	b.Cleanup(env.Close)

	sessionID := env.CreateSession(b)
	latencies := make([]time.Duration, 0, samples)

	b.ResetTimer()
	for i := 0; i < samples; i++ {
		start := time.Now()
		env.SendKeystroke(b, sessionID, "x")
		env.WaitForEcho(b, sessionID, "x", 500*time.Millisecond)
		latencies = append(latencies, time.Since(start))
	}
	b.StopTimer()

	sort.Slice(latencies, func(i, j int) bool {
		return latencies[i] < latencies[j]
	})

	p99idx := int(float64(len(latencies)) * 0.99)
	p99 := latencies[p99idx]

	b.ReportMetric(float64(p99.Milliseconds()), "p99_rtt_ms")

	if p99 > maxP99 {
		b.Errorf("keystroke-to-echo p99 %v exceeds NFR-001 limit %v", p99, maxP99)
	}
}
```

## Feasibility Assessment

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Input space size | fixed | 500 samples; loopback; no network noise |
| Proof complexity | high | requires full in-process stack with echo detection |
| Tool support | testing.B + testenv | testenv must support loopback echo measurement |
| Estimated proof time | ~30s | 500 × (10ms upstream + 50ms downstream) + overhead |

## Ownership Status (Wave 4 audit L-2, 2026-06-28)

VP-042 is currently **unowned**: no story's `vp_traces` carries it.

**History:** VP-042 was initially assigned to S-4.02 (upstream-replay). RULING-002
(2026-06-28) correctly removed it from S-4.02 scope: `internal/replay` is a pure-core
map-insert; benchmarking it at per-call µs resolution cannot verify the 100ms round-trip
SLO. VP-042 requires a full in-process loopback stack spanning `internal/halfchannel` +
`internal/arq` + `internal/multipath` + echo detection, which no current wave story
assembles.

**Intended future owner:** A dedicated integration-benchmark story — suggested ID
**S-BL.BENCH** (backlog stub, not yet authored) — that wires the complete loopback
`testenv` and runs the 500-sample round-trip harness defined in the proof harness
skeleton above. This story does not exist yet; it should be created as a backlog
stub when Wave 5+ planning begins. Until that story is authored and carries VP-042
in its `vp_traces`, this VP remains unverified.

**Action required:** Create backlog story S-BL.BENCH (or equivalent) with:
- `vp_traces: [VP-041, VP-042]` (both benchmark VPs share the loopback harness)
- `architecture_modules: [internal/halfchannel, internal/arq, internal/multipath]`
- `bc_traces: [BC-2.01.001, BC-2.02.001]`

> M-3 note: if M-3 (product-owner) creates a backlog placeholder for the full-stack
> integration harness, VP-042 and VP-041 should both be listed in that stub's
> vp_traces. The backlog stub implied by M-4 (S-BL.NI ARQ-reconnect) is a separate
> story with different scope (network-ingress / RESYNC frame mechanics).

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-06-23 | architect |
| Ownership annotation added | 2026-06-28 | architect |
| v1.2 — audited (Phase 6 VP sweep). Ruling: **UNPROVEN-BLOCKED**. No `BenchmarkKeystrokeToEcho_P99` implementation found in develop `f09fe73`; VP-042 is unowned per Wave 4 audit L-2 (no story `vp_traces` it). Harness skeleton depends on `internal/testenv.NewLoopback` + `LoopbackConfig` + `CreateSession` + `SendKeystroke` + `WaitForEcho`, and `internal/testenv` does not exist in develop. Blocker: full-stack loopback benchmark harness (`internal/testenv.NewLoopback` spanning `internal/halfchannel` + `internal/arq` + `internal/multipath`). Anchor story: S-BL.BENCH (backlog stub, per ownership section above). | 2026-07-06 | formal-verifier (phase6-vp-sweep) |
