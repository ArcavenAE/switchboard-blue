---
artifact_id: VP-033
document_type: verification-property
level: L4
version: "1.2"
status: draft
producer: architect
timestamp: 2026-06-23T00:00:00
phase: 1b
traces_to: '.factory/specs/architecture/ARCH-INDEX.md'
inputDocuments:
  - '.factory/specs/behavioral-contracts/ss-04/BC-2.04.003.md'
  - '.factory/specs/behavioral-contracts/ss-04/BC-2.04.004.md'
  - '.factory/specs/architecture/ARCH-07-verification-architecture.md'
source_bc: BC-2.04.003
module: internal/session
proof_method: e2e
feasibility: feasible
verification_lock: true
proof_completed_date: 2026-07-06
lifecycle_status: active
introduced: v0.1.0
modified:
  - 2026-07-06T00:00:00  # v1.1 — audited (VP sweep batch 2, UNPROVEN-BLOCKED: internal/testenv absent)
# PROVEN 2026-07-06 — S-BL.TESTENV merged PR #110 (62e38d3): TestE2E_Console_AttachDetachLifecycle (internal/session/vp033_034_e2e_test.go, integration tag). Basis: real session.Publisher + AccessNode lifecycle — real code path. In-process rig (internal/testenv); goroutine-leak-fenced (TestClose_NoGoroutineLeak). Coordinator ran integration suite post-merge: ok.
deprecated: null
---

# VP-033: Console Attach/Detach Lifecycle

## Property Statement

Console.Attach(session):
  console receives downstream frames from the session

Console.Detach():
  downstream frames stop flowing to the console
  ∧ the session continues running on the access node (not terminated)

The attach/detach lifecycle must be clean: attaching starts frame delivery;
detaching stops delivery without ending the remote session.

## Source Contract

- **BC:** BC-2.04.003 — Console attaches to session by name; receives downstream stream and sends upstream keystrokes
- **BC:** BC-2.04.004 — Console detach releases session without closing it; session continues on access node
- **Postcondition/Invariant:** Postcondition — "Detach stops frames; session survives."

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| e2e | go test (testing.T) | yes | full stack: router + access + console; attach → receive frames; detach → frames stop; session still alive |

## Proof Harness Skeleton

```go
//go:build integration

package session_test

import (
	"context"
	"testing"
	"time"

	"github.com/arcavenae/switchboard/internal/testenv"
)

// TestE2E_Console_AttachDetachLifecycle verifies the full attach/detach
// lifecycle across a running router + access + console stack.
func TestE2E_Console_AttachDetachLifecycle(t *testing.T) {
	t.Parallel()

	ctx, cancel := context.WithTimeout(context.Background(), 30*time.Second)
	t.Cleanup(cancel)

	// Spin up a minimal in-process switchboard stack.
	env := testenv.New(t, ctx)
	t.Cleanup(env.Close)

	sessionID := env.CreateSession(t)

	// --- Attach phase ---
	console := env.AttachConsole(t, sessionID)

	// Generate downstream output by sending a keystroke.
	env.SendKeystroke(t, sessionID, "echo hello\n")

	// Wait for downstream frames to arrive at the console.
	frames := console.CollectFrames(t, 2*time.Second)
	if len(frames) == 0 {
		t.Fatal("expected downstream frames after attach; received none")
	}

	// --- Detach phase ---
	console.Detach(t)

	// Allow any in-flight frames to drain.
	time.Sleep(200 * time.Millisecond)
	beforeCount := len(frames)

	// Generate more output on the session.
	env.SendKeystroke(t, sessionID, "echo world\n")
	time.Sleep(500 * time.Millisecond)

	framesAfterDetach := console.CollectFrames(t, 0)
	if len(framesAfterDetach) > beforeCount {
		t.Errorf("frames received after detach: expected 0 new frames, got %d",
			len(framesAfterDetach)-beforeCount)
	}

	// Session must still be alive on the access node.
	if !env.SessionAlive(t, sessionID) {
		t.Error("session was terminated after console detach; expected it to survive")
	}
}
```

## Feasibility Assessment

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Input space size | single scenario | deterministic attach/detach sequence |
| Proof complexity | high | requires full in-process stack (testenv); frame collection infrastructure |
| Tool support | testing.T + testenv | testenv package must provide in-process router/access/console |
| Estimated proof time | < 10s | includes session startup latency |

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-06-23 | architect |
| v1.1 — audited (Phase 6 VP sweep). Ruling: **UNPROVEN-BLOCKED**. No discharging test found; harness skeleton imports `github.com/arcavenae/switchboard/internal/testenv`, and the `internal/testenv` package does not exist in the develop tree at `f09fe73`. No attach/detach e2e test named or annotated for VP-033. Blocker: multi-console e2e infra (`internal/testenv.New` + `CreateSession` + `AttachConsole` + `SendKeystroke` + `CollectFrames` + `SessionAlive`). Anchor stories: candidate S-4.03/S-4.04 (or a dedicated e2e-harness story). | 2026-07-06 | formal-verifier (phase6-vp-sweep) |
