---
artifact_id: VP-074
document_type: verification-property
level: L4
version: "1.0"
status: draft
producer: product-owner
timestamp: 2026-06-29T00:00:00
phase: 1b
traces_to: '.factory/specs/architecture/ARCH-INDEX.md'
inputDocuments:
  - '.factory/specs/behavioral-contracts/ss-06/BC-2.06.001.md'
  - '.factory/specs/architecture/ARCH-07-verification-architecture.md'
source_bc: BC-2.06.001
module: internal/metrics
proof_method: unit
feasibility: feasible
verification_lock: false
proof_completed_date: null
lifecycle_status: active
implementing_story: S-5.01
introduced: v0.1.0
modified: []
deprecated: null
---

# VP-074: QualityIndicator Threshold Classification Maps (RTT, Loss) → (Green | Yellow | Red) Correctly

## Property Statement

For each (RTT ms, loss pct) pair the `QualityIndicator` must produce the
exact Quality level specified by BC-2.06.001 PC-2/PC-3/PC-4:

1. Green: RTT p99 ≤ 100 ms AND loss ≤ 5 %
2. Yellow: RTT p99 ∈ (100, 500] ms OR loss ∈ (5, 20] %
3. Red: RTT p99 > 500 ms OR loss > 20 % OR no paths available

Boundary values (RTT = 100, 100.001, 500, 500.001; loss = 5, 5.001, 20, 20.001)
must each resolve to the correct side of the boundary. Exactly three enum
values exist: `Green`, `Yellow`, `Red`; no other value is ever returned.

## Source Contract

- **BC:** BC-2.06.001 — Quality Indicator (green/yellow/red) Derived from Measured Path Latency and Loss
- **Postconditions:** PC-2 (Green threshold), PC-3 (Yellow threshold), PC-4 (Red threshold)

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| unit | `go test` (table-driven) | yes | all 6 nominal regions + 8 exact boundary values; 14 test cases |

## Proof Harness Skeleton

```go
package metrics_test

import (
	"testing"

	"github.com/arcavenae/switchboard/internal/metrics"
)

func TestQualityIndicator_ThresholdClassification(t *testing.T) {
	t.Parallel()

	cases := []struct {
		name    string
		rttMs   uint32
		lossPct uint32
		want    metrics.Quality
	}{
		// --- nominal green ---
		{"green_rtt_low_loss_zero", 15, 0, metrics.Green},
		{"green_rtt_mid_loss_low", 50, 3, metrics.Green},
		// --- green/yellow boundaries ---
		{"boundary_rtt_100_green", 100, 0, metrics.Green},
		{"boundary_rtt_101_yellow", 101, 0, metrics.Yellow},
		{"boundary_loss_5_green", 10, 5, metrics.Green},
		{"boundary_loss_6_yellow", 10, 6, metrics.Yellow},
		// --- nominal yellow ---
		{"yellow_rtt_only", 200, 0, metrics.Yellow},
		{"yellow_loss_only", 10, 10, metrics.Yellow},
		{"yellow_rtt_and_loss", 150, 3, metrics.Yellow},
		// --- yellow/red boundaries ---
		{"boundary_rtt_500_yellow", 500, 0, metrics.Yellow},
		{"boundary_rtt_501_red", 501, 0, metrics.Red},
		{"boundary_loss_20_yellow", 10, 20, metrics.Yellow},
		{"boundary_loss_21_red", 10, 21, metrics.Red},
		// --- nominal red ---
		{"red_rtt_high", 600, 25, metrics.Red},
	}

	for _, tc := range cases {
		tc := tc
		t.Run(tc.name, func(t *testing.T) {
			t.Parallel()
			qi := metrics.NewQualityIndicator(metrics.DefaultThresholds())
			// Downgrade is immediate (BC-2.06.001 Invariant 3 / AC-004).
			// A single update is sufficient to assert classification.
			qi.Update(tc.rttMs, tc.lossPct)
			if got := qi.Current(); got != tc.want {
				t.Errorf("VP-074: Update(%d ms, %d%%): got %s, want %s",
					tc.rttMs, tc.lossPct, got, tc.want)
			}
		})
	}
}
```

## Feasibility Assessment

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Input space size | bounded | 14 discrete cases covering nominal regions + all 8 threshold boundaries |
| Proof complexity | very low | deterministic threshold lookup; no state machine needed beyond single update |
| Tool support | stdlib `testing` | No external dependencies; table-driven pattern |
| Estimated proof time | < 5 ms | In-process; pure computation |
| Pure-core applicable? | Yes | QualityIndicator.Update is a pure function given a fixed threshold config |
| Relation to VP-027 | orthogonal | VP-027 tests transition ordering over sequences; this VP tests single-step threshold mapping |

Note: `metrics_test.go` in story S-5.01 contains 14 unit tests that directly implement
this VP — `TestQualityIndicator_ThresholdClassification` and
`TestQualityIndicator_ThresholdBoundary`. The harness skeleton above is the canonical form.
Reference implementation: `.worktrees/S-5.01/internal/metrics/metrics_test.go`.

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-06-29 | product-owner (BC-2.06.001 VP table disambiguation — split from ambiguous unit/proptest dual-citation of VP-027; L-001 finding) |
