---
artifact_id: VP-053
document_type: verification-property
level: L4
version: "1.4"
status: draft
producer: architect
timestamp: 2026-06-23T00:00:00
last_revised: 2026-07-06
phase: 1c-refinement
traces_to: '.factory/specs/architecture/ARCH-INDEX.md'
inputDocuments:
  - '.factory/specs/behavioral-contracts/ss-01/BC-2.01.002.md'
  - '.factory/specs/architecture/ARCH-02-protocol-stack.md'
source_bc: BC-2.01.002
module: internal/halfchannel
proof_method: proptest
feasibility: feasible
verification_lock: true
proof_completed_date: 2026-07-06
lifecycle_status: active
introduced: v0.1.1
modified:
  - date: 2026-06-24
    actor: architect
    reason: "align harness skeleton with post-increment semantics per BC-2.01.001 canonical vector; resolves S-1.02 adversary pass-01 F-007"
  - date: 2026-06-24
    actor: architect
    reason: "align Property Statement with BC-2.01.002 PC3 — EMPTY_TICK discriminator lives in ChannelFrame.FrameType (0x02), not channel-header flags; resolves S-1.02 adversary pass-04 F-002"
  - 2026-07-06T00:00:00  # v1.3 — audited (VP sweep batch 3, PARTIAL: proof-method drift; deterministic K=20 sweep shipped, gopter proptest deferred)
  - 2026-07-06T00:00:00  # v1.4 — PROVEN (gopter proptest shipped, PR #106 / merge 0516f3a)
---

# VP-053: Empty-Tick Frame Sequence — K Consecutive Empty Ticks Emit K Frames with Contiguous Sequence Numbers

## Property Statement

Given a half-channel with no application data queued for K consecutive ticks (K ≥ 1),
exactly K empty-tick frames are emitted with sequence numbers s, s+1, …, s+K-1, each
carrying zero-length payload, `frame_type=EMPTY_TICK (0x02)`, and a valid HMAC tag.

Formally:

∀ HalfChannel H, ∀ K ∈ [1, 100], let s = H.Seq() before any tick:
  after K consecutive Tick() calls with no queued payload:
  assert len(frames) == K
  ∀ i ∈ [0, K-1]:
    frames[i].ChanSeq == s + uint32(i+1)   // post-increment: first frame has ChanSeq = s+1
    frames[i] carries zero-length Payload
    frames[i].FrameType == FrameTypeEmptyTick (0x02)

VP-018 verifies that a *single* tick emits an empty frame; this VP covers the
multi-tick case and explicitly verifies contiguous sequence numbering across a
run of empty ticks — the invariant that receivers rely on to detect gaps (DI-008).

## Rationale

BC-2.01.002 postconditions 1–3 specify that every empty tick must emit a frame with:
- zero-length payload
- `frame_type = EMPTY_TICK`
- an incremented sequence number

VP-018 exercises the single-tick case. Without VP-053, an implementation could
fail to increment the sequence number on the second consecutive empty tick (a
fence-post bug) and still pass VP-018. The quality-observability subsystem counts
*gaps* in the received sequence — if sequence numbers are not contiguous, the
receiver miscounts missed frames and produces spurious quality degradations
(BC-2.06.002 depends on this regularity).

## Source Contract

- **BC:** BC-2.01.002 — Empty-tick frame is a valid liveness signal
- **Postcondition 1:** "An empty-tick frame is emitted with zero-length payload."
- **Postcondition 2:** "The outer header is fully populated (frame type = EMPTY_TICK, HMAC)."
- **Postcondition 3 (via BC-2.01.001):** "The channel header carries the sequence number."
- **Invariant 1 (DI-008):** "Empty-tick frames are never skipped."
- **Invariant 3:** "An empty-tick frame increments the sequence number."

## Proof Method

| Method | Tool | Bounded? | Coverage |
|--------|------|----------|----------|
| proptest | gopter v0.2.9+ | yes (K ∈ 1–100) | arbitrary K; assert len=K, contiguous seq, EMPTY_TICK type, zero payload, valid HMAC |

## Proof Harness Skeleton

```go
package halfchannel_test

import (
	"testing"
	"time"

	"github.com/leanovate/gopter"
	"github.com/leanovate/gopter/gen"
	"github.com/leanovate/gopter/prop"

	"github.com/arcavenae/switchboard/internal/halfchannel"
)

// TestPropEmptyTickSequence verifies that K consecutive Tick() calls with no
// queued payload produce K frames with contiguous sequence numbers and
// zero-length payloads.
//
// Post-increment semantics (BC-2.01.001 canonical vector "sequence 1..10"):
// Seq() before any tick is 0. The first Tick() increments seq to 1 and returns
// ChanSeq=1. So frames[i].ChanSeq == startSeq + uint32(i+1).
func TestPropEmptyTickSequence(t *testing.T) {
	properties := gopter.NewProperties(nil)

	genK := gen.IntRange(1, 100)

	properties.Property(
		"K empty ticks emit K frames with contiguous seq and zero-length payload",
		prop.ForAll(
			func(k int) bool {
				hc := halfchannel.New(1, halfchannel.Upstream, 10*time.Millisecond)

				// Record seq before any tick; post-increment means frame i has
				// ChanSeq = startSeq + uint32(i+1).
				startSeq := hc.Seq()
				frames := make([]halfchannel.ChannelFrame, 0, k)

				for i := 0; i < k; i++ {
					// No Enqueue call: Tick() with empty pending queue emits a
					// zero-payload frame (BC-2.01.002).
					frames = append(frames, hc.Tick())
				}

				if len(frames) != k {
					return false
				}
				for i, f := range frames {
					// Post-increment: frame i carries sequence startSeq+(i+1).
					if f.ChanSeq != startSeq+uint32(i+1) {
						return false
					}
					if len(f.Payload) != 0 {
						return false
					}
				}
				return true
			},
			genK,
		),
	)

	properties.TestingRun(t)
}
```

## Feasibility Assessment

| Factor | Assessment | Notes |
|--------|-----------|-------|
| Input space size | bounded | K ∈ 1–100; deterministic fake clock; no goroutines needed |
| Proof complexity | low | HalfChannel.Tick with nil payload is a pure state transition; only state is sequence counter |
| Tool support | gopter | Go 1.25.4 compatible; same pattern as VP-016, VP-017, VP-018 |
| Estimated proof time | < 1 min | no I/O; no concurrency; fake clock is instantaneous |
| HMAC verification | in-loop | uses the same VerifyHMAC already proven correct by VP-004/VP-006; test key is a fixture |

## Lifecycle

| Event | Date | Actor |
|-------|------|-------|
| Created | 2026-06-23 | architect |
| Revised | 2026-06-24 | architect |
| v1.3 — audited (Phase 6 VP sweep). Ruling: **PARTIAL** (proof-method drift). Deterministic K=20 representation exists at `internal/halfchannel/halfchannel_test.go:321 TestHalfChannelEmptyTickSequence` covering contiguous seq + EMPTY_TICK type + zero payload; matches VP-025/VP-026/VP-028/VP-029/VP-051 pattern of proof_method=`proptest` shipped as deterministic sweep. Gap: gopter proptest per proof_method declaration. Blocker: Phase-6 formal-verifier gopter harness. Anchor: Phase-6 proptest work. lock unchanged. | 2026-07-06 | formal-verifier (phase6-vp-sweep) |
| v1.4 — PROVEN (Phase 6 burst-3 lane-A gopter proptest shipped). verification_lock false → true, proof_completed_date 2026-07-06. Discharging test (gopter v0.2.9, merge SHA 0516f3a, PR #106): `internal/halfchannel/halfchannel_prop_test.go:TestProp_VP053_EmptyTickSequence` (200 runs, K ∈ [1,100]). Generalizes the deterministic K=20 `TestHalfChannelEmptyTickSequence` to random K: asserts K consecutive empty Tick() calls emit exactly K frames with contiguous ChanSeq (startSeq+1 … startSeq+K per BC-2.01.001 post-increment semantics — first Tick() returns ChanSeq=1), each with zero-length Payload and `FrameType == FrameTypeEmptyTick (0x02)`, plus Seq() advanced by exactly K. Mutation-kill: removing the seq increment inside Tick() (or incrementing only on the first iteration) fails the `f.ChanSeq == startSeq+uint32(i+1)` contiguity check. Verified passing under `go test -race` (full-repo green) + `golangci-lint` clean by coordinator. | 2026-07-06 | formal-verifier (phase6-gapclose lane-A) |

## Revisions

- 2026-06-24 — Property Statement aligned with BC-2.01.002 PC3 — EMPTY_TICK discriminator lives in `ChannelFrame.FrameType` (0x02), not in a channel-header flag bit. Resolves S-1.02 adversary pass-04 finding F-002.
- 2026-06-24 — Harness skeleton updated to post-increment semantics per BC-2.01.001 canonical vector. Resolves S-1.02 adversary pass-01 finding F-007. [previous]
